# Bug ledger — pharos baseline

Migrated verbatim from `SPEC.md` §B on 2026-07-27. Ids B1…B129 are stable; each
entry records cause + fix and links the invariant that now guards the class.
Append new bugs here. Next free id is one past the highest below.

```
id|date|cause|fix
B80|2026-07-16|BROWSER PLAYBACK (Lace incident) — the DirectPlay-verdict sibling of B77. A remote viewer (lace, desktop Linux Firefox, WAN 62.169.136.4 via prototype.aliflix) hung ~11 min loading Supergirl S01E05 (av1-in-Matroska). Loki: PlaybackInfo returned route:"direct" (SupportsDirectStream:true, NO TranscodingUrl); jellyfin-web silently retried PlaybackInfo ~4× over 11 min before falling back to h264-HLS on its own (no server-side 4xx/5xx — invisible). Root cause: `negotiate` returned DirectPlay for the av1/matroska source (client profile matched the container), and `direct_play`/`supports_direct_stream` were set true for ANY DirectPlay verdict — but pharos's `/stream` serves Matroska VERBATIM, relabelled `video/webm`, which a browser's `<video>`/MSE only plays when webm-legal (VP8/9/AV1 + Opus/Vorbis). B77 fixed the REMUX decisions (VideoRemux/AudioRemux) but left the DirectPlay-verdict path stranding a browser with no fallback URL. The B43 `force_webm` net that would have caught it was OFF for lace (config `linux_firefox_h264=true`, B60), and `source_firefox_native` (av1 IS firefox-native) additionally suppressed it. (Same session also hit an uncapped-WAN-transcode rebuffer on E04 + a 34 Mbps movie — separate root cause, B81.)|B80/V40. New `browser_matroska_direct_unplayable` gate: when a BROWSER (Mozilla UA) gets a DirectPlay verdict for a Matroska/MKV container (ffprobe "matroska"/"matroska,webm"; a genuine "webm" container is trusted — natively browser-playable + client-listed) that is NOT `webm_relabel_playable`, `playback_info` downgrades the Decision to a Transcode (h264/aac/ts, carrying the connection-aware ceiling) BEFORE force_webm/direct_play compute — so a TranscodingUrl is emitted, SupportsDirectStream resolves false, and the downstream HLS pipeline (keyed on the registered Decision) transcodes. A Linux-FF that can't decode h264 is still redirected to VP9 by force_webm, which now fires on the non-direct decision. `webm_relabel_playable` moved above the gate. Guard: browser_matroska_fallback_tests (downgrades ONLY an unplayable browser Matroska DirectPlay; genuine webm / native app / non-matroska / already-transcoding / audio-only preserved). Full `just test` 1300/1300.
B81|2026-07-16|BROWSER PLAYBACK (Lace incident, same session as B80) — a remote/WAN viewer rebuffered on high-bitrate transcodes. lace on WAN "Auto" quality: Supergirl S01E04 (10.3 Mbps h264 source) transcoded at ~10 Mbps and a 34 Mbps movie at the 12 Mbps encoder ceiling — far more than a home uplink sustains. Root cause: jellyfin-web "Auto" advertises an effectively-unlimited MaxStreamingBitrate, so `negotiate` left the transcode uncapped and `effective_video_bitrate(None, source)` = min(source, 12M) targeted ~source; there was NO server-side default cap and NO connection awareness, and the emitted TranscodingUrl carried no VideoBitrate param so a client quality-pick didn't reach the segment transcoder.|B81. New `[server].remote_default_bitrate_bps` (default 6 Mbps, 0 disables). `playback_info` clamps a transcode's ceiling to it when the client is on a non-private (WAN) address — `Decision::clamp_video_bitrate` (min with any explicit lower client pick; fills an "Auto"/None cap). client_is_remote (realip_remote_addr → X-Forwarded-For) treats RFC1918/ULA/link-local/loopback/CGNAT-Tailscale as local; unparseable → local (never throttle a LAN viewer). The ceiling rides the TranscodingUrl as `&VideoBitrate=` (h264/vp9/remux) through playback_qs master→variant→segment, so it survives a GC'd session + applies to remux re-encodes. Guards: clamp_video_bitrate_*, qs_video_bitrate_cap_*, min_opt_*, remote_ip_tests (asserts lace's real 62.169.136.4 remote; pod-net/Tailscale/v6 local). Full `just test` 1300/1300.
B82|2026-07-17|LIBRARY BROWSE (Google TV) — the "Movies" and "TV Shows" library grids came back EMPTY on the Jellyfin Android TV app (0.19.9, jellyfin-sdk-kotlin/OkHttp) while jellyfin-web listed fine. No 4xx/5xx — pharos logged `GET /items?...&parentId=<id> → 200` with an empty envelope (invisible as an error). Root cause: the Android TV client browses via `/items?parentId=6dcaebce-be81-6e0b-6dca-ebcebe816e0b&includeItemTypes=Movie&recursive=true` — a DASHED UUID, because jellyfin-sdk-kotlin round-trips every id through a `Uuid` type and re-serialises it dashed. pharos stores library/synth/entity wire_ids in the dashless 32-hex form `library_id_for_root` emits (`{h:016x}{h:016x}`), and `resolve_parent_filter` (+ legacy `restrict_to_parent`) matched `?ParentId=` by RAW string equality (`l.wire_id == pid`, `series_id_for_key(..) == pid`, `item_ids_for_genre(pid)`, …). The dashed id equals none of the ~12 dashless candidates → falls through every branch → `ParentResolution::Empty` → blank grid. jellyfin-web echoes the dashless id verbatim, so it matched and worked — masking the bug. Sibling of the B15 id-format sweep, which canonicalised inbound ROUTE-PATH ids (`parse_item_id` accepts dashed) but never touched ParentId QUERY-PARAM resolution. Diagnosed from pharos pod stdout (Loki was crash-looping); the `http.target` span carried the full dashed querystring.|B82/V41. New `canonical_parent_id(pid)` — dashed 8-4-4-4-12 hex UUID → dashless lowercase 32-hex; every other shape (dashless hex, legacy decimal, non-id) returned borrowed/untouched. Applied at the single `pid` bind in BOTH `resolve_parent_filter` and `restrict_to_parent`, so all downstream `== pid` comparisons (library, media-root, series, season, artist, album, genre, person, studio, collection, tag) accept the dashed form. Guards: canonical_parent_id_tests (dashed→dashless+lowercase incl. dashed all-zeros; non-dashed shapes pass through without allocating), parent_id_dashed_uuid_resolves_same_as_dashless (integration: dashed Movies wire_id resolves the same single item as the dashless id, not an empty grid). Full workspace `cargo nextest` 1303/1303, clippy -D warnings clean.
B83|2026-07-17|SIMILAR ITEMS (mixed library) — "More Like This" for a movie surfaced unrelated content (an AWS training video under "Back to the Future"). Root cause: `similarity_score` gave a `+5` floor to ANY same-`kind` pair, and `items_similar` kept everything scoring `>0`. Movie files rarely carry embedded genre metadata (pharos's only content signal besides series/album), so in the deployed single-root mixed library (`/var/lib/pharos/media`, all content one "mixed" library) nearly every Movie-vs-Movie pair scored exactly 5, passed the gate, and the rail became an alphabetical dump of every Movie-kind item — tie-broken by title, so "AWS…" sorted to the top. The `+5` was originally added to stop Movies appearing under Audio "More Like This", but the audio-vs-video hard gate already covers that; the floor's real effect was to make same-kind sufficient on its own.|B83/V42. `similarity_score`: (1) same-kind `+5` now applies ONLY when a real signal already scored (`if s > 0 && kind == kind`), so it is a tiebreak not a qualifier; (2) new same-immediate-parent-folder signal (`+15`) — in a mixed single-root library the user's folder layout (`…/Movies/*` vs `…/Training/*`) is the most reliable "same content group" signal, so co-located films still cluster while cross-category clips drop out. Result: a movie with no shared genre/series/folder yields a short/empty rail, never unrelated junk. Guards: similarity_score_tests (same-kind-alone → 0; shared-genre → 40; same-folder-alone → 20, cross-folder → 0); existing items_similar_scores_by_series_then_genre + similar_on_music_is_music_only still green. Full workspace `cargo nextest` 1306/1306, clippy -D warnings clean.
B84|2026-07-17|MARK-WATCHED CRASH (Google TV) — marking an episode watched consistently CRASHED the Jellyfin Android TV app. adb: the app calls `POST /UserPlayedItems/{itemId}` (the modern userId-less jellyfin-sdk-kotlin endpoint; the user is the bearer), which pharos did NOT implement — only the legacy `/Users/{userId}/PlayedItems/{itemId}`. The unrouted POST returned an error the SDK then tried to deserialize as a UserItemDataDto and threw, killing the app (logcat: socket PID flipped mid-session = process relaunched). Same missing-modern-endpoint class as the DTO-parity bugs. Item id arrives DASHED (kotlin re-serialises ids), already handled by parse_item_id.|B84. Add the four userId-less routes `POST|DELETE /UserPlayedItems/{id}` + `POST|DELETE /UserFavoriteItems/{id}` (user = bearer, no path userId, no require_bearer_match). Extract the four mutations into shared `op_*` fns so the legacy and userId-less handlers can't drift. Response is the same UserItemDataDto. Guard: userid_less_played_endpoint_marks_and_returns_dto (exercises the dashed-UUID id shape + the favourite variant). Full workspace `cargo nextest` 1307/1307, clippy -D warnings clean.
B85|2026-07-17|TV SHOWS NAVIGATION DEAD (Google TV) — on the Android TV app a Series tile showed no artwork and selecting it did nothing (stayed on the TV Shows screen); "Go to Series" from an episode dead-ended; a season's episode list came back empty. adb: the app requests `GET /Shows/5fe55c65-1a0f-4e9c-5fe5-5c651a0f4e9c/Episodes`, `/Shows/{id}/Seasons`, `/Items/{seriesId}`, `/Items/{seriesId}/Images/Primary` with a DASHED id. pharos's SYNTH ids (series/season/person/collection/library) are resolved by comparing the raw path string to the dashless `series_id_for_key`/`season_id_for_key`/hash — never dashed-normalised (B15's `parse_item_id` covers REAL ids only; B82 covered the ParentId query param only). So every dashed synth id missed → empty season list, 404 artwork, 404 series detail → navigation dead. Same dashed-kotlin-id root as B82, on the route-path + synth-id surface.|B85/V41. Promote `canonical_parent_id` → `pub(crate) canonical_wire_id` (unchanged dash-strip+lowercase of an 8-4-4-4-12 UUID; every other shape untouched) and apply it at each synth-id path choke point: `shows_seasons` + `shows_episodes` (path SeriesId + `?SeasonId=`), `fetch_item_dto` (`/Items/{id}` detail → series/season/collection/library-view), and `serve_image` (person + Series/Season artwork). Guard: dashed_synth_series_id_resolves_across_show_routes (dashed SeriesId resolves /Shows/{id}/Episodes + /Seasons + /Items/{id} detail identically to dashless). Full workspace `cargo nextest` 1308/1308, clippy -D warnings clean.
B89|2026-07-17|POSTERS LOAD SLOWLY (all clients) — the user: "TV Show and Movies posters take far too long to load… I want them near instant." Root cause: every image response (`serve_image` + `serve_local_artwork` + `get_chapter_image`) was built as `HttpResponse::Ok().content_type(..).body(bytes)` with NO `Cache-Control` and NO `ETag`. So neither jellyfin-web's browser cache nor the Android TV app's okhttp/Glide cache could reuse a poster — every grid render and every back-navigation re-requested the FULL set of tiles from the server, re-reading (and for cold tiles re-extracting/re-scaling) each one. The on-disk cache + per-width scaling already existed, but the bytes were shipped over the wire again and again because the client was never told they were cacheable. Real Jellyfin sends a long max-age on its tagged image URLs; pharos sent none.|B89/V45. New `deliver_image` helper stamps `Cache-Control: private, max-age=604800` (7d) + a content `ETag` (served file length+mtime, hex) on every 200, and short-circuits to a bodyless `304` when the client's `If-None-Match` matches (cheap revalidation after the TTL lapses). All read paths — extracted frame, scaled copy, webp/avif re-encode, local sidecar, chapter thumb — route through it; `If-None-Match` is threaded from the four GET/HEAD handlers + the chapter handler (`if_none_match_of(req)`). 404s and the person-photo 302 stay uncached (so new art / a fixed redirect isn't pinned). Guards: served_image_carries_cache_control_and_etag, matching_if_none_match_yields_304. Full workspace `just test` 1316/1316, clippy -D warnings clean. NOTE: this fixes REPEAT loads (the browsing case); first-ever extraction of a sidecar-less poster still pays one ffmpeg frame-grab — scan-time poster warming is a separate, larger lever (disk + scan cost) left for a follow-up decision.
B88|2026-07-17|CHAPTER IMAGES MISSING (Google TV) — chapter thumbnails never appeared on the Jellyfin Android TV app (the user: "chapter images are either not generated or not loading"). Root cause: `ChapterInfoDto` (the `Chapters` array in a `BaseItemDto`) carried only `Name` + `StartPositionTicks` + `ImageDateModified` — it had NO `ImageTag` field at all. Jellyfin's real `ChapterInfo.ImageTag` is a nullable string, and every client (jellyfin-web, jellyfin-sdk-kotlin) gates the whole chapter-image path on it: a chapter thumbnail is rendered — and `/Items/{id}/Images/Chapter/{index}?tag=` is fetched — ONLY when `ImageTag != null`. With the field absent it deserialised as null, so the client treated every chapter as image-less and never requested the frame. The `/items/{id}/images/chapter/{index}` route + `ImageCache::chapter` (on-demand ffmpeg seek to `start_ms`) were already implemented and working — they were simply never called. So "not loading", not "not generated".|B88/V44. Add `image_tag: Option<String>` (`ImageTag`) to `ChapterInfoDto`. Populate per chapter in `BaseItemDto::from_domain`: video → `Some(image_tag_for(item.id, "chapter:{idx}"))` (stable, per-index, so each thumbnail keys distinctly); Audio → `None` (no video frame to extract). The chapter-image route serves by index and ignores the tag value, so the tag only needs to be present + stable. Guard: video_chapters_advertise_a_non_null_image_tag (video chapters all carry distinct non-null tags; audio chapter stays null). Full workspace `just test` 1314/1314, clippy -D warnings clean.
B87|2026-07-17|EPISODES OUT OF ORDER (Google TV) — a season's episode list showed episodes in a scrambled order (Buffy S02E20 as the immediate next of S02E04, then S02E06) on the Jellyfin Android TV app, while jellyfin-web listed them correctly. Root cause: the Android TV app lists a season's episodes via `/Items?parentId=<season>&IncludeItemTypes=Episode&SortBy=ParentIndexNumber,IndexNumber,SortName`. `sort_primary` maps `ParentIndexNumber`/`IndexNumber` → `SortPrimary::TrackOrder`, and the TrackOrder arm of `build_media_query` emitted the AUDIO columns `(disc_number, track_number, Name)`. Those columns are NULL for episodes → every episode tied on disc/track → the sort collapsed to the `Name` (title) then `Id` tiebreak → an effectively alphabetical / hash order. The `ParentIndexNumber`/`IndexNumber` tokens are TYPE-OVERLOADED in Jellyfin (season+episode for an Episode row, disc+track for an Audio row); the TrackOrder mapping only ever handled the audio meaning. jellyfin-web drives its season page off `/Shows/{id}/Episodes` (a separate handler that sorts in-memory by `(season_number, episode_number)`), so it was correct — masking the `/Items` bug.|B87/V43. Add `SortKey::SeasonNumber` (`season_number` column, allowlisted in `sort_key_column`). Make the `SortPrimary::TrackOrder` arm of `build_media_query` kind-aware: episode-scoped (`mq.kinds` contains `Episode`, OR `mq.parent` is a `Series`/`Season` pivot) → `(SeasonNumber, IndexNumber, Name)`; else the album `(DiscNumber, TrackNumber, Name)` unchanged. Guards: episode_sort_tests (unit: episode kind AND Season parent both pick season/episode keys; Audio+Album parent keeps disc/track), episode_list_sorts_by_air_order_not_title (integration: episodes seeded with reverse-alphabetical titles come back in episode-number order over the real `/Items` HTTP path + store). Full workspace `just test` 1313/1313, clippy -D warnings clean.
B86|2026-07-17|MUSIC WON'T PLAY (Google TV) — every music track was listed but hitting play did nothing. pod logs: after a 200 PlaybackInfo the app fetched `GET /audio/{id}/stream?container=mp3&static=true&tag=<ETag>` and got 401 (repeatedly). Native direct-play (ExoPlayer) fetches the media URL RAW with no bearer header — B75 handles this for video by forwarding the MediaSource ETag (== PlaySessionId) as `?tag=` and authorizing it in `stream::authorize_media` (resolves the registered session, matches media_id). But B75 was applied only to `stream_video`/`head_video`; `stream_audio`/`head_audio` were left on the strict `AuthUser` extractor, which 401s a tokenless request before any handler logic → all music DirectPlay dead. (Video worked because its DirectStreamUrl path uses authorize_media; audio carries no DirectStreamUrl and the app builds `/Audio/{id}/stream?static=true&tag=` itself.)|B86. `stream_audio` + `head_audio` now parse the id and call `authorize_media(&state, &req, media_id)` (the ETag `?tag=` capability) exactly like `stream_video`/`head_video`, dropping the `AuthUser` extractor. Tokenless-and-tagless still 401s (route_auth_audit `/Audio/1/stream`=Auth stays green), only a valid item-scoped capability tag passes. Guard: native_capability_etag_authorizes_tokenless_audio_stream (PlaybackInfo → ETag → GET /audio/{id}/stream?tag= must not 401) + seed gained an Audio row. Full workspace `cargo nextest` 1309/1309, clippy -D warnings clean.
B79|2026-07-16|LATENT (found during the B78/V38 json!→DTO sweep, NOT YET SHIPPED as a crash fix): the `/socket` remote-control pushes send `PlayState.Command` (kotlin `PlaystateCommand` enum) and `GeneralCommand.Name` (kotlin `GeneralCommandType` enum) as free strings from `canonical_command(title_case(path_param))` — an arbitrary `/Sessions/{id}/Playing/{command}` or `/Command/{command}` path yields a value outside the enum → a native cast-TARGET client (jellyfin-sdk-kotlin) crashes decoding the enum (same class as B78). Worse, `session_play_request` emits `"Play"` and `is_playstate_command("Play")` routes it into the PlayState branch, but `Play` is NOT a `PlaystateCommand` member (valid set: Stop/Pause/Unpause/NextTrack/PreviousTrack/Seek/Rewind/FastForward/PlayPause) — so even pharos's own cast-play would crash a native target. Not observed live yet (needs a native device as a remote-control TARGET, which the reported crashes weren't).|FIXED 2026-07-16 (V39). `translate_session_command` now routes by the real Jellyfin taxonomy and NEVER emits an out-of-set enum: (1) `Play` → the dedicated `Play` MessageType carrying a typed `PlayRequestData` (`PlayCommand` gated to the `PlayCommand` enum, default `PlayNow`; `ControllingUserId` a real UUID) — no longer a PlayState `Command:"Play"`; (2) a command in the exact `PlaystateCommand` set (`Play` REMOVED from `is_playstate_command`) → `PlayState`; (3) a command in the exact `GeneralCommandType` set → `GeneralCommand`, with a non-null UUID `ControllingUserId` (was `""` — itself an invalid-UUID crash) and `Arguments` coerced to a `Map<String,String>` (scalars stringified, non-strings dropped — was arbitrary JSON, another crash); (4) anything else → DROPPED (`None`) + `tracing::warn`, so a poisoned enum can never reach the wire. Controlling user threaded through `SocketBroadcast::SessionCommand`/`notify_session_command` from the 3 `/Sessions` handlers. SyncPlay `SendCommand` path checked clean (only Unpause/Pause/Seek — valid `SendCommandType`). Guards: translate_session_command_play_is_its_own_message_not_playstate, _play_defaults_invalid_playcommand_to_playnow, _unknown_command_is_dropped_not_poisoned, _routes_general_commands (Arguments now `"60"` string + ControllingUserId asserted). Full `just test` 1294/1294.
B78|2026-07-16|NATIVE ANDROID/GOOGLE TV CRASH (two reported symptoms — home-screen load + mid-playback — one root cause). Live `adb logcat` (device 192.168.1.46, org.jellyfin.androidtv 0.19.9) caught the exact throw: `kotlinx.serialization.MissingFieldException: Field 'Type' is required for type 'org.jellyfin.sdk.model.api.BaseItemDto', but it was missing at path: $.Data[0].NowPlayingItem` unwinding through `SessionInfoDto$$serializer` → `SessionsMessage$$serializer`. The `/socket` `Sessions` broadcast (state PlaybackProgress → socket.rs translate) hand-built `"NowPlayingItem": { "Id": item_id }` as a raw `serde_json::json!` literal with NO `Type`. jellyfin-web (loose JS) tolerates it; the kotlin `BaseItemDto` has exactly two no-default REQUIRED fields — `Id` (present) + `Type` (missing) — so the deserializer throws and ACRA kills the app. It fires on app launch (socket connects, any active session pushes a Sessions snapshot → menu crash) AND every ~10s playback-progress tick (mid-playback crash). Invisible to the B14 kotlin-DTO audit because that audit fixed the typed `*Dto` STRUCTS; this payload bypassed them as a json! literal. No crash log ever reached pharos: jellyfin-androidtv reports via ACRA to its OWN backend, not /ClientLog/Document (0 hits in pod logs). NOT codec-related — Supergirl S01E04 is vanilla h264/eac3/1080p SDR; the DV-RPU log spam was an unrelated file.|B78/V38. `NowPlayingItem` is now a typed `NowPlayingItemLite { Id, Type }` inside typed `SessionsBroadcastEntry`/`SessionPlayStateLite` (socket_messages.rs), serialized not json!-literaled. `Type` = `MediaKind::base_item_kind()` (new canonical PascalCase projection in pharos-core, the inverse of from_wire); the progress handler resolves the item kind via `stores.get`, threads `item_kind: Option<MediaKind>` through the broadcast; kind-unresolved OMITS NowPlayingItem (nullable) rather than emit it Type-less. Guards: translate_playback_progress_emits_sessions_outbound (asserts NowPlayingItem.Type=="Episode") + translate_playback_progress_omits_now_playing_item_when_kind_unknown. FOLLOW-UP (user-directed): audit ALL remaining `serde_json::json!` wire literals → typed DTOs (V38), and move socket serialization to sonic-rs. Verify live on device after deploy.
B77|2026-07-16|BROWSER PLAYBACK: h264 + 6ch eac3 (and ac3/dts) in Matroska — ~4000+ library files — was advertised `SupportsDirectStream:true` to browsers that can't play the raw file. Root-cause of the T98 trace: is_firefox=false was CORRECT (config `linux_firefox_h264=true`, the B60 opt-out, deliberately falses it for Linux FF), NOT a detector bug. The real defect: `SupportsDirectStream` was set true for every DirectPlay/AudioRemux/VideoRemux decision, but pharos's `/stream` serves the source file VERBATIM (no container remux, no audio transcode — only deliver_stream's webm re-label). A `matroska,webm`-container source never matches a single-valued profile container (`"webm".eq("matroska,webm")` is false), so h264+eac3 → VideoRemux, advertised DirectStream → the browser got a raw Matroska (`video/x-matroska` rejected) / undecodable eac3; AudioRemux additionally emitted NO TranscodingUrl, so the client had no fallback. Native players (ExoPlayer) that CAN play mkv+eac3 declare it → DirectPlay, unaffected.|B77/V37. `supports_direct_stream` now true ONLY when the raw file plays as-is: a real DirectPlay, OR a webm-codec Matroska (VP8/9/AV1 + Opus/Vorbis) the re-label serves as `video/webm` to a client whose profile lists a matching webm DirectPlayProfile. AudioRemux(video)+VideoRemux otherwise → DirectStream:false + a master.m3u8 TranscodingUrl (segment surface re-encodes h264+aac; `-c:v copy` per-segment is broken, B45); AudioRemux(audio) → /audio/{id}/universal. Errs safe: worst case is a transcode that still plays, never an unplayable raw stream. Guards: browser_codec_matrix src7/8 + direct_stream_only_advertised_when_raw_file_plays. NOTE recommend live-verify on the deployed jellyfin-web before relying on it.
B72|2026-07-15|NFS disk-hammering bug CLASS — heavy whole-file media ops starve live playback: reachable without the bg_io gate, re-opening the source per-item, refetching uncached, or fanning a cache-miss into concurrent duplicate demuxes; compounded by playback paths that never mark activity so the regulator's gate never parks. Surfaced as Supergirl S01E03 stuck buffering: the file's ~10 embedded text subtitle tracks cold-demuxed one-ffmpeg-open-PER-TRACK (measured 494-520s each over NFS) concurrent with a library scan, while the client direct-streamed stream.mp4 off the same NFS → data starvation. Audit sites: (1) REGULATOR BLIND — note_playback_activity() only in stream.rs:376 (webm); deliver_stream/serve_from_offset/stream_audio/audio_universal/audio_remux never mark it → bg_io stays at BG_IO_MAX during direct-play+audio; stamped once per-request so a long single GET goes stale after BG_IO_BUSY_WINDOW_SECS=12s. (2) trickplay_backfill.rs:303 generate_item bypass=true: 3× whole-file DECODE (widths 320/640/1280) + subtitle demux + font extract, ungated on the watched file. (3) subtitles.rs:267 pre_extract_subtitles loop + library_watch.rs:343 throttle=false: N whole-file demuxes ungated on playback path. (4) subtitles.rs:420 deliver_srt: NO cache — whole-file demux every Stream.srt fetch. (5) image_cache.rs ZERO fetch_locks (sub/trickplay/hls caches all have them): fetch:194 poster/backdrop + ensure_all_attachments:417 fonts stampede (fonts double-fired from subtitles.rs:150 + trickplay_backfill.rs:363) + fixed .batch.tmp scratch races/corrupts. (6) run_ffmpeg_embedded/_ass vtt/ass cold: mutex-deduped, no bg_io permit. (7) waveform.rs:88 run_astats: full decode, uncached, no dedup. (8) segment_backfill.rs:135 analyze_season: 2 opens/episode (bounded, persisted → first-compute). (9) subtitles.rs:1174 discover_sidecars: read_dir walks per PlaybackInfo over NFS lookupcache=none. Cleared: fingerprint.rs (samples size+duration+first/last 1MiB, not whole-file; log error = NFS timeout on sample read).|V34+V35+V36. Make the resource un-gettable without the guard: (a) BgPermit token newtype (only from bg_io.acquire_owned) required BY SIGNATURE on every heavy source op → ungated op won't compile [V34]; (b) PlaybackLease + MeteredBody: centralize media open behind AppState::open_source (source path private), lease restamps note_playback_activity continuously as bytes flow, wired into ALL delivery paths [V35]; (c) shared single-flight helper a cache can't exist without + per-item ONE-open batching + persistent caches for deliver_srt & waveform + per-caller-unique scratch [V36]. Sequence T93-T97; BgPermit first (retrofits sites 2/3/4/6/7 by signature). Guards per-task.
B58|2026-07-14|SyncPlay deep audit — scrubbing the timeline while playing could leave the whole group stuck PAUSED after the seek. SeekTo always sets playback=Paused before broadcasting the Seek, then computed `resume = playback is Playing` = false. A single seek was fine (that line ran while still Playing), but jellyfin-web sends a BURST of Seeks while dragging the scrubber: the 2nd+ seek arriving before the 1st's readiness gate resolved recomputed resume from the already-Paused state → the gate settled the group Paused even though everyone was playing.|B58. SeekTo carries over an already-open gate's resume_playing (which holds the true pre-burst intent) instead of recomputing from the just-set Paused playback; only a seek with NO gate open reads the live playback. Guard: seek_burst_while_playing_stays_playing (paused-clock, resolves via anti-wedge).
B57|2026-07-14|SyncPlay deep audit — three actor edge cases that could confuse or wedge a real party. (a) AddMember was not idempotent: a duplicate New/Join for a member already in the roster (client retry, or the New/Join churn a socket race triggers) re-inserted a fresh MemberRec — resetting its buffering + ignore_wait flags — and re-broadcast a "user joined" toast to everyone, so a mid-buffer re-join could drop the freeze flag and spam phantom joins. (b) SetNewQueue with an empty queue set playing_index=0 on no items, broadcast an empty PlayQueue, and opened a readiness gate → group resolves to Playing with current_playlist_item_id()==None, a wedge only a later non-empty queue clears. (c) An Unpause issued while a member was buffering opens a gate on that member, but ws-native/native clients signal recovery with BufferingEnd, not Ready — and BufferingEnd never cleared the gate's pending set, so it could only release via the 30s anti-wedge (a needless hang on every mid-buffer resume).|B57. (a) AddMember on an existing member_id is now a reconnect: refresh name+last_seen, re-send that member's own Joined+catch-up, reply — but skip the roster-wide MemberJoined and preserve flags. (b) SetNewQueue returns early on an empty item list. (c) BufferingEnd now drops the member from any open WaitingGate and resolves it when it was the last holdout (same as MemberReady/SetIgnoreWait). Guards: duplicate_add_member_is_idempotent, empty_set_new_queue_is_ignored, buffering_end_resolves_an_open_unpause_gate.
B62|2026-07-15|Quick Connect finalize path was near-invisible: `/QuickConnect/Initiate` logged nothing, so a code/secret that later 404'd on poll/authorize couldn't be correlated to its mint. Not a bug per se — an observability gap that blocked diagnosis of B63.|B62. Registry actor logs Initiate (code + secret PREFIX + device + live pending count), Authorize (code + matched/UNKNOWN + live code count), Consume/finalize (secret prefix + known/authorized/consumed). Connect poll at DEBUG (fires ~5s; its 404 already logged by HTTP middleware). Secrets logged as 8-char prefix only (bearer credential). This trace immediately revealed B63 (all three QC steps succeeded server-side → the failure was the response body).
B70|2026-07-15|Diagnostic: the Android TV crashed PARSING the PlaybackInfo 200 response (no server error, no /ClientLog report). Added gated logging of the PlaybackInfo response body (PHAROS_LOG_ALL_REQUESTS) so the offending field could be captured. Revealed B71.
B71|2026-07-15|CRASH ON PLAY: the logged Android-TV PlaybackInfo body showed subtitle `MediaStream.Index` = 1000000, 1000001, … and `DefaultSubtitleStreamIndex` = 1000000 for an item with 12 SIDECAR subtitle files. pharos numbered sidecar streams from a fixed `SIDECAR_BASE_INDEX = 1_000_000` (a discriminator so they wouldn't collide with ffprobe indices). The jellyfin-sdk-kotlin players (Android/Google TV) treat `MediaStream.Index` POSITIONALLY (array/track-selector index) and crash out-of-bounds on the sparse 1_000_000; jellyfin-web tolerated the gap, so it was invisible until a native client. Real Jellyfin uses contiguous indices.|B71. Sidecar wire indices are now PER-ITEM contiguous — `sidecar_base_index(probe)` = one past the highest real ffprobe stream index — so every emitted `Index` is a real, small, collision-free position. The stream builder AND the 3 subtitle-fetch handler sites compute the same base identically (removed the fixed `SIDECAR_BASE_INDEX` const), so wire index ↔ source stays consistent. Toward the user's "make invalid states unrepresentable" — a full StreamIndex newtype / sequential re-index is the follow-up. Guard: sidecar_subtitle_indices_are_contiguous_not_a_sentinel. Diagnostic B70 can be reverted (logAllRequests off) once confirmed.
B69|2026-07-15|Proactive kotlin-audit sweep (post-B68, "make invalid states unrepresentable") found two more latent strict-client crashes in inline json!: (a) `GET /Tags` emitted `Type:"Tag"` — NOT a valid kotlin BaseItemKind enum member (crashes a TV tag-browse); (b) `GET /MediaSegments/{itemId}` (Skip Intro/Outro during playback) set `Id` to `"{item_id}:{idx}"` — the kotlin MediaSegmentDto types Id as UUID, so the non-UUID string crashes the strict client mid-playback. Also: the B68 fix (mixed→null CollectionType, Type Tag→Folder) broke three tests that had CODIFIED the buggy values ("mixed", "Tag") — caught only by the full suite, not the targeted runs (lesson: run full `just test` before push).|B69. New typed MediaSegmentDto with a deterministic-UUIDv5 Id constructor (malformed-id unrepresentable); build_media_segments returns Vec<MediaSegmentDto> instead of hand-built json!. /Tags Type "Tag"→"Folder" (valid BaseItemKind; IsFolder already true). Updated the 3 codified-bug tests to the correct values. Guards: media_segment_id_is_a_deterministic_uuid + the updated library/tag tests. First increment of the type-system sweep toward the full audit (§T) — the rest of the inline item json! still needs a typed BaseItemKind enum + BaseItemDto structs.
B68|2026-07-15|The Android TV got past login/capabilities/Latest (B64/B65/B67) and crashed building the home screen with NO server error and NO crash report reaching /ClientLog — the signature of a strict kotlin client crashing while PARSING a 200 body. pharos logs only error statuses, so the 200 sequence was invisible; added opt-in full request logging (PHAROS_LOG_ALL_REQUESTS via config.obs.logAllRequests) to trace it. The sequence ended at `GET /UserViews` (200). Cause: the inline CollectionFolder json! carried a PARTIAL `UserData` — `{Played, PlayCount}` — but the kotlin `UserItemDataDto` requires SIX non-null fields (playbackPositionTicks, playCount, isFavorite, played, key, itemId); a present-but-incomplete nested object throws the parser. Also `CollectionType:"mixed"` is not a valid kotlin `CollectionType` enum value (Jellyfin mixed libraries send null). Both crash the strict client on an otherwise-200 response.|B68. folder_user_data(item_id) emits the full UserItemDataDto for every synthesised folder/stub (library views ×3, series, season — 5 sites); wire_collection_type maps "mixed"/"" → null. Guards: userviews_items_carry_kotlin_complete_userdata (asserts each view's UserData has all 6 fields + CollectionType != "mixed"). Diagnosis tooling: request logging (B68). This is [[project_client_dialect_compat]] Class 2 in INLINE json! (structs were audited clean; the hand-built json! responses are the remaining surface — motivates the full inline-json audit).
B67|2026-07-15|After B65, the Android TV crashed AGAIN building its home screen: `GET /Items/Latest` → 400 "invalid id". pharos only registered `/Users/{id}/Items/Latest`; the TV uses the path-less `/Items/Latest`, which fell through to `/Items/{id}` (id="latest") → parse fail → 400 → crash. Same path-less-alias class as B65's /UserItems/Resume — the jellyfin-sdk-kotlin apps use the newer path-less user endpoints while pharos registered only the legacy `/Users/{userId}/...` forms.|B67. Register `/Items/Latest` (BEFORE `/Items/{id}` so `latest` isn't parsed as an id) → items_latest (user from bearer) sharing a new latest_items() core with the path form. Audited the other `/Users/{userId}/...` routes: Latest/Resume now aliased; NextUp/Views already had `/Shows/NextUp` + `/UserViews`; favorite/played mutations are path-less-aliasable but not boot-critical (deferred). Guard: items_latest_alias_not_400.
B66|2026-07-15|`POST /ClientLog/Document` was a 204 stub that DISCARDED the body — so when the Android TV crashed and "sent a crash report", the server kept nothing, and the B65 crash had to be reconstructed from access logs. Make it functional: log the report to the server stream (kubectl/Loki — where we read it) capped at 16KB inline, write the full body to `<log_dir>/clientlog/<ts>_<client>.log` when a log dir is set, and return the real `ClientLogDocumentResponseDto {FileName}` (typed DTO via the SIMD wire helper). Auth best-effort so a crashing client's report always lands. Guard: client_log_document_returns_filename.
B65|2026-07-15|After B63/B64 fixed Quick Connect LOGIN, the Android TV then crashed ("Something went wrong, a crash report was sent") immediately post-login. Two errors right after the finalize: (a) `POST /Sessions/Capabilities` → 400 — the TV sends its capabilities as QUERY params with NO body (`?playableMediaTypes=Video&playableMediaTypes=Audio&supportedCommands=...&supportsMediaControl=true`), but pharos's handler required `web::Json<CapabilitiesBody>`, which 400s with no body → the strict client crashed. (real Jellyfin: `/Capabilities` = query params, `/Capabilities/Full` = JSON body; pharos routed both to the body handler.) (b) `GET /UserItems/Resume` → 404 — pharos only registered the legacy `/Users/{id}/Items/Resume`; the TV uses the newer path-less `/UserItems/Resume`, so "Continue Watching" 404'd. The crash-report upload (`/ClientLog/Document`) is a 204 stub so its body wasn't captured — diagnosed from the 400/404 access logs instead.|B65. capabilities() body is now Option<web::Json<_>>; a body-less POST parses the query (caps_from_query: case-insensitive, repeated keys → Vec, infallible) so it always 204s. Added the /UserItems/Resume route as a path-less alias (user from bearer) sharing a new resume_items() core with /Users/{id}/Items/Resume. Guards: capabilities_query_form_no_body_returns_204, capabilities_full_json_body_still_returns_204, user_items_resume_alias_returns_200. Camelcase-query class = [[project_client_dialect_compat]] Class 1.
B64|2026-07-15|Quick Connect STILL failed after B63 with the same "Unable to connect to server", server flow still fully succeeding (finalize consumed=True, token issued). Second kotlin-required field gap, this time nested deeper: the AuthenticationResult's `User.Policy` (jellyfin-sdk-kotlin `UserPolicy`) has ~30 non-null properties; pharos's UserPolicyDto had all but two — `authenticationProviderId` and `passwordResetProviderId` (both non-null `String`, no `?`). The Android/Google-TV SDK threw parsing UserDto.policy → whole response rejected. Diffed EVERY nested DTO of the finalize (UserDto/UserPolicy/UserConfiguration/SessionInfo) against the fetched kotlin models to find it.|B64. UserPolicyDto emits authenticationProviderId + passwordResetProviderId (real-Jellyfin default provider ids; any non-empty string satisfies the non-null contract). Guard upgraded from an array-presence check to `kotlin_strict_auth_result`: the QC finalize body is deserialized into structs mirroring the kotlin models' NON-null fields, so serde fails the test on ANY future missing required field — before it reaches a device. Same class as [[project_client_dialect_compat]]; motivated the full DTO audit (§T).
B63|2026-07-15|Quick Connect on a TV: code entered fine, phone authorized fine, but the TV showed "Unable to connect to server". B62 tracing proved the ENTIRE server flow succeeded — Initiate (code minted) → Authorize (matched) → finalize (secret_known, token issued), all HTTP 200. The failure was the finalize RESPONSE body: pharos's `SessionInfoDto` (inside the AuthenticationResult) omitted two NON-nullable lists of the jellyfin-sdk-kotlin `SessionInfoDto` — `playableMediaTypes: List<MediaType>` and `supportedCommands: List<GeneralCommandType>` (both required, no `?`). The Android/Google-TV SDK threw deserializing the response, surfaced as a connection error even though the token was already issued. jellyfin-web (JS) ignored their absence, so /Users/AuthenticateByName never caught it. NOT the pod-restart / in-memory-state loss first suspected (the user correctly pushed back on that).|B63. SessionInfoDto carries PlayableMediaTypes (["Video","Audio","Photo"]) + SupportedCommands ([] — a valid non-null list). Applies to BOTH the QC finalize and /Users/AuthenticateByName (same DTO), so password login on strict kotlin clients is hardened too. Guard: full_flow QC test asserts the finalize SessionInfo carries both as arrays. Diff class = [[project_client_dialect_compat]] (kotlin-required response fields).
B61|2026-07-14|Quick Connect on a TV showed the 6-digit code, then it VANISHED within seconds — before the user could type it on their phone. Root cause: the `/QuickConnect/Connect` poll response (the TV polls it every ~5s) was a PARTIAL QuickConnectResult — missing DeviceName/AppName/AppVersion, which Initiate correctly includes. Those are non-null strings in Jellyfin's C# QuickConnectResult; the Android/Google-TV kotlin SDK rejects a response missing them (the same reason Initiate had to add them). So Initiate rendered the code, then the first poll's response failed kotlin deserialization → the TV app treated Quick Connect as failed and hid the code. The registry TTL (300s) was never the issue. Missed because the QC integration test asserted the metadata on Initiate but the Connect-poll assertion only checked Authenticated/Secret.|B61. Store the initiating client's device_name/app_name/app_version in the PendingRequest (from the Initiate X-Emby-Authorization header) and echo them on BOTH Initiate and the Connect poll, so the poll is a full QuickConnectResult. Guard: connect_poll_includes_device_and_app_metadata (asserts the poll carries DeviceName/AppName/AppVersion/DateAdded as non-null strings).
B60|2026-07-14|SyncPlay stream of Sabrina S01E01 hung for alison (Linux Firefox, member 36bce9b8). Traces: her client was on the VP9 path (force_webm — B43 forces desktop Linux Firefox to VP9 because some builds' MSE can't decode h264 despite canPlayType saying "probably"), and its SEPARATE audio rendition stalled — `/videos/<id>/vp9/audio/a409.m4s` returned "audio segment not ready" 8× (a deep-segment seeked audio session that hadn't produced a409 within the 5s poll, worsened by concurrent load), so her player got no audio → buffering → freeze loop. B55's anti-wedge resumed the GROUP every 30s (worked), but her client couldn't recover. B59 already moved Windows/Chrome/Mac off VP9; Linux FF was the lone holdout on the fragile split-audio path.|B60. New `[server].linux_firefox_h264` config flag (default false = keep the B43 VP9 force) gates the `is_firefox` force: set true and desktop Linux Firefox is served h264, joining the shared encode (V33/V59) and leaving the VP9 split-audio path entirely. Reversible without a code change if a Linux FF genuinely can't decode h264 in MSE (playback fatals → flip back). Enabled in the chart (linuxFirefoxH264: true) — modern Linux Firefox decodes h264 in MSE. PlaybackInfo codec-decision trace now also logs client_offers_h264 (evidence: does the hung client even advertise h264?).
B59|2026-07-14|jana joined a SyncPlay party with alison+lace and never got a stream, while the group played fine. Traces: alison+lace on h264 HLS (`hls1/*.ts`), but jana's browser requested `vp9/30.m4s`, `vp9/36.m4s` and then bailed — a cold, separate VP9 encode that stalled. Root cause: encode sharing keys on the negotiated codec (cache key has no user/device but DOES include codec), so it only shares when members land on the SAME codec. `negotiate` picked the transcode video codec via `pick_first_csv(tp.video_codec)` = the client's FIRST-listed codec; jana's Windows Firefox jellyfin-web listed VP9 first, so she diverged onto VP9 (software, slow) instead of sharing alison+lace's warm h264. Her Windows Firefox can decode h264 fine (the Linux-FF `force_webm` quirk correctly excludes Windows), so the VP9 route was avoidable — she fell into it purely via first-listed-codec.|V33. `negotiate` now prefers h264 when the client's transcoding profile offers it (pick_preferred_video_codec), so mixed-browser groups converge on one shared h264 encode; VP9/AV1-only clients keep their codec, and Linux-FF force_webm still overrides to VP9. Added a `playbackinfo: codec decision` trace (is_firefox/client_offers_vp9/force_webm/negotiated codec/route) to make the next divergence self-evident. Guards: prefers_h264_over_first_listed_video_codec, transcode_target_prefers_h264_when_client_lists_vp9_first.
B56|2026-07-14|SyncPlay audit (deep pass) found the member delivery sink table (`MemberSinks`, DashMap<member_id, sink>) was NOT generation-fenced, unlike the hub's own per-device session store. Two ways a live socket got silently blackholed (matching the "got stuck / didn't start playing" reports): (a) two connects for one deterministic member_id race their `sinks.insert` — register() is serialized so gens are ordered, but the subsequent insert into MemberSinks is a plain last-writer-wins DashMap write, so the OLDER task's insert landing after the newer's overwrites the live sink with a dead one; (b) `add_caller_to_group` (HTTP New/Join) re-inserts the sink it resolved from the hub, but if a reconnect installed a fresh sink between resolve and insert, the stale snapshot clobbers it. The disconnect `remove` was likewise unconditional — a late teardown from an older socket could wipe the sink a reconnect just registered. A blackholed member receives no Play/Pause/PlayQueue/GroupJoined until its next reconnect.|V32. MemberSinks::insert/remove now carry the owning socket's conn_gen: insert lands only when gen ≥ stored, remove only when gen == stored (dashmap remove_if). Jellyfin /socket threads reg.gen (added conn_gen to ResolvedSession so the HTTP path fences too); WS-native /sync/v1/ws uses a process-wide monotonic counter; the WS-native immediate teardown also gen-checks before RemoveMember. Guards: stale_generation_insert_does_not_clobber_the_live_sink, stale_generation_remove_spares_the_live_sink.
B55|2026-07-14|SyncPlay audit (post-B54) found the V19 buffering freeze could wedge the group FOREVER, matching the live "member buffering — freezing group" traces that survived every prior fix. One member's BufferingStart pauses the whole group and sets group_paused_due_to_buffering; the only paths that cleared it were BufferingEnd/Ready from a STILL-PRESENT member. So if the sole buffering member disconnected mid-buffer (socket drop / tab close / crash) — no BufferingEnd ever comes — remove_member dropped it from the roster + readiness gate but left the freeze standing, stranding everyone else paused. Two more holes with no safety net: a member that simply buffers forever (slow link) had NO deadline (unlike the readiness gate's 30s anti-wedge), and a group hydrated mid-freeze on a rolling deploy restored the freeze flag but cleared every member's buffering flag, so the BufferingEnd auto-resume could never fire — frozen from the first message.|V31. remove_member now lifts the freeze immediately when the departing member was the last one buffering (same auto-resume as BufferingEnd). buffering_since (monotonic, not persisted) arms a BUFFERING_MAX_MS=30s anti-wedge in the actor select loop: on expiry it force-clears all buffering flags and resumes (or disarms if a readiness gate owns the resume). apply_persist re-arms buffering_since when it restores a frozen group. Guards: buffering_freeze_lifts_when_sole_buffering_member_leaves, buffering_freeze_times_out_via_anti_wedge (paused-clock).
B54|2026-07-14|Group formed (B53 fixed the member collision — alison+jana now distinct members) but playback never started: the group had no queue, no SetNewQueue ever POSTed. Root cause: a socket-registration race. jellyfin-web opens /socket then immediately POSTs /SyncPlay/New; the socket's hub registration runs in a spawned task that lagged the HTTP handler, so add_caller_to_group's single hub.resolve missed ('New/Join but no /socket registered', 19:14:25Z), the member was never added, the fresh group died member-less, the creator never received GroupJoined, and jellyfin-web's enableSyncPlay→time-sync→SetNewQueue→play chain stalled (nothing to watch). A confused retry then churned New/Join/Leave.|resolve_with_retry: New/Join poll hub.resolve for up to ~2s (50ms steps) so a socket registering just after the group-forming POST still admits the member; normal case resolves first try. Extended (same race, wider surface): dispatch() — the path for EVERY group-forming command, incl. SetNewQueue — resolves with the same retry, so the queue-load POST that races its own socket registration also admits instead of silently dropping (a dropped SetNewQueue = group with nothing to play). Guards: resolve_with_retry_wins_the_socket_registration_race.
B53|2026-07-14|Alison+Lace+Jana couldn't watch Code Geass together: socket disconnect/reconnect war + repeated buffering-freeze (traces 18:27-18:30). jellyfin-web's deviceId is browser-derived (live socket key = base64 of the User-Agent), so three different USERS on the same Firefox sent the SAME deviceId; SyncPlay member ids derive deterministically from deviceId, so all three collapsed into ONE member and fought over the single socket (each connect bumped the prior). Also surfaced 3 different media transcoding for one 'group' — the collision desynced them onto separate items (defeating encode sharing).|SyncPlay session key folds the authenticated user into the deviceId (AuthSession::sync_key = user_id:device_id), used identically by /socket registration + every HTTP command handler; different users on one browser are now distinct members, same-user reconnect still stable. Untouched: the segment cache key (no user/device), so same-content group playback still shares ONE encode. Guards: sync_key_separates_users_on_the_same_device (unit); syncplay-two-users.spec.ts (2-user same-deviceId Playwright: both stay connected, leader Play reaches both).
B52|2026-07-14|Two live playback freezes (Supergirl S01E01 h264 + S01E03 VP9), both CPU starvation. (a) B49 wired the whole-file subtitle event-window scan into pre_extract_subtitles, which the LIBRARY-WIDE warm-all runs for every item — so each fresh deploy stormed the libav pool with library-wide whole-file NFS demuxes (traces: trickplay op waited 841s for a worker; 52 warm passes + 8 failing scans during one playback), starving live transcodes → 15-32s segments (6s budget) → freeze; amplified by many redeploys each restarting the warm-all. (b) B50's 40M ceiling is not realtime-encodable by software VP9 (a 1080p segment took 32s; the B50 bench used synthetic noise).|Never scan event windows from a warm pass — the played item's windows scan on demand from the segment handler (gate_image_sub_burn), one item, bg-IO gated + deduped. Bitrate ceiling 40M→12M (real win over the old 8M, within the measured ~1.5-2x realtime VP9 1080p margin, source-bounded). Amends B49/B50. Guards: effective_video_bitrate/target_video_bitrate window tests updated to the 12M ceiling.
B51|2026-07-14|Playback hung every time a subtitle appeared. Two causes: (1) prefetch cloned the served segment's GATED opts — during a quiet (burn-off) stretch it cached upcoming subtitle segments WITHOUT the burn, a different cache key from the client's real request WITH the sub → guaranteed miss + live encode right when the sub showed; (2) burn segments encode ~2x slower (overlay + B50's higher bitrate ≈10s/6s seg) and cluster at dialogue, so the shallow near-prefetch couldn't absorb a run of them.|Prefetch carries the client's ACTUAL subtitle pick (pre-gate) and gates each target independently (cache key matches what the client requests); window-aware deep prefetch front-loads the upcoming BURN segments (from the B49 event windows) during quiet stretches — ~2.5min lookahead, capped per request, rolled forward. Guards: burn_prefetch_picks_only_window_segments_capped.
B50|2026-07-14|VP9 playback visibly grainy: jellyfin-web's quality picker offered 40 Mbps but pharos hard-clamped every transcode to HLS_MAX_BITRATE_BPS=8 Mbps (`cap.min(8M)` in both the VP9 segment path and the h264 Transcode arm) — a high-bitrate source starved into 8 Mbps VP9 at the realtime encoder. Benchmarked: SSIM 0.718@8M vs 0.757@25M on grainy 1080p; cpu-used made no difference (8≈6≈5) and cpu-used 4 fell below realtime — so bitrate, not encoder speed, was the whole problem.|Ceiling 8M→40M; new effective_video_bitrate(cap, source) honours the negotiated cap bounded by source bitrate (never over-encode a low-bitrate source) and ceiling. cpu-used stays 8 (keeps 3.2x realtime margin for concurrent streams). Guards: effective_video_bitrate_honours_cap_bounded_by_source, target_video_bitrate_clamps_into_window (updated).
B49|2026-07-14|Avatar unwatchable during first gated play: B46's event-window scan ran UNGATED on first play — whole-file NFS read (4.6 min) of the movie being streamed; burn segments ballooned 7-11s → 46-56s. Compounded by a pod reschedule (12:32Z) that discarded the first in-flight scan + re-ran boot backfills (133 corrupt-file re-probes, libav pool saturated) over the live session. Scan-on-first-play was the wrong moment: the cost lands exactly when a viewer is present.|Window scan takes a shared adaptive bg-IO permit for its whole run (regulator trickles it during playback); pre_extract_subtitles kicks the once-ever scan for every image sub track (rel idx = position among ALL sub streams, the [0:s:N] convention) so scan-time/warm-all/playback-priority passes pre-generate windows before first play. Amends V31.
B48|2026-07-14|"More From {artist}" showed TV shows + movies and "More Like This" showed TV instead of similar music: IncludeItemTypes=MusicAlbum maps to NO store kind so the type filter silently no-op'd (V-lesson: unknown enum token = no filter) and AlbumArtistIds/ContributingArtistIds/ExcludeItemIds were unknown query params (silently ignored); synth album/artist ids returned empty from /Similar while audio tracks could surface same-genre-string movies ("Nu Metal" documentary) because similarity never gated media class.|MusicAlbum-only requests short-circuit to synthesized albums honouring the artist-role filters (contributing = performs-on-but-doesn't-own); music_similar scores albums (same album artist + genre-token overlap, minus ExcludeArtistIds) and genre-adjacent artists; similarity_score hard-gates music↔music / video↔video. Guards: more_from_artist_rail_is_albums_only, appears_on_rail_shows_contributions_not_own_albums, similar_on_music_is_music_only.
B47|2026-07-14|Artist + album detail pages effectively empty: ParentId={artist} returned raw Audio tracks while jellyfin-web's artist page renders a discography of ALBUM cards (queries verified against the deployed 10.11.8 bundle); album tracks came back title-ordered because SortBy=ParentIndexNumber,IndexNumber mapped to SortName and the probe never captured track/disc/year tags at all.|Probe schema v2 adds track_number/disc_number/year (both probers, columns 0037, re-probe backfill); Audio DTOs emit IndexNumber/ParentIndexNumber/ProductionYear; ParentId={artist} with no type filter synthesizes the artist's albums (+loose tracks); TrackOrder sort maps to disc,track,name SQL. Guards: artist_children_are_albums_plus_loose_tracks, album_children_are_tracks_in_track_order (exact bundle queries).
B46|2026-07-14|Na'vi (forced PGS) burn caused constant long buffering: burn segments ran BELOW realtime — traces show 5.7-11.5s per 6s VP9 segment vs ~2.5s unburned (spikes 21/27s, outliers 170-225s), wait=0 throughout so pure encode cost, and B44's default-burn made every Avatar segment pay it. But the forced track is SPARSE — most segments contain no subtitle event, so the overlay decode+composite+re-encode was pure waste on the majority of the timeline. Outliers remain unattributed (no seek/burn fields or slow-segment WARN existed).|V31. Event-window gating: one whole-file packet scan per (file,mtime,track) on the libav pool feeds per-segment burn gating; empty windows skip the burn + share the subtitle-off cache key. First watch burns until the scan lands (fail-open), then only event-bearing segments pay. Slow segments (>3× content duration) WARN with full attribution. Guards: scan_recovers_image_sub_event_windows (behavioral, real mux), event_windows_round_trip_and_fail_open, overlap_test_covers_edges, merge_collapses_overlaps_and_keeps_gaps.
B45|2026-07-14|Avatar + Supergirl wouldn't START playing on Mac Firefox (post-B43): B43 moved Mac FF off the VP9 force onto the h264 `/hls1` surface, whose h264-source path stream-copied video (`Decision::VideoRemux` + the no-session fallback both emitted `-c:v copy`). Copy segments are broken three ways (live-probed via replayed trace URLs): `-output_ts_offset` is inert under copy (ffmpeg 8.1) so EVERY segment restarts at PTS≈0; copy cuts on source keyframes so durations drift off the EXTINF grid (seg1 = 8.17 s vs 6.006 advertised); and 5.1 AAC passed through 6-channel — undecodable in Firefox MSE → fatal append on segment 0 → hls.js variant-fallback (720p seg0 → main seg0 → dead, the exact trace signature at 07:16Z). Chrome tolerates all three, so X11-Chrome sessions masked it for days. Even re-encoded segments carried +1.4 s of default mpegts muxdelay skew. B41's guard asserted the argv STRING, never ffmpeg's actual output — flag-inert-under-copy shipped green.|V30. Segmented surface never copies: VideoRemux arm + fallback both re-encode H.264 (`target_video_bitrate` cap), AAC gains `-ac 2` (Opus precedent), mpegts gains `-muxdelay 0 -muxpreload 0`, `hls_output_codecs_string` always advertises the re-encode avc1 token, HLS_GEN_VERSION 2→3 wipes the poisoned cache. Guards: mid_timeline_segment_carries_true_pts_and_stereo_audio (BEHAVIORAL — real ffmpeg + ffprobe on the production argv), video_remux_session_reencodes_on_segment_surface, fallback_reencodes_even_h264_source, fallback_burns_image_subs_on_h264_source, mpegts_zeroes_the_mux_delay, aac_downmixes_to_stereo.
B44|2026-07-14|Avatar's Na'vi sections played unsubtitled by default: the forced-Na'vi PGS track ships default+forced in the container, the probe captured both flags, and PlaybackInfo advertised it via DefaultSubtitleStreamIndex — but `stream_selection` only baked CLIENT-provided subtitle indices into the TranscodingUrl. With no explicit pick, the client believed the default track was active while the transcode burned nothing (image subs render ONLY via burn). Forced tracks exist precisely so untranslated dialogue plays subtitled with zero user action; real Jellyfin burns them by default.|When neither query nor body carries SubtitleStreamIndex, the transcode URL now falls back to the container's default-disposition track IF it's an image sub (text defaults keep delivering External for client-side render); an explicit `-1` (subs off) still wins. Guards: default_image_sub_is_baked_without_client_pick, explicit_subtitles_off_beats_the_default_track.
B43|2026-07-14|Seeks felt slow (~2-5s) for everyone: the Firefox VP9 force (`force_webm`) matched EVERY Firefox UA. The quirk exists because DESKTOP-LINUX distro Firefox builds can lack the system H.264 decoder while canPlayType still says "probably" — but Firefox on macOS (VideoToolbox), Windows (Media Foundation) and Android (MediaCodec) decodes H.264 natively. Forcing those onto VP9 turned an h264 source's near-instant remux (stream copy ≈ NFS read speed) or direct play into a ~2.5s-per-segment libvpx encode — the dominant cost of every seek (and 3 pipelined seek segments contend, so worse).|`is_firefox` now requires `Linux` in the UA and not `Android`: only desktop-Linux Firefox keeps the force; Mac/Windows/Android Firefox negotiate normally (h264 direct play / remux / h264 HLS). Linux Firefox seeks stay bounded by one VP9 landing-segment encode (vp9 path already prefetches ahead). Guards: mac_firefox_is_not_forced_onto_vp9 (dual h264+vp9 profile → direct play), linux_firefox_still_forced_onto_vp9.
B42|2026-07-14|Seeking within Avatar stalled (post-B41): video segments transcode per-request (seek-instant) but the VP9 AUDIO rendition is ONE detached whole-file ffmpeg session writing a0→aN sequentially from 0. A deep seek requested a996.m4s while the encoder was ~1min into crawling the whole ~3h file over NFS → 404 "audio segment not ready" (live log 06:08:45Z) → hls.js starved the audio track and the seek hung. Any seek beyond the encoder's progress in a freshly-started title hit this.|ensure_audio_hls_covering(want_seg): a request ≤20 segments past the running session's progress just waits (the poll budget covers it); anything further spawns a SECOND seeked session — `-ss <seg*6>` input seek, `-start_number <seg>` (absolute names), `-output_ts_offset <seg*6>` (source-anchored tfdt — a PTS-0 fragment buffers at 0:00, the B41 class), throwaway playlist name so the from-0 session's audio.m3u8 done-marker is never clobbered. Sessions share the dir; overlaps rewrite identical bytes. Guards: audio_hls_args_from_zero_has_no_seek_or_offset, audio_hls_args_seek_session_is_source_anchored.
B41|2026-07-14|Selecting a (burned/PGS) subtitle restarted the movie from 0:00. Two compounding server bugs (Loki-traced, Avatar 05:18Z): (1) the H.264 HLS TranscodingUrl dropped `{stream_selection}` — the VP9 URL threads AudioStreamIndex/SubtitleStreamIndex through, the h264 `master.m3u8` URL didn't — so the selected PGS sub never reached the segment handler (burn=False on every segment; also an explicit audio pick fell back to default whenever the client landed on the h264 surface); (2) `.ts` segments carried NO `-output_ts_offset` (only the fMP4/VP9 path patches timestamps), so every mpegts segment starts at PTS≈0 — fine for linear play from 0 (hls.js re-anchors), fatal for a MID-TIMELINE start: the sub-switch changeStream resumed at seg ~1204 (hls.js startPosition = resume ticks on pharos's full-timeline playlist), the fetched segments buffered near PTS 0 while the playhead sat at ~2h → permanent stall → the user's retry restarted at 0:00.|Both HLS video TranscodingUrls (h264 Transcode + VideoRemux) carry `{stream_selection}`; mpegts segments get `-output_ts_offset <start>` like the fMP4 path; HlsSegmentCache gains a `.gen_version` wipe-on-mismatch marker (bumped to 2) so old PTS-0 cached segments never mix with offset ones in one session. Guards: h264_master_url_carries_stream_selection (playbackinfo), mpegts_segment_carries_output_ts_offset / mpegts_segment_at_zero_has_no_offset (transcode).
B40|2026-07-14|Avatar subtitles dead: its subs are PGS (image); pharos correctly routes image subs to BURN-IN (text delivers out-of-band, ADR-0006) — but the burn used ffmpeg's `subtitles=` filter, which is libass/TEXT-ONLY. Every burned segment aborted with "Only text based subtitles are currently supported" → 500 on every init/segment request with SubtitleStreamIndex set (live: /videos/{id}/vp9/init.mp4?…&SubtitleStreamIndex=3 → 500). Since BOTH call sites gate burn to image subs, the burn path could never have worked at all. The text-filter burn also forced OUTPUT seeking (decode-from-0 per segment) — still wired in for the broken path.|V29. Burn rides `-filter_complex "[0:v:0][0:s:N]overlay=eof_action=pass[,chain][vout]"` with explicit `-map [vout]` + audio map (filter_complex disables default stream selection); chains ahead of the VAAPI hwupload parts; `eof_action=pass` keeps video flowing through sub-less segment windows. overlay reads the sub stream from the SAME single input, so seeks are INPUT-side again (fast + video/sub aligned). Dead `subtitles=` escape helper removed. Guards: burn_subtitle_uses_overlay_filter_complex, burn_subtitle_with_explicit_audio_maps_that_track, burn_subtitle_with_seek_uses_input_seek.
B39|2026-07-14|Avatar showed trickplay previews up to 1h39m58s and grey boxes after: generation was INTERRUPTED partway (fixed 900s heavy-op timeout — a ~3h film needs ~25min at the observed 6-7× realtime NFS decode rate; worker/pod death on deploys is the second interruption class) and the tile-0 "generated?" probe then treated the truncated set as complete forever: advertised in DTOs, never re-attempted. The libav worker writes sheets straight into the served dir (no tmp/rename), so partial output persists.|V29. `.complete` marker per (media,width) written only after every sheet lands; is_generated + the boot prime walk key on the marker; ensure_generated verifies marker-less sets against the expected sheet count (±1 trailing sheet VFR tolerance) — complete pre-marker sets stamp without re-decoding, truncated ones wipe + regenerate; backfill boot verify-pass stamps all pre-existing complete sets in one cheap local readdir so existing coverage re-advertises seconds after deploy; heavy-op timeout scales `max(900s, content_duration/2)`. Guards: truncated_sheet_set_is_regenerated_not_trusted (+VFR tolerance), cached_only_fetch_never_generates reworked for marker semantics.
B38|2026-07-14|Intro-skip seek froze a live 3-member party ~46s then resumed from the PRE-seek position (re-watching 24s) — total ~70s (Loki-reconstructed, 2026-07-13 20:48:24Z: seek → 46s zero segment requests → segs 11-15 fetched sequentially at exactly 6s realtime cadence from 66s; seg 15, the 90s seek target, was never client-requested — only pharos prefetch built it). Three-factor chain: (1) `lead_time_ms = MIN_LEAD + max_rtt/2` UNCLAMPED — one member's pathological clock sample (throttled tab measuring its own ~80s stall as RTT) scheduled every command ~40s into the future for the WHOLE group; (2) spurious same-item player-transition Readys (B37's PlaylistItemId check passes — same episode) resolved the seek gate ~1s after broadcast, long before any client ran its scheduled seek at `When`; (3) the resulting early Play made jellyfin-web's `scheduleUnpause` cancel the still-pending seek callback (`clearScheduledCommand`), and `scheduleUnpause` only corrects players AHEAD of target — so at `When` (+40s) everyone unpaused in place at the old position and just-in-time-fetched forward.|V28. clock.rs discards samples with RTT > 10s (`MAX_SAMPLE_RTT_MS`); `lead_time_ms` clamps the RTT half to 2s (`MAX_HALF_RTT_LEAD_MS`) — lead ∈ [200ms, 2.2s]; `WaitingGate.not_before_server_ms` = the gated command's `at_server_ms` (Seek; 0 for queue-change gates) and `MemberReady` ignores gate ACKs arriving before it. Gate-open log now carries `lead_ms` + `not_before_server_ms`. Guards: pathological_rtt_sample_is_discarded (clock.rs), seek_lead_is_clamped_against_pathological_rtt, ready_before_seek_schedule_time_does_not_resolve_gate (group.rs).
B37|2026-07-13|Next-episode skip desynced a live 3-member party (Code Geass e20→e21): NextItem opened a readiness gate on all members, but MemberReady/BufferingStart DROPPED the PlaylistItemId jellyfin-web sends on every post — and jellyfin-web posts Ready on EVERY player transition, including the OLD episode's teardown right after the queue change. Those stale Readys (arriving 1-3s after NextItem, before any client could have loaded e21) satisfied the new item's gate → Play(0) broadcast while players were still loading → real Buffering posts later froze the group mid-chaos → unpause-mashing, socket drops, manual group re-creation (live logs 21:10:39-21:13Z). Compounding: /SyncPlay/NextItem ignored its body PlaylistItemId (real Jellyfin no-ops a mismatch — double-press/two-member race = skipped episode), and the wire UserLeft carried member_id.to_string() (raw uuid rendered in the "left the group" toast — the hint that exposed the family). Group actor had ZERO gate/queue/freeze logging, so the incident was reconstructed from HTTP dispatch lines alone.|V27. GroupMsg/RemoteCommand Ready/Buffering/NextItem/PreviousItem carry playlist_item_id (serde-default → bus rolling-deploy safe); pli_is_stale() validates against the current entry; stale Ready/Buffering → ignored + single-member PlayQueue catch-up reusing the queue's LastUpdate (up-to-date clients drop it, behind clients load the right item and re-report); stale Next/Previous → no-op; ServerMsg::MemberLeft carries the display name (uuid only as empty-name fallback). Actor now logs gate open/resolve (INFO), anti-wedge timeout with the pending member list (WARN), queue advances, buffering freeze, member removal. Guards: stale_ready_does_not_resolve_queue_change_gate, stale_ready_catch_up_reuses_queue_timestamp, stale_buffering_does_not_freeze_group, next_item_for_stale_entry_is_ignored, member_left_carries_display_name (group.rs); translate_member_left_carries_username_not_uuid (socket.rs).
B36|2026-07-13|Watched ticks / favourite stars / resume bars never updated in-page — only on manual refresh. The `UserDataChanged` socket frame pharos sent was `UserDataList:[{ItemId:"<decimal>"}]`: jellyfin-web patches cards via `querySelectorAll('[data-id="'+userData.ItemId+'"]')` (data-id is the 32-hex wire id — decimal matches NOTHING) and the detail page filters entries by `Key` (absent) — and even on a match there were no `Played`/`IsFavorite`/`PlayedPercentage` fields to apply. Bonus finds: `PlayedPercentage` was hardcoded 0.0 in every UserItemDataDto (card resume bars permanently blank), and mark-played/favourite on a synthetic series/season id 400ed "invalid item id" (parse_item_id correctly rejects synth ids as media ids; mutate had no folder path — jellyfin-web's series/season mark-watched buttons dead).|SocketBroadcast::UserDataChanged now carries `entries: Vec<serde_json::Value>` — full serialized UserItemDataDtos; all 4 emit sites (PlayedItems/FavoriteItems mutate, /Sessions/Playing/Progress, /Sessions/Playing/Stopped) build the real DTO via new from_domain_with_runtime (PlayedPercentage = position/runtime, clamped); synth series/season ids cascade the mutation over child episodes and batch every changed entry + a folder entry (ItemId/Key = synth id) into ONE frame, returning the folder DTO like real Jellyfin. Guards: translate_userdata_changed_carries_full_dto_entries (wire), mark_played/unmark/favorite broadcast tests + season-cascade + series-favorite-cascade + unknown-synth-404 (jellyfin_user_data), stop_near_end_broadcasts_full_played_dto + progress_broadcasts_dto_with_played_percentage (playback_stopped_audit).|V26
B35|2026-07-13|BaseItemDto.Trickplay advertised for EVERY video regardless of tiles on disk (with_trickplay computed purely from probe): clients render the scrub-preview UI, request tiles, 404, show an empty box — indistinguishable from working previews minus images. Made the zero-coverage outage (B34) invisible from the client side.|TrickplayCache keeps an in-memory generated-set (primed by a one-shot root walk at construction, updated on generation + eviction, self-healing via is_generated's disk fallback); AppState::generated_trickplay_widths filters the configured widths per item and every with_trickplay site now advertises ONLY widths with real tiles. Plus rate/visibility: backfill COOLDOWN 3s→1s; libav pool warns when an op waits >10s for a worker slot (the B34 starvation was an untimed silent acquire). Guard: http_get_item_emits_nested_trickplay reworked — generated width advertised, ungenerated width omitted, tile-less item omits Trickplay entirely.|V1
B34|2026-07-13|Trickplay coverage 119/12100 (~1%) after days; played items never got previews. Two library-wide walkers spawn at boot — the standalone subtitle warm-all AND the trickplay sweep (which itself warms each item's subs+fonts after its sprites) — and the warm-all's cold whole-file demuxes (minutes each) monopolized BOTH the shared bg_io gate and all 4 libav pool workers (pool permits.acquire() waits untimed), so trickplay generation — sweep AND the gate-bypassing priority path — starved silently forever. Found via the new backfill lifecycle logs (pass started, 0 generations in 30min, no errors) + live probes (fresh image ops fast ⇒ pool alive; nudged seeds never materialized).|Drop the redundant boot warm-all; the sweep is the single ordered walker (tiles → subs → fonts, newest-first). Playback-priority single-item warms (PlaybackInfo, ungated) unchanged. Backfill lifecycle logging (spawn/pass/coverage/nudge at INFO) keeps this failure class visible.|V5
B33|2026-07-12|Scenario-matrix run exposed it: a group left UNOWNED after a rolling deploy. Boot reconciliation fires 10s after the new pod starts — while the OLD pod is still draining (30s grace) and holding each group's pg advisory lock — so get_or_create loses the election and caches a REMOTE handle (bus-forwarder to a dying owner). Nothing re-attempts ownership without client activity: the group ran with NO actor — no ghost prune, no dissolution, snapshot squatting in the picker (live: 3-member group immortal 18+ min post-boot; local real-time probe of the same prune path passed, isolating the divergence to ownership). Timing-dependent — earlier boots won the race and pruned fine.|Boot reconciliation retries in rounds (10s, then 20s × 9 ≈ 3.5 min, far past any drain window) until every fresh group answers a snapshot() probe — ownership is detectable from outside the registry because only a LOCAL actor answers (remote handles drop the reply). Registry-level guard: get_or_create_upgrades_remote_handle_to_owned_after_lock_frees (fake two-replica ownership map: locked → remote/None, freed → owned+hydrated roster). Plus realtime_hydrated_ghosts_prune (#[ignore] probe) documenting the real-time prune path works.|V25
B32|2026-07-12|Music playback dead in jellyfin-web (playwright repro: play press → NO PlaybackInfo, no audio element). Track detail page fetches the track's AlbumId — a synth MusicAlbum wire id — via /Users/{uid}/Items/{id}; fetch_item_dto resolved library/series/season/collection/playlist synth ids but NOT album/artist → 400 "invalid id" → the itemDetails view aborted before the play button was wired.|synth_album_or_artist in fetch_item_dto: recover the name by hashing distinct_album_names / distinct_artist_names (same one-way-hash pattern as resolve_parent_filter, so ParentId-resolvable ⇔ item-resolvable) and emit the list-builder DTO shape (+ChildCount for albums). Guard: synth_album_and_artist_ids_resolve_as_single_items (both /Items/{id} and /Users/{uid}/Items/{id}).|V1
B31|2026-07-12|The 0-participant group B30 targeted was RESURRECTED, not just leaked: StorePersistence spawned persist and remove as independent tasks, so the actor's terminal sequence — final-mutation persist (0-member roster) then remove-on-empty — could apply in reverse order in the store, the late upsert re-creating the row after its delete. Live trace: picker click → join hydrates → member joins/leaves → race resurrects a 0-member row that survives boot reconciliation cycles (each hydrate-dissolve-remove races a fresh persist).|Per-group op chain in StorePersistence: every persist/remove awaits the group's previous op before running — store-visible order now matches call order; fire-and-forget from the actor's view is preserved. Guard: remove_after_persist_never_resurrects_the_row (200 iterations; runs under --features postgres). Straggler row cleared by the next boot reconciliation.|V25
B30|2026-07-12|B29 verified live (8 stale groups → 2 within 5min of deploy) but exposed one more leak: a ZERO-participant group that never dissolves. /SyncPlay/New with no registered socket spawns the actor and the creator's AddMember never arrives — ever_joined stays false, so the had-members-then-empty terminate rule never arms → immortal empty group squatting in the picker.|Join deadline: an actor nobody has EVER joined dissolves 120s after spawn (checked on the prune tick; persisted snapshot removed too). Guards: never_joined_group_dissolves_at_join_deadline + joined_group_survives_past_join_deadline (paused clock).|V25
B29|2026-07-12|Join picker full of STALE groups (8 listed, mostly dupes/empty rosters). Orphaned snapshots have NO actor: after a restart a snapshot only spawns a group actor when someone touches it (B24 recovery / join) — if nobody returns (closed browsers, repro runs), the T83 ghost prune never runs against it and the snapshot haunts the picker (B28 lists it) until the 48h janitor. Bonus: an actor hydrated from a crash-orphaned EMPTY snapshot set ever_joined=false and idled forever without deleting its row.|Boot reconciliation (sync_recovery::spawn_boot_reconciliation): ~10s after startup, get_or_create every fresh snapshot → hydrated actors hand orphans to the existing lifecycle (members get MEMBER_TTL_MS to reconnect, no-shows pruned, emptied actor deletes the snapshot) — self-cleaning in ~3min. Hydration now always sets ever_joined=true (a group only ever persists after its creator joined) so a hydrated-empty actor dissolves on its first prune tick. Guard: hydrated_group_with_no_show_members_dissolves_and_removes_snapshot (paused clock, real snapshot roundtrip, asserts actor termination + snapshot REMOVAL).|V25
B28|2026-07-12|Audit: /SyncPlay/List read only the in-memory registry — after a restart (or on a replica not owning the group) the join picker was EMPTY even though the party existed as a fresh sync_groups snapshot. Anyone not auto-recovered by B24 (new joiner, member who left) couldn't rejoin; the group had to be recreated.|list_groups merges fresh persisted snapshots (≤ RECOVERY_WINDOW, dedup vs in-memory handles) into the listing via pharos_sync::group::snapshot_summary; Join already goes through get_or_create which hydrates from the same snapshot. Guard: list_includes_fresh_persisted_groups_after_restart (fresh listed, stale hidden, participants from roster).|V25
B27|2026-07-12|Audit: a mid-play buffering member froze the group FOREVER — BufferingStart broadcasts the corrective Pause (V19 buffer isolation, correct) but recovery had no path back: the HTTP wire pairs Buffering→Ready, and MemberReady only cleared the member's flag; group_paused_due_to_buffering stayed set and nobody restarted playback. Real Jellyfin auto-resumes (WaitingGroupState issues an internal Unpause once IsBuffering() clears). Every network hiccup = party stops until a human presses play.|MemberReady (and ws-native BufferingEnd): when the freeze was buffering-caused and the last buffering member recovers (no gate open), start_playing at the frozen position ("Ready"). Voluntary pauses untouched (flag not set). Guards: buffering_freeze_auto_resumes_on_last_ready + voluntary_pause_is_not_auto_resumed_by_ready.|V19
B26|2026-07-12|B24's restart recovery never fired in the live pod-restart repro (0 "recovered persisted membership" logs; fallback NotInGroup fired instead). The GRACEFUL DRAIN destroyed what recovery needed: SIGTERM closes every /socket → each disconnect schedules the 20s reconnect-grace teardown → the still-draining old process (30s grace) executes them → RemoveMember for every member → group empties → the actor deletes its own snapshot (remove-on-empty) → the new pod finds nothing. Shutdown was indistinguishable from "every client left at once".|Process-wide SHUTTING_DOWN flag (state::begin_shutdown, set by a SIGTERM/SIGINT listener installed before actix's own handler): the socket disconnect teardown (both the ws-native immediate removal and the grace-delayed one, re-checked at fire time) becomes a no-op while draining, leaving memberships + snapshots intact for the next replica. Extends V25. Live-verified via pod logs across two prod restarts: both harness clients AND a real jellyfin-web session hit "recovered persisted membership after restart" → "reconnected into existing group" → client dispatched ready (player re-followed group playback).|V25
B25|2026-07-12|lace WEDGED in SyncPlay: stuck on a paused frame, couldn't play, couldn't leave, couldn't reach the library. Client was in group mode while the server had no group for her session (the B24 post-restart state) — and `/SyncPlay/Leave` never told the LEAVER anything: it sent RemoveMember to the group (whose MemberLeft broadcast fires after roster removal, reaching only the REMAINING members) and answered a bare 204. jellyfin-web exits SyncPlay mode only on a GroupLeft/NotInGroup socket message, so Leave was a silent no-op and the only escape was a page reload.|leave_group always sends ServerMsg::GroupLeft to the caller's sink — group attached or not (→ SyncPlayGroupUpdate/GroupLeft; jellyfin-web disableSyncPlay). Extends V25's "never silently drop". Guards: syncplay_restart_recovery.rs leave-with-group + leave-without-group both assert GroupLeft reaches the leaver.|V25
B24|2026-07-12|Mid-episode group watch desynced on Pause (alison paused, lace kept playing; group had to be recreated). Tracing: pod restarted 16:00:59 (season-poster deploy); alison's socket reconnected 16:03; her Ready/Pause/Unpause all hit "syncplay: session is in no group — command dropped". Group SNAPSHOTS survive deploys (B4.3c persistence + hydration), but (a) the SessionHub's deviceId→group map is in-memory and (b) MemberId was minted randomly per process — so after restart the persisted roster named ids no reconnecting device could ever match: membership unrecoverable by design, and the drop silent (server WARN only; jellyfin-web pauses locally BEFORE POSTing, so the presser one-sidedly paused).|Three-part: MemberId derives deterministically from deviceId (UUIDv5, hub::member_id_for_device) so persisted rosters stay valid across restarts; /socket connect with no hub group recovers membership from sync_groups snapshots (sync_recovery::find_persisted_group → get_or_create hydrates → ResyncMember re-syncs transparently); a command from a session with no recoverable group answers ServerMsg::NotInGroup (→ SyncPlayGroupUpdate/NotInGroup; jellyfin-web disables SyncPlay visibly). New invariant V25. Guards: hub member-id stability, snapshot_contains_member vs real persisted state, syncplay_restart_recovery.rs (recovery + NotInGroup).|V25
B23|2026-07-12|Every season of a show served the IDENTICAL series poster (Buffy report; live-verified: all 7 season Primary responses byte-equal to the series poster). A synth Season id resolves to a representative episode and serves that episode's artwork rows — which record the series-root poster.jpg (the sidecar provider probes the show folder for episodes). Nothing ever looked at the per-season art that IS on disk: the Kodi/Jellyfin series-root convention (`season01-poster.jpg` … `season07-poster.jpg`, confirmed via /Environment/DirectoryContents).|`season_sidecar_path` in images.rs: for a synth id matching the representative episode's season id, probe series-root `season{NN}-{poster,fanart,banner}` (zero-padded + bare, `season-specials-*` for S0) before the episode-artwork fallback; series-poster fallback stays for art-less seasons (Jellyfin parity). Sidecar provider also gains `landscape` as a Thumb base. New invariant V24. Guard: jellyfin_season_images.rs (season-specific + specials alias + fallback).|V24
B1|2026-05-26|parse_device_id matched `Device=` before `DeviceId=` because the loop returned on first hit. Test caught via auth header containing both keys.|prefer DeviceId; fall back to Device only if absent. Caught by V11 test-first practice — no new invariant.
B2|2026-07-09|Progressive A/V drift on continuous VP9 fMP4 HLS playback (~ms/segment, seek resyncs): per-segment encodes were zero-based and fmp4.rs re-anchored tfdt onto an exact seg·6.0s grid, but real content is ~6.012s video vs ~6.006s audio (frame-grid overhang vs opus preskip) — grid-forcing re-timed the tracks against each other every boundary and it accumulated over an episode.|Source-anchor each independent segment at the encoder (`-output_ts_offset` + `-avoid_negative_ts disabled` + `-enc_time_base 1:90000` + `+frag_discont`); fmp4.rs stops grid-rewriting (only clamps negative tfdt); VP9 cache codec tag bumped to orphan stale zero-based segments. Regression guard: tests/vp9_fmp4_hls.rs asserts per-track cross-segment tiling on the source timeline (V13-style wire-shape proof through real ffmpeg). Parallel per-segment encode preserved.
B3|2026-07-09|Seeking mid-playback buffered far longer than one segment-encode: hls.js aborts its prefetched old-position segment fetches on a seek; actix dropped those handler futures + their scheduler reply oneshots, but the scheduler kept the jobs queued and still ran each (~realtime VP9 encode) when a device permit freed, so the seek-target segment sat behind a pile of dead work.|Scheduler skips any queued job whose reply oneshot `is_closed()` (receiver dropped ⇒ caller gone) at both dispatch points (`place` + `drain_pending`→`try_place_no_queue`). Live-stream path already cancels via its RAII permit on stream drop. Test: 5 blockers + 10 abandoned + 1 seek-target → only 6 run (was 16).
B4|2026-07-09|Audio-track switching did nothing on the VP9/Firefox HLS path. Stock jellyfin-web 10.11.11 switches a TRANSCODED stream's audio by full-reload — re-POSTing /Items/{id}/PlaybackInfo with the new AudioStreamIndex in the `playbackInfoDto` BODY — but pharos built the transcode URL's stream-selection suffix only from the request QUERY string, so the body index was dropped and every switch reloaded the DEFAULT track.|Parse AudioStreamIndex+SubtitleStreamIndex from PlaybackInfoBody, fall back to them when absent from the query (query still wins). Guarded by tests/jellyfin_playbackinfo_stream_selection.rs (body index reaches TranscodingUrl) + tests/vp9_audio_switch.rs (real decode proves the segment carries the selected track).
B5|2026-07-09|ASS/SSA subtitles rendered late on Firefox ("Fetching assets" then appeared mid-episode). Client libass (SubtitlesOctopus) fetches EVERY embedded font before drawing a cue, but `deliver_attachment` extracted fonts one-per-request and each `extract_attachment` re-opened the whole multi-GB NFS source to copy one attachment — a title with N fonts paid N cold opens on first play; subs only appeared once all cached (replay instant).|One-open batch: new TinyOp::ExtractAllAttachments (libav::attachment::extract_all_attachments) dumps every attachment in a single source open; `ImageCache::ensure_all_attachments` warms them all; `deliver_attachment` warms-all on the first font request then serves it; the background pre-generator also pre-extracts fonts (with the ImageCache now threaded in). Guard: tests/attachment_fonts.rs — one request warms all 3 fonts (was 1).
B6|2026-07-09|Switching audio track on a multi-audio title 500'd every segment ("[vf#0:1] Error sending frames to consumers: Invalid argument", -22; "at least one stream received no packets") → playback broke on switch. Default (no audio index) worked; any explicit AudioStreamIndex failed. The explicit-audio branch mapped `-map 0:v?` (ALL video), so a file with an embedded cover-art / attached-picture 2nd video stream (anime) had the poster mapped too and the encoder choked on it. Latent until B4 made the index actually reach the segment.|Map only the primary video: `-map 0:v:0?` (matches ffmpeg default selection + the probe's first-video choice; still optional for audio-only). Shared arg builder → fixes both VP9/Firefox and H.264 paths. Guard: audio_stream_index_emits_explicit_map asserts 0:v:0? and not 0:v?.
B7|2026-07-09|Subtitles "take too long to download": tracing (added #[instrument] on deliver_* + elapsed_ms on the ffmpeg extracts) showed the first Stream.js/vtt/ass fetch of a not-yet-warmed track = ~30s (deliver_js MISS → webvtt extracted elapsed_ms=29588). Root cause: subtitle extraction demuxes the WHOLE multi-GB NFS source (sub packets span the container; conversion itself is <0.02s), and the SubtitleCache was memory-only so every pod restart re-paid it. Distinct from B5 (ASS fonts).|Disk-backed subtitle cache (`SubtitleCache::with_disk`, persisted under the cache PVC — sibling of transcode/image dirs) so extraction is once-ever + survives restarts, + scan-time pre-extraction of added/updated items' text subs (bounded concurrency, NFS-aware) in library_watch + a playback-gated startup warm-all over the existing library so playback is warm before the first fetch. Guards: subtitle_cache disk_layer_survives_a_restart + library_watch scan_time_warm_populates_subtitle_cache.
B8|2026-07-09|Mid-playback AUDIO-track switching did nothing (only a full stop+resume applied the new track) — yet the SAME stock jellyfin-web works on vanilla Jellyfin. Live traces showed the switch made ZERO server requests. jellyfin-web's changeStream FIRST calls stopActiveEncodings = `DELETE /Videos/ActiveEncodings?playSessionId=…` (no item id in the path, so it was invisible to episode-filtered greps; an unmatched actix route 404s without a handler/log); pharos never registered it → 404, and jellyfin-web's switch promise chain is unguarded (no .catch), so the rejection killed the chain before the new PlaybackInfo fired. Vanilla answers 204.|Register `DELETE /Videos/ActiveEncodings` → drop the play session (per-segment transcode, nothing long-lived to kill), return 204. Also unblocks mid-playback subtitle/quality switches (same changeStream path). Guards: stop_active_encodings_returns_204 + _requires_auth. Bonus: StatusRootSpanBuilder now WARN-logs any ≥400 response so a missing-endpoint 404 is greppable (was invisible).
B22|2026-07-12|SyncPlay STILL dead after B9 (server protocol correct): two-Playwright-session repro showed group create/join + SetNewQueue + PlayQueue broadcast + Waiting all working, then BOTH clients crashing in updatePlayQueue — "TypeError: Cannot read properties of undefined (reading 'Type')" — so startPlayback never ran, no player, no Ready, gate dead. Root cause: jellyfin-web's QueueCore refetches the queue's items via /Items?ids=<71 hex ids>; the /Items list filter had a SECOND leftover decimal-era `.filter(s.len() <= 20)` guard (sibling of the parse_id_csv one B15 fixed) that dropped every canonical 32-hex id → zero rows → translateItemsForPlayback read items[0].Type of an empty array. Also broke every other /Items?ids= consumer.|Guard widened to ≤36 (dashed-GUID max) ahead of parse_item_id. Guard test items_ids_filter_accepts_hex_and_decimal (hex + decimal both return the rows). Lesson reinforced from B16: sweep VALUE-shape guards (length/charset filters), not just parse sites, when changing a wire format — `rg 'len\(\) <= 20'` found exactly one more.
B21|2026-07-12|Playwright UI sweep follow-ups: (a) favorites-tab / cast person images 404'd — /Items/{personWireId}/Images/Primary had no handler branch for people even though the scanner stores a scraped thumb_url for 853/1320 people; (b) music grid 404s investigated to ground truth: NOT a bug — ffprobe of the flagged files shows no embedded art stream and no folder cover; jellyfin-web renders its placeholder tile (866 audio items with folder art serve fine). |(a) person Primary now 302s to people.thumb_url before the image-cache guard (Live-TV channel-logo pattern; person wire ids never collide with item ids since parse_item_id rejects non-zero-high-half hex). Guard: person_primary_image_redirects_to_scraped_thumb. (b) recorded as cosmetic-polish backlog T78 (art-aware audio ImageTags) + T79 (cast-list PrimaryImageTag). Backlog also gains T80 (postgres test harness, the B19 class guard).
B20|2026-07-12|Search 400'd after B18's CiQuery landed: jellyfin-web sends REPEATED query params for list fields (`fields=PrimaryImageAspectRatio&fields=CanDelete`); the old PascalCase structs never bound the lowercase `fields` key so the repeats were ignored, but CiQuery normalized both onto the same scalar field → serde duplicate-key → 400 on /Items search + /Artists. Caught by the Playwright UI sweep, same session.|CiQuery merges repeated normalized keys into one comma-joined value (mirrors ASP.NET array binding; jellyfin list params are comma-CSV). Guards: ci_query unit (fields merge) + jellyfin_items repeated_query_params_merge_instead_of_400. Sweep also recorded two PRE-EXISTING gaps (not regressions): favorites-tab person images 404 (/Items/{personWireId}/Images/Primary unimplemented for people) and music album-art 404s (embedded audio art not served) — both open.
B19|2026-07-12|/Items?ParentId=<season synth id> 500'd on Postgres: "decoding column 2: i64 not compatible with INT4". distinct_season_keys decoded season_number (INTEGER/INT4 in the pg schema) as i64 — resolve_parent_filter probes season keys for any non-library ParentId, so season browsing via the /Items pivot died. Postgres-only (sqlite is dynamically typed), so the sqlite-backed test suite can never catch this class; schema-aware sweep of every query_as tuple in postgres.rs found exactly this one mismatch (entity ids already i32; playlists BIGSERIAL; COUNT/item_id BIGINT).|Decode i32 + widen to i64. Class guard remains OPEN: the store trait suite still doesn't run against real Postgres (planned Phase A.4 pg test service, never landed) — until it does, any pg-only type/SQL divergence ships silently. That work item is the true fix; tracked in the zero-downtime plan.
B18|2026-07-12|Seasons unusable: clicking any season showed the WHOLE series' episodes (Arrow S1 → all 129 across 6 seasons), duplicated + interleaved with other shows. Playwright-drove the live jellyfin-web + curl proof: /Shows/{series}/Episodes?SeasonId= (PascalCase) → correct 23; ?seasonId= (camelCase) → 129 (filter ignored). jellyfin-sdk-kotlin/js (jellyfin-web 10.11 + all apps) sends camelCase query params (tv-shows-api.js: localVarQueryParameter['seasonId']); pharos's ShowsEpisodesQuery (and ~28 other web::Query structs) bound PascalCase-only via serde, so the SDK's camelCase key silently deserialized to None → no filter. Real Jellyfin (ASP.NET) binds query params case-INSENSITIVELY; this is the systemic Class-1 casing bug (QuickConnect B11 was one instance). NOT caught by tests: every query test sent PascalCase, matching the old rename.|New CiQuery extractor (api/jellyfin/ci_query.rs): snake-cases every incoming query KEY (SeasonId/seasonId → season_id; Is4K/is4K → is_4k via a digit-boundary rule) then defers to actix's parser, so plain snake_case Rust fields bind ANY casing — mirrors ASP.NET. Swept all 29 jellyfin web::Query<T> usages → CiQuery<T>, structs → rename_all snake_case, dropped the ListQuery Is4K/Is3D field renames. Guards: ci_query snake unit tests; jellyfin_items shows_episodes_season_filter_is_case_insensitive (camelCase AND PascalCase both filter to the one season). Lesson: match the real API's case-insensitivity, and test query endpoints with the SDK's camelCase, not just jellyfin-web's PascalCase.
B17|2026-07-12|Home screen fully broken ("Next Up and everything") after B14: console showed home load STOPPING at a 404 on GET /LiveTv/Programs/Recommended — no NextUp/Resume/Latest requests ever fired after it. Chain (jellyfin-web source): B14 added `EnableLiveTvAccess: true` to UserPolicy (the C# always-field); loadLiveTV gates on exactly that flag (previously ABSENT → falsy → section skipped, which is why this never fired before); its getLiveTvRecommendedPrograms call has NO .catch; the 404 rejection propagates into loadSections' Promise.all → the ENTIRE home render dies. Every home endpoint (NextUp/Resume/Latest/UserViews) verified fine server-side — the kill was one missing route + one advertised-but-unanswerable feature flag. Same class as B8 (unguarded jellyfin-web promise chains die on missing endpoints).|Register GET /livetv/programs/recommended → empty ItemsResult (LiveTv is a REAL pharos feature — tuner/channels/EPG — so the policy flag stays true; the empty result makes the "On Now" section hide itself). Guard: jellyfin_live_tv.rs recommended_programs_is_registered_because_home_dies_on_its_404. Lesson: adding an always-on-wire POLICY flag can newly ENABLE client codepaths — check what jellyfin-web gates on each flag before advertising it.
B16|2026-07-12|B15's hex-id rollout broke ALL video playback: the sweep's regex matched `let id: u64 = <var>` but the five HLS playback handlers bind `id_num`/`media_id` with `.parse()` on the following line — missed. PlaybackInfo emitted hex-id TranscodingUrls; the playlist/init/segment routes 400'd "invalid id" on them → jellyfin-web hls.js dead on every title. Every test stayed green: HLS tests call routes with DECIMAL ids directly (legacy form still parses) and nothing followed a server-emitted URL back into the server. Also caught in the same sweep-hole: sessions.rs progress/stopped handlers parsed ItemId decimal-only → resume positions silently stopped persisting for hex-era clients; items.rs parse_media_id (tag mutations) + one admin item op.|All 8 stragglers swapped to parse_item_id. Regression guard tests/jellyfin_wire_id_routes.rs: (a) table-driven ROUTE MATRIX — 17 id-bearing route families each requested with canonical-hex, legacy-decimal AND dashed-GUID forms, asserting no 400/"invalid" (new id-bearing routes add one line); (b) URL-FOLLOW test — PlaybackInfo's emitted TranscodingUrl must embed the canonical hex id and be accepted by the route it points at (the test that would have caught this). Lesson: a wire-format change needs an emit→parse round-trip test through the server's OWN emitted URLs, not just direct route calls.
B15|2026-07-12|Native Android app (ExoPlayer) STILL failed everything with "Unable to resolve playback info" after B13/B14 (response shapes proven kotlin-clean). Traced jellyfin-android source: the toast = player_error_invalid_play_options, fired when the WebView→native bridge's PlayOptions has no playable ids — PlayOptions.fromJson parses every id with toUUIDOrNull() and SILENTLY DROPS non-UUID ids. pharos item ids were u64 decimal strings ("152979617944103156"), not GUIDs → every id dropped → empty native play queue, before any playback request reached the server. Jellyfin item ids ARE Guids; jellyfin-web tolerates any string, the native bridge doesn't. Bonus find during sweep: parse_id_csv's len≤20 guard (sized for decimal u64) would have silently dropped the new 32-hex ids from playlist/collection mutations.|Canonical wire id = u64 zero-padded to dashless 32-hex GUID (wire_item_id; matches the 32-hex shape series_id_for_key already used). parse_item_id accepts 32-hex (leading-zero namespace), dashed-GUID, and legacy decimal (open sessions). Swept: all BaseItemDto/MediaSourceLite/UserData/Trickplay-key/search-hint/DeliveryUrl emissions + every route/query id parse (~20 sites) + Ids= CSV filters (len guard ≤36). Goldens regenerated. Guards: wire_item_ids_are_guid_shaped_and_round_trip (round-trip + dashed + legacy + series-namespace rejection) + kotlin-strict test asserts MediaSource.Id is a dashless GUID.
B14|2026-07-12|Full C#-canonical DTO audit (jellyfin source at ~/git/personal/jellyfin): real Jellyfin serializes with ignore-null, so every NON-NULLABLE value-type property of a C# DTO is ALWAYS on the wire — the exact set strict SDK clients require. pharos gaps found: UserPolicy missing 13/30 always-fields; SessionInfoDto (auth result) missing 6/7; GET /Sessions rows missing 3; QuickConnect Connect poll missing DateAdded (Android TV poll); UserItemDataDto missing ItemId; ChapterInfo missing ImageDateModified; item-embedded MediaSourceLiteDto missing 11 (same class as B13's PlaybackInfo shape); SyncPlay SendCommand missing GroupId entirely + When/EmittedAt/PlaylistItemId conditionally skipped (native-app SyncPlay dead); UserDto missing HasConfiguredEasyPassword. SECURITY: /System/Info/Public aliased the full authenticated SystemInfo handler (unauthenticated exposure of the whole shape — path values were hardcoded placeholders, no real disclosure) and /System/Info itself was unauthenticated.|All always-fields added with real-Jellyfin default values; SendCommand fields unconditional with Guid.Empty fallback; /System/Info/Public now serves only Jellyfin's 7-field PublicSystemInfo subset; /System/Info requires auth (route_auth_audit expectation flipped). Goldens regenerated (additive). REMAINING (next): pharos item ids are u64 decimal strings, NOT GUID-shaped — jellyfin-android's WebView→native bridge parses ids with toUUIDOrNull() and silently DROPS them all → empty play queue → "Unable to resolve playback info" on the native player regardless of response shapes. Fix = GUID-shaped wire ids (u64 zero-padded to 32-hex) + dual-format parsing.
B13|2026-07-12|Native Android app (ExoPlayer path) failed EVERYTHING with "Unable to resolve playback info" — zero server-side errors logged (responses were 200s). Root cause: jellyfin-sdk-kotlin deserializes with kotlinx.serialization which THROWS on any missing non-defaulted field; pharos's PlaybackInfo omitted 6 kotlin-REQUIRED MediaSourceInfo fields (ReadAtNativeFramerate, IgnoreDts, IgnoreIndex, GenPtsInput, IsInfiniteStream, HasSegments), emitted null for the non-nullable TranscodingSubProtocol enum on direct-play, and skip-serialized 5 kotlin-REQUIRED MediaStream booleans on Video/Audio rows (IsForced, IsHearingImpaired, IsExternal, IsTextSubtitleStream, SupportsExternalStream) + never emitted IsInterlaced/IsOriginal at all. jellyfin-web (JS) is lenient so browser testing never sees this — same failure family as the QuickConnect DateAdded/DeviceName precedent. AUDIT of the class also found ~15 PascalCase-only web::Query structs (ListQuery/ShowsEpisodesQuery/NextUpQuery/SegmentQuery/...) where the SDKs' camelCase params are silently IGNORED (default fields) or 400 (required fields) — native-app browse filters/paging silently wrong.|Emit the full kotlin-required field set: MediaSourceInfo gains the 6 fields + TranscodingSubProtocol always "http"/"hls" (never null) in both primary + alternate sources; MediaStreamDto's Is* flags become always-serialized bools + new is_interlaced/is_original. Guard: jellyfin_playbackinfo_kotlin_strict.rs asserts every kotlin-required field present + non-null on MediaSources/MediaStreams/MediaAttachments (field lists copied from the generated kotlin models). Query-casing sweep tracked as follow-up (systemic case-insensitive param binding).
B12|2026-07-12|Phone (WebView player, h264 hls1 path) couldn't play most titles; one played then hung. Tracing: every hls segment `device=cpu`, `encode_ms` avg 3405 max 12400 for 6 s segments — BELOW realtime → player drains buffer → ABR thrashes through all variants of the same segment (each a fresh cold encode) → stall. Design intent was many parallel small encodes on a many-core box, but three settings self-defeated it: (a) sw x264/x265 got NO `-preset` → x264 default `medium` (quality-first, ~3-5× slower than Jellyfin's `veryfast`); (b) NO `-threads` cap on x264/x265 encode OR decode (only VP9 was capped) → each job auto-threaded across all 16 cores; (c) `default_cpu_permits()` = cores (16) → 16 admitted all-core jobs thrashed each other. Measured single encode: 940% CPU (old) — 4 concurrent demanded ~38 cores on a 16-core box. Library dominated by 10-bit HEVC + E-AC3 (heavy decode) amplified it. Also: node is a 16-vCPU VM of the EPYC — the "lots of cores" premise never reached the pod (infra, separate).|V23. `-preset veryfast` + `-threads sw_encode_threads()` on libx264/libx265 (scoped: NVENC/VAAPI presets differ, `veryfast` aborts NVENC); input-side decoder `-threads` cap for any sw video transcode; `default_cpu_permits()` = cores/sw_encode_threads() (16→4); prefetch depth from permits, clamped ≤4 (big box would flood queue + starve second viewer's live segment). Bench: per-segment CPU 940%→372% at same speed; 4 concurrent 6 s segments in 1.64 s wall (14.6× realtime aggregate, fits 16 cores). Guards: software_h264_gets_speed_preset_and_bounded_threads, nvenc_h264_keeps_hw_preset_vocabulary, video_copy_skips_thread_caps, cpu_permits_match_thread_budget_not_core_count.
B11|2026-07-12|Quick Connect timed out on the login device before the code could be entered. Tracing showed BOTH clients 400ing every request: Jellyfin Android TV polled `GET /quickconnect/connect?secret=…` (lowercase) → 400 "Secret query param required"; the signed-in Android browser POSTed `/quickconnect/authorize?code=…&userId=…` (lowercase) → 400 "Code query param required" (retried 9×). pharos bound these query params case-SENSITIVELY as PascalCase (`#[serde(rename_all="PascalCase")]` on `web::Query` structs), but the real Jellyfin API params are camelCase and ASP.NET binds case-INSENSITIVELY — so jellyfin-web's `?Secret=`/`?Code=` works while every mobile/TV SDK (lowercase) 400s. Web-only testing (and an earlier capital-`Secret` curl) hid it.|V22 extended. QuickConnect Connect/Authorize now read `secret`/`code` via a case-insensitive `query_param_ci` helper (actix's own query parser + eq_ignore_ascii_case); finalize body `QuickConnectDto` gained `#[serde(alias="secret")]`. Guard: jellyfin_quick_connect.rs flow_works_with_lowercase_query_params_like_android_clients. NOTE (known, not yet fixed): ~11 other `web::Query` structs across the Jellyfin surface share the same PascalCase case-sensitivity — latent for any non-web client that sends camelCase; left as-is in this focused fix to avoid regressing the working web path.
B10|2026-07-11|Quick Connect login never completed: code showed, user authorized on a signed-in device, login device stayed on the dialog then errored. Traced jellyfin-web login (controllers/session/login/index.js + bundled jellyfin-apiclient): after polling `GET /QuickConnect/Connect?Secret=` until `Authenticated:true`, it calls `apiClient.quickConnect(data.Secret)` = `POST /Users/AuthenticateWithQuickConnect {Secret}` and reads `result.User.Id`+`result.AccessToken`. pharos (a) never registered /Users/AuthenticateWithQuickConnect (POST 404); (b) omitted `Secret` from the Connect response so `data.Secret` was undefined → apiClient.quickConnect(undefined) rejected before any request; (c) minted the token inside Connect AND consumed the pending record on that poll, so even with (a)+(b) fixed the record was gone before finalize.|V22. Connect is read-only + echoes Secret + no token; one-shot consumption moved to new QcMsg::Consume (removes only an AUTHORIZED entry); new POST /Users/AuthenticateWithQuickConnect (unauth; Secret is the credential) consumes → issues token vs the authorizing user (stores.get().into_user() + stores.issue, record_activity) → returns the same AuthenticationResultDto as AuthenticateByName; unauthorized/spent → 401. Guards: jellyfin_quick_connect.rs full_flow_finalizes_at_authenticatewithquickconnect (poll non-consuming ×2, finalize-before-authorize 401, finalize 200 + token authenticates /Users/Me, second finalize 401) + quick_connect.rs consume_before_authorize_returns_none_and_keeps_entry.
B9|2026-07-11|SyncPlay group watch near-unusable: every resume/seek hung group ~30s, one member routinely stranded on spinner while other played. Readiness gate protocol-inverted vs traced jellyfin-web 10.11.8: client posts /SyncPlay/Ready ONLY on player 'playing' transition (paused player posts nothing; StateUpdate Waiting drives OSD icon only), but group.rs gated Unpause/Seek on Ready from ALL members while WITHHOLDING the command that causes the transition → circular wait, resolved only by 30s anti-wedge; unpause spam replaced gate = fresh deadline each click = indefinite hang; anti-wedge Play landed ≥30s after click = outside browser user-activation window → Firefox blocked video.play() → stranded spinner. Compounding: (a) ServerMsg::Pause carried no PositionTicks and jellyfin-web schedulePause seeks to command's ticks → every server Pause rewound client to 0:00, drift correction off by default = permanent desync; (b) /SyncPlay/SetIgnoreWait was no-op 204 → member that halted (client 30s playbackstart budget) stayed in every future gate's pending set → all later gates also ran full 30s.|V21. Unpause of non-buffering group broadcasts Play immediately (gate only on actively-buffering members); SeekTo broadcasts Seek at gate entry (clients ACK via scheduleSeek 'ready') and gates only the resume; SetIgnoreWait honored (excluded from pending, releases open gate, Ready clears flag, persisted); Pause carries frozen position everywhere (catch-up now single positioned Pause — survives client's single-slot pre-time-sync command queue); late-Ready heal only while Playing (paused heal re-triggered Ready = command loop); StateUpdate reasons cased to client's OSD match strings. Guards: group.rs unpause_with_nobody_buffering_starts_immediately, unpause_gates_only_on_buffering_members, seek_broadcasts_seek_at_gate_entry_then_play_on_ready, ignore_wait_member_is_excluded_and_releases_open_gates, pause_broadcast_carries_the_frozen_position, ready_while_group_paused_sends_nothing; socket.rs translate_pause_carries_position_ticks.
B94|2026-07-18|Cannot seek in a direct-play mp4 (Avatar: The Way of Water, h264/aac mp4, SupportsDirectPlay) on Firefox. Firefox's `<video>` opens playback with `Range: bytes=0-` (spans the whole file) and REQUIRES a 206 to that opening probe to enable seeking. actix-files 0.6.10 gates its 206 on `offset != 0 || length != total` (named.rs:605), so a full-file range returns 200 while still stamping a Content-Range header. Firefox reads the 200 as "server ignores ranges", marks the media non-seekable (seek bar inert / restarts at 0), and never sends offset ranges — prod logs show every `/videos/{id}/stream.mp4` at 200, zero 206. curl proved it: `Range: bytes=0-` → 200+Content-Range; `Range: bytes=1000000-2000000` → correct 206. Cause: deliver_stream trusted NamedFile's status verbatim. Not Avatar-specific — any browser direct-play file on Firefox (Chrome retries with offsets so tolerates it).|V47. deliver_stream promotes any Range-request response that carries Content-Range but is still 200 to 206 (`has_range && status==OK && has CONTENT_RANGE → PARTIAL_CONTENT`). Guard: jellyfin_stream.rs full_file_range_bytes_zero_dash_returns_206 (bytes=0- → 206 + Content-Range + full body).
B95|2026-07-19|Cannot seek in a direct-play mp4 on Firefox — seeking snaps back to the furthest-buffered point and advances only as more downloads. Firefox HEAD-probes a progressive `<video>` source to learn whether the server honours byte ranges before treating the media as seekable; jellyfin-web hands the browser an EXTENSIONED DirectPlay URL (/videos/{id}/stream.mp4) but HEAD was registered ONLY on the extension-less /videos/{id}/stream, so the probe 404'd → Firefox read "no range support" and collapsed HTMLMediaElement.seekable to buffered. Chromium opens with GET bytes=0- (no HEAD) so it was unaffected. First half of the Firefox DirectPlay-seek saga; B101 later found the HEAD handler itself reported Content-Length 0.|V47/V48. Route HEAD /videos/{id}/stream.{ext} to the same head_video handler as the extension-less form (already emits Accept-Ranges + Content-Length + Last-Modified). Guard: jellyfin_stream.rs head_alt_extension_route_advertises_ranges.
B96|2026-07-19|The jellyfin-web dashboard user page (profile/access/parental control) GETs /Users/{id} for the user being ADMINISTERED, not the signed-in admin. user_by_id confined every caller to their own record (requested != bearer_id → 403 "user mismatch"), so an admin opening another user's page got a 403 jellyfin-web never catches → the page spun forever.|B96. An admin may read ANY user's record (load the requested user, return ITS dto incl. config); non-admins stay scoped to self. Mirrors real Jellyfin (user administration is an admin capability). Guard: jellyfin_admin admin_can_get_another_users_profile + non_admin_cannot_get_another_users_profile.
B97|2026-07-19|/Items/Latest ("Recently Added") was never actually recency-ordered: latest_items returned list_items_cached() order (id/cache order) and .take(limit) off the front, so a freshly-scanned title (newest DateCreated) never appeared in the home row — past the limit older items filled it, and a movie added days after the bulk import sat outside the returned slice entirely.|B97. Sort the filtered set by created_at desc (id tiebreak) BEFORE the limit; applies to every IncludeItemTypes the row requests (Movies + TV). created_at feeds the DTO DateCreated so it matches what the client shows. Guard: jellyfin_latest latest_orders_by_date_created_desc + latest_newest_is_first_and_respects_limit.
B98|2026-07-19|A library scan wiped 8192/13972 media rows (Supergirl and others vanished) after a PARTIAL NFS listing — the mount returned far fewer files than exist yet reported walk_errors==0, so mark-and-sweep deleted every "unseen" row as if the media were gone. A transient mount hiccup could thus erase most of the catalog with no error.|V51. sweep_unseen is blast-radius-capped via sweep_exceeds_guard(unseen, total): a sweep removing BOTH >25% of rows AND ≥100 rows is BLOCKED (logged), while a genuine large delete under the fraction cap still proceeds. Recovery = fresh full mount + full scan (re-adds ungated). Guard: sqlite_store sweep_guard_blocks_catastrophic_mass_delete + sweep_guard_allows_large_delete_under_the_fraction_cap + small/large/fraction unit tests.
B99|2026-07-19|Google TV pages a season via GET /Items?ParentId=<season>&IncludeItemTypes=Episode with NO SortBy; sort_primary(None) defaulted to SortName, so pharos ordered the season ALPHABETICALLY BY EPISODE TITLE (Neon Genesis Evangelion S1 → E7 "A Human Work", E3 "A Transfer", E18 "Ambivalence"…). /Shows/{id}/Episodes already sorted right; only the generic /Items path (also used by the Android TV SDK for a season) was affected.|V43. build_media_query treats a query as episode-scoped (IncludeItemTypes=Episode OR a Series/Season parent) and, when the client sends no SortBy, defaults it to the same (season, episode) chain the explicit ParentIndexNumber,IndexNumber token resolves to (B87); non-episode grids keep the SortName default; an explicit SortBy is still honoured. Guard: episode_no_sortby_defaults_to_season_then_episode_when_kind_is_episode + _for_a_season_parent + non_episode_no_sortby_still_defaults_to_name.
B100|2026-07-20|spawn_scan_tracked called try_start but ran the scan even when it returned None (a scan already running), so every /Library/Refresh spawned another concurrent full catalog walk. During the B98 recovery this stacked ~10 concurrent scans re-probing 14k files over NFS, I/O-starving the single k8s node until etcd/apiserver went ServiceUnavailable.|V52. One scan at a time: start_or_mark_pending acquires the single slot or folds the request into ONE pending re-run (force OR'd) firing when the current scan finishes; finish_or_rerun keeps the task Running across the re-run (no Idle gap); a cancelled run never re-runs; the coalesced re-run rescans the full current media-root set. Guard: scan_tasks concurrent_starts_coalesce_into_one_pending_rerun + a_lone_run_with_no_pending_finishes_to_idle + a_cancelled_pass_does_not_rerun_even_with_pending.
B101|2026-07-20|Cannot seek to an unbuffered position in a direct-play mp4 (Raiders of the Lost Ark, h264/aac mp4, SupportsDirectPlay) on Firefox — same symptom as B94, DIFFERENT root. The file is non-faststart (atom order ftyp/free/mdat → moov seek-index at EOF); to seek, Firefox HEAD-probes /videos/{id}/stream.mp4 to learn the byte length so it can range-fetch the trailing moov, then requests offset ranges. The HEAD returned Content-Length: 0 for a 2.3 GB file: head_response built the reply with `.finish()` → empty `()` body = BodySize::Sized(0), and actix-http's h1 encoder derives a HEAD response's Content-Length from the body BodySize (bytes never sent) AND drops any hand-inserted Content-Length header (encoder.rs skip_len). Zero length reads as "nothing to seek" → seekable collapses to buffered. B95 had added the HEAD route (fixed a 405) but left the size bug. Faststart files (moov at front) seek fine regardless, so the bug is title-specific (Zombieland too). curl proved it: HEAD → Content-Length 0; mid-file Range → correct 206.|B101/V48. head_response serves the HEAD through actix-files NamedFile: its body is a SizedStream declaring BodySize::Sized(file_len) so the encoder emits the real length; the reader is never polled on a HEAD so no bytes are read. Also sets Accept-Ranges, Content-Type, ETag, Last-Modified + conditional-GET preconditions. Prod-verified post-deploy: HEAD now Content-Length 2361610362 + accept-ranges bytes. Guard: jellyfin_stream.rs head_reports_real_content_length (asserts the HEAD body BodySize == file size — a header-only assertion can't catch it since the clobber is in the wire encoder, downstream of call_service).
B102|2026-07-20|A remuxed/downmixed audio stream (/Audio/{id}/universal when the source codec is client-unacceptable or channels exceed the cap → audio_remux) could not seek at all. It is a live ffmpeg pipe (chunked, no Content-Length, not byte-seekable), so jellyfin-web seeks by re-requesting the URL with a fresh StartTimeTicks (same contract the progressive-WebM transcode honours). audio_remux spawned ffmpeg from t=0 with no -ss and ignored StartTimeTicks, so the encode always restarted at 0 and seeking was capped to already-streamed audio.|B102/V49. audio_universal parses StartTimeTicks and threads it into audio_remux, which emits `-ss <secs>` as an INPUT seek (before -i) for a fast keyframe seek + decode-forward. argv now built by a pure audio_remux_args helper so the ordering is unit-testable without spawning ffmpeg. Guard: audio_remux_args_seek_is_input_option (-ss precedes -i, correct seconds, downmix preserved) + audio_remux_args_no_seek_at_zero.
B103|2026-07-20|The VP9 continuous-audio VOD playlist (vp9_audio_playlist) computed its segment count from probe.duration_ms.unwrap_or(0.0) with NO fallback, so a media row whose persisted probe lacked a duration collapsed the whole audio timeline to a single 6 s segment (.max(1)) — the client could then only seek within the first segment. The video variant already avoided this by routing duration through load_hls_item (persisted probe → live-ffprobe fallback).|B103/V50. vp9_audio_playlist resolves duration via load_hls_item, the same ffprobe-backstopped path the video variant uses. Guard: vp9_audio_rendition.rs audio_playlist_recovers_duration_when_probe_lacks_it (seeds no duration_ms against a real 42 s clip; asserts the playlist enumerates the full timeline ~7 segments, not 1).
B104|2026-07-20|The Jellyfin Android app (org.jellyfin.mobile, UA "Ktor client", ExoPlayer/media3) renders ASS subtitles as opaque BLACK BARS (text invisible) — reported on Neon Genesis Evangelion S01E15. Cause: pharos decided subtitle delivery purely by is_text (text→External, image→Encode) and IGNORED the client's declared DeviceProfile.SubtitleProfiles (the string "SubtitleProfile" appeared nowhere in the codebase), so it advertised every ASS track as External + SupportsExternalStream. The Android app's media3 SSA renderer can't handle heavy fansub typesetting → black boxes; and its profile omits ass/ssa by default (gated behind exoPlayerDirectPlayAss, off) so per Jellyfin semantics the server should have burned them. jellyfin-web was unaffected (SubtitlesOctopus/libass renders ASS). The session was already HLS-transcoding, so a burn was ~free.|V53. pharos now parses SubtitleProfiles and decide_subtitle_delivery routes each track (subtitle_delivery.rs); a burn-required text sub is advertised Encode + its index forwarded on the TranscodingUrl; a DirectPlay decision downgrades to Transcode when the selected/default sub must burn; the transcoder rasterizes ASS via the subtitles= filter from the cached .ass sidecar + fontsdir (NOT the whole source per segment — the final-review-caught NFS-stutter regression), setpts-aligned per segment. Image burn unchanged (overlay). Guards: see V53. Deployed 2026-07-20.
B105|2026-07-20|Swapping audio track mid-playback (english↔japanese, Code Geass s02e03) on the VP9/fMP4 path (Firefox) desynced A/V. VP9 video segments are audio-free; audio is a SEPARATE continuous rendition. The rendition anchored its `-ss` input seek AND `-output_ts_offset` to the NOMINAL grid `start_seg*6.0` (hls_cache.rs audio_hls_args), while the video segments seek+tfdt to the FRAME-SNAPPED grid `segment_start_secs = round(seg*6.0*fps)/fps`. On a non-integer-fps source the two grids differ by up to ~1 frame, so the renditions no longer share a timeline → a fixed sub-frame skew injected at the switch point. Same failure the video path already documents in build_args_for_device ("nominal 6.0 s tfdt grid accumulates ~6 ms/segment into audible A/V drift").|B105/V54. Thread source frame rate into ensure_audio_hls/_covering/audio_hls_args; frame-snap the audio seek anchor via audio_seg_start_secs (mirrors segment_start_secs); frame-align the audio playlist EXTINF to the video variant (segment_time_range); bump HLS_GEN_VERSION 3→4 to orphan stale nominal-anchored _audiohls dirs. Guard: hls_cache.rs audio_hls_args_seek_anchor_is_frame_snapped_to_video_grid (seg1 @ 23.976 fps → -ss 6.006 + -output_ts_offset 6.006, not 6.000).
B106|2026-07-20|Same report: the audio-track swap ALSO took an extraordinarily long time to resume, AND in a SyncPlay group both members switching audio wedged the whole group on a spinner until someone seeked backward. The audio rendition is one continuous ffmpeg encode (no -t, -hls_list_size 0) running to EOF; want_seg only governs WHEN a segment appears. The rule `want_seg <= AUDIO_SEEK_LOOKAHEAD_SEGS(20) => start_seg 0` forced any switch inside the first ~120 s to re-encode 0→playhead (Opus + whole-file NFS demux, throttled behind the bg-IO gate) before playback resumed. A switch lands in a fresh -a{idx} dir (no from-0 session) so ALWAYS paid the full head re-encode. In a group this escalated: both switch → both post /syncplay/buffering → group freezes (group.rs:1704); resume is gated on `!any(member.buffering)` (group.rs:1746/1884) so the cold rendition never re-readies → 30s anti-wedge cycles + jellyfin-web's 30s WaitForEventDefaultTimeout haltGroupPlayback drops the member. Seeking backward lands on already-encoded segments → readies → resume.|B106/V55. Extract pure choose_audio_start_seg: reuse a running from-0 session only in the near-start window, reuse a running seek session within its lookahead, else seek straight to the playhead; from-0 whole-file rendition reserved for a genuine start-of-file request. Cuts the switch re-buffer to ~1-2 s (first seek-to-playhead segment) so a group member re-readies well inside both the freeze and the client 30s budget. ffmpeg-written playlists are throwaway (pharos synthesises the audio m3u8) so seek sessions don't regress the playlist. Guard: hls_cache.rs shallow_switch_seeks_to_playhead_not_from_zero + play_from_start_uses_whole_file_from_zero_session + sequential_early_play_reuses_running_from_zero_session + deep_seek_past_running_from_zero_spawns_seek_session + segment_within_seek_session_lookahead_is_reused. Also retargeted stale ground-truth vp9_audio_switch.rs at the separate rendition (had probed now-audio-free video segments).
B107|2026-07-25|BROWSER PLAYBACK (Zen/Firefox 152, prototype.aliflix) — every video failed with `HTTP "Content-Type" of "video/matroska" is not supported`, a type pharos never emits, while jellyfin-web retried PlaybackInfo forever (2333 decisions in minutes, one new play_session_id per ~100 ms, ~30 range GETs/sec, Angie warning `an upstream response is buffered to a temporary file`). Root cause: `container_for` resolved ffprobe's ambiguous `format_name = "matroska,webm"` by preferring the `webm` alias, on the premise that the file IS the more specific member — false for the library's 9443 .mkv files. Three consequences compounded: (1) the negotiator compared `webm` against the client's DirectPlayProfiles, matched jellyfin-web's webm profile and returned DirectPlay (`pharos_playback_decision_total` carried exactly ONE series, `direct_play_block="not_blocked"` — direct play had never once been blocked); (2) the B80 guard `source_is_matroska` tests for the `matroska`/`mkv` token that the rename had already stripped from `source.container`, so it read false and the downgrade that exists precisely for this case never fired; (3) `webm_relabel_playable` decided browser playability from CODECS being WebM-legal, accepting any Matroska carrying them, so it returned true for exactly the files it was meant to catch — and it also gates `SupportsDirectStream`, so pharos advertised the raw file as playable with no TranscodingUrl. Firefox ships no Matroska demuxer at all: it reads the EBML DocType, finds `matroska` where WebM requires `webm`, and refuses the resource whatever codecs are inside — reporting the container it SNIFFED rather than the `video/webm` header pharos actually sent, which sent the investigation hunting a header bug that did not exist. Proven by reading the file's first 256 bytes through the public URL: EBML DocType `matroska`, track `V_AV1`.|B107/V56,V57. `container_for` takes the source path and disambiguates the Matroska family by extension (mkv/mka/webm), ISOBMFF untouched; `webm_relabel_playable` extracted as `source_plays_raw_in_browser` and gated on a genuine `webm` container before any codec clause; `DeliveryMime`/`source_is_webm_legal` stop relabelling a Matroska `video/webm` (a .mkv now serves `video/x-matroska`, and open/range-seek/HEAD still agree on ONE type). Observability shipped FIRST as its own commit: the decision log gains `source.container_probe` (ffprobe's raw alias list, beside the token pharos derived) and `client.direct_play_containers` (what the client actually declared), so a container decision is comparable from one line instead of reconstructed from a DeviceProfile by hand — LogQL `{namespace="pharos"} |= "playback decision" | json | source_container != source_container_probe`. Both fixes verified red-then-green by disarming them. Full `just test` 1641/1641, clippy -D warnings clean.
B108|2026-07-25|HLS SEGMENT STALL (hls.js `fragLoadTimeOut`, prototype.aliflix) — cold segments took ~94 s end-to-end while the encode inside them took ~3.4 s: `queue_wait_ms` 88159-91159 against `encode_ms` 3103-3613 on Cobra Kai segs 180-184, and 164566/4477 on a prewarmed seg 6. Root cause: `spawn_segment_prefetch` is dispatched BEFORE the client-blocking `segment_bytes_keyed` for the same request (hls.rs — the comment says so: "spawned before this segment's own read so they pipeline"), and both entered the scheduler's single FIFO as indistinguishable jobs. Depth is `hw_budget - 2` capped at 6, plus up to 4 burn-horizon segments, per served segment — so a handful of requests buried the segment a browser was actually waiting for under tens of speculative encodes, and `pending_cap` 256 let the backlog grow unbounded. Compounded by the segment cache's per-key `fetch_locks`: a queued prefetch holds its key's lock across the whole scheduler wait, so a client request for that same segment blocks on `lock_wait_ms` and inherits the entire queue wait it was meant to be spared. The diagnosis cost two wrong turns that the signals could not settle — "Nvenc registered but never selected" (read from gauges at IDLE, correlated with segment logs from an already-replaced pod) and "video_remux means no re-encode" (the master playlist says `CODECS="avc1.640028,mp4a.40.2"`) — because no signal distinguished a client's segment from a guess: `queue_wait_ms` proved a segment WAITED and nothing said what it waited BEHIND.|B108/V58. Instrumentation shipped FIRST as its own commit: `JobClass` threaded from the mint entry point through the scheduler; `SchedSnapshot` splits pending by class, names the oldest entry's age, counts inflight jobs and live streams; `DeviceStat` attributes each device's occupancy by class so `in_use` minus the attributed count is unexplained; a job dispatched after >3 s logs the queue it escaped; `submit_live` logs the permit it took, its device, and its hold duration on drop (that path holds a permit for a whole progressive transcode and reports no completion, so its occupancy previously had nothing behind it), and a rejected live stream logs the rejection plus the occupancy that caused it (the caller then runs an UNSCHEDULED inline ffmpeg holding no permit — load the scheduler cannot see). New series: `pharos_transcode_pending_by_class{class}`, `_pending_oldest_seconds`, `_inflight_jobs`, `_live_streams`, `pharos_transcode_queue_wait_seconds{class}`, `_encode_seconds{class,device}`, and a `class` label on `pharos_segment_produced_total` so success and failure stay symmetric. Then the fix: background admitted only out of spare capacity, never queued. Signal verified red-then-green by disarming the class split. Full `just test` 1648/1648, clippy -D warnings clean.
B109|2026-07-25|OBSERVABILITY DESTROYED BY NOISE — libav wrote decoder/demuxer diagnostics straight to fd 2, which the crash-isolated `transcode-worker` inherits to the pod's stdout: ~11.5M lines/hour on `{namespace="pharos"}`, evicting the structured JSON logs from both `kubectl logs` and Loki within minutes. It cost real diagnosis time twice — the Zombieland SyncPlay incident and the B108 segment stall, during which the very segment logs needed to prove the queue theory had already been flushed out by the flood. The earlier mitigation (9dd1d12, cap the global level to Error + `AV_LOG_SKIP_REPEATED`) did not touch the bulk of it, because the lines a damaged source emits are THEMSELVES at Error — "Invalid NAL unit size", "missing picture in access unit", "EBML header parsing failed", "Header missing" — and the library holds ~133 real-size but zeroed files that produce them per frame, continuously. Cause: level is the wrong axis. The defect was the DESTINATION (raw stderr, outside every filter and every structured sink), not the verbosity.|B109/V59. `libav::logbridge` installs an `av_log` callback that formats the line exactly as libav would, strips the per-decoder pointer, and emits it on the `libav` tracing target at DEBUG (fatal and above at ERROR), so the default `info` filter drops the flood and `PHAROS_LOG=info,libav=debug` brings it back as structured records. Volume stays queryable via `pharos_libav_log_total{level,component}`, counted before any filtering, with a bounded `component` label. Note this replaces libav's default callback and with it `AV_LOG_SKIP_REPEATED`, whose repeat-collapsing lives inside that callback. Signal verified red-then-green by disarming `install()`.
B110|2026-07-25|TAIL SEGMENT 500 — playing a title to its end returned 500 on the final segment. `SegmentWindow::for_segment` asked for a full 6.006 s even for the last segment, where the source ends partway through, so the encoder stopped at EOF and the B-class completeness check (which compares produced duration against the window's) read that as truncation, retried with a deeper preroll, got the same answer and failed. Caught by its own log on Guilty Crown S01E01 (1372.121 s, tail segment 228): `reached only 0.021s of the requested 6.006s` at attempt 0 (preroll None) and attempt 1 (preroll 45s). That is the last segment of nearly every title, since a length is rarely an exact multiple of 6.006 s — and the playlist ALREADY advertised a clamped tail, so the transcode and the EXTINF had been describing different windows. The first fix reached for `duration_ms.unwrap_or(0)` at the four call sites, which converted "duration not probed yet" into "clamp to the grid's 0.01 s floor" for EVERY segment index: the compat Playwright suite (whose seeded rows are unprobed — its startup rescan fails on `UNIQUE constraint failed: media_items.path`) went from 23 passed to 2 failed, both on `video.currentTime` never leaving 0, with the server's own `hls segment transcode far below realtime` WARN carrying `seg_secs: 0.01` on segment 0.|B110/V60. The window is clamped through `SegmentGrid::frame_snapped_range` so there is ONE clamp shared with the playlist that advertises it, and `for_segment` takes `Option<f64>` so a call site cannot launder an unknown duration into a zero (a non-positive duration is treated as the same absence). Signal verified red-then-green by disarming the guard: `total Some(0.0) seg 0: got 0.01 s, want the nominal 5.966666666666667 s`. Verified by query, not assertion — the pre-fix compat run logs two `"seek_secs":0.0,"seg_secs":0.01` windows, the post-fix run logs none and passes 23/23. Full `just test` 1652/1652, clippy -D warnings clean.
B111|2026-07-25|A video-only fMP4 segment carried a second track: `index=1 bin_data/codec_type=data/codec_tag=text/handler_name=SubtitleHandler`, on a rung where audio is a separate rendition. It is an MP4 CHAPTER track, not a subtitle, which is why `-sn` — unconditional in `build_args_for_device` since the VP9 stray-`mov_text` fix — did not stop it: chapters are not an input stream, ffmpeg copies them by default, and the mp4 muxer materialises them as a QuickTime chapter track. Every fMP4 rung of every chaptered source carried one (the VP9 rung of the same title was confirmed to carry it too), i.e. any ripped episode. FOUND while hunting the B112 playback wedge and initially reported as its cause — WRONGLY. `just probe-flags` builds two ladders differing only in `-map_chapters -1` and plays both with real hls.js: the chapter-bearing ladder played cleanly in Firefox AND Chromium, no errors, no refetch. The defect is real and the fix stands as hygiene; the causal claim was refuted before it shipped, and B112 records what the wedge actually was.|B111/V61. `-map_chapters -1` beside the existing `-sn`. No cache generation bump: segments already cached with the track were measured playable, so orphaning a whole generation would have been churn justified by a diagnosis that turned out to be wrong. Guard asserts the INVARIANT — a video-only segment carries exactly one video track — verified red before the fix with `got ["video", "data"]`.
B112|2026-07-25|PLAYBACK WEDGE (Cobra Kai, Zen/Firefox 152, prototype.aliflix) — `HLS Error: Type: otherError Details: internalException` from hls.js's `onWorkerError`, playback never advancing. The server log showed ONE segment (`h264cmaf/19.m4s`) fetched 17 times inside a single second, every request `200`. Not a byte defect: the exact bytes prod served were pulled back through a port-forward and replayed through real hls.js in REAL Firefox 152 (`just probe-bytes ... system-firefox`) — video, audio and the h264+Opus pairing all played to completion, 8 fragments, zero refetches. Two hypotheses were refuted the same way rather than assumed (Firefox rejecting `avc1` in MSE — the user's own console returned `true`; a video/audio playlist timeline mismatch — both playlists carried 229 segments and an identical 1371.070 s total). The actual cause, from Loki: the client asked for the segment at 19:52:49 and the server produced it at 19:54:34. `queue_wait_ms 214534, encode_ms 2788, device cpu` — 214 SECONDS waiting for a device permit against a 2.8 s encode, with `pending_interactive 0, pending_background 0` and `inflight_background 6`. Nothing was queued; six already-admitted BACKGROUND jobs held every permit while a client-blocking job starved. B108 stopped speculative work from QUEUEING ahead of clients; it does not stop work already admitted from HOLDING. The 17 fetches were hls.js retries released together when the one transcode finally landed. Compounding it, 60% of segments in the window ran on `cpu` despite NVENC being confirmed healthy at boot (`sessions: 8`), and NVENC's median encode (2932 ms) was barely better than the CPU's (3555 ms) — a gap far too small for an offloaded decode. Diagnosis cost: the line naming the winning device sat at DEBUG while the deployment runs at INFO, so nothing in the logs could distinguish a working GPU from an idle one; and `kubectl logs` had already rotated the window away under INFO chatter (Loki still had it — B109's lesson, re-learned).|B112/V62. Instrumentation shipped FIRST, before any fix: `DecodeAccel` makes GPU decode a reported verdict rather than an inference, `record_placement` emits the placement at INFO with the losing candidates, and each job gets a `transcode_job` span carrying its facts through to `outcome`. The starvation itself (background HOLDING permits, and the CPU-vs-NVENC placement skew) is NOT yet fixed — named here so the fix is measured against the signal rather than guessed at, which is what this bug cost three times over.
B113|2026-07-25|PLAYBACK STALL (Ghost in the Shell 1995, a 36.7 GB / 4979.8 s ≈ 59 Mbps 1080p remux, `SupportsDirectPlay:false` → full transcode). Client-visible symptom: three `404 audio segment not ready` on the VP9 audio rendition (`a5.m4s` twice, `a48.m4s`), which stalls hls.js. Root cause was upstream of the audio path. TWO PlaybackInfo calls 6 s apart (21:29:02, 21:29:08), the second carrying `AudioStreamIndex=1` and a new PlaySessionId; both sessions then requested the SAME video segments. `SegmentIdentity::new` folded the client's audio track into the key unconditionally, and the fMP4 rungs pass it through even though they choose `AudioDelivery::Separate` — audio is a demuxed rendition there and the argv ends in `-an`, so the segment carries no audio at all. Two keys for identical bytes, so the single-flight lock (keyed by exactly that identity) could not collapse them and BOTH transcoded: 6 of 47 distinct (container, seek) keys encoded twice, each pair byte-for-byte equal (8972316, 10498618, 10781180, 9964804 — proof of two independent deterministic encodes, not one file counted twice). The doubled GPU + NFS load dragged 6.006 s segments from 2087 ms to 13383/15659/16240/16990 ms — BELOW REALTIME — which starved the continuous audio session reading the same NFS stream. It fell below one segment per `AUDIO_POLL_STALL` (3 s), so a live session was declared wedged and 404'd. The 12 s cold grace could not save it: `audio_session_progress` was a GLOBAL high-water mark across every session under the rendition root, so once any session had produced anything, a freshly-spawned seek session was judged on the stall budget while still opening a 36.7 GB source. Diagnosis cost: the handler collapsed the cache's error to the literal string "audio segment not ready" and logged nothing on that path, so which of three budgets expired took a code read; and two `out_bytes: 0, outcome=done` jobs sent the investigation down a false empty-encode lead — they were live-stream sinks, whose bytes the worker never sees.|B113/V63,V64. Four commits, instrumentation FIRST per ODD: `AudioWaitGiveUp` + `HlsCacheError::AudioNotReady` name the expired budget through to the HTTP body with `pharos_audio_wait_total{outcome}` on both outcomes; then the key fix (audio track keys a segment only when the segment carries audio); then the stall budget raised to 10 s and asserted against `SEGMENT_SECONDS`; then read-wait progress scoped to the sessions that could serve the request. `out_bytes` is now recorded only for sinks that can measure it, so absent means unmeasured and 0 means empty.
B114|2026-07-25|SYNCPLAY EVICTION — member "lace" was silently removed from a live watch party mid-episode; their next SyncPlay command answered `NotInGroup`. Not a socket fault: a deploy hydrated the group at 21:15:58, `apply_persist` stamped every restored member seen-now, and each therefore got exactly `MEMBER_TTL_MS` (150 s) to reconnect. The LAN member reconnected in 28 s. lace is on WAN and jellyfin-web's socket backoff took 162 s — `21:18:28 pruning unresponsive member (ghost)` → removed; `21:18:40 /socket connected + registered in hub`; `21:18:53 POST /syncplay/pause → session is in no group`. Twelve seconds. lace's socket did not genuinely drop until 21:30:42, twelve MINUTES after the prune, so the member was never unresponsive. The prune judged every member on one TTL, but a member silent WITH a live socket (a dead client holding a place in every readiness gate) and one whose socket is GONE (holding nothing, since `follower_ids` filters on `connected`) are opposite situations. Compounding it, `apply_persist` restored members `connected: true`, asserting a socket this replica had never seen — which both made gates wait on absent members and put every restored member on the short TTL.|B114/V65. `DISCONNECTED_TTL_MS` (10 min, sized for a WAN client's exponential backoff after a deploy) for members with no socket, ghost TTL retained for silent-but-connected ones; hydrated members restored `connected: false`, which is what makes the longer grace safe. The prune log now carries `connected` so the two classes are distinguishable in Loki.
B115|2026-07-25|CHAPTER THUMBNAILS 400 — `GET /Items/{id}/Images/chapter/{n}` answered `400 unknown image type`, seven in one burst while jellyfin-web rendered the chapter strip for a playing title, each carrying a distinct `tag` (so the item genuinely advertised chapter images the server then refused). The chapter route EXISTED; it was unreachable. It was registered after `/items/{id}/images/{image_type}/{image_index}`, and actix matches in REGISTRATION order — a literal segment does not outrank a parameter — so every request went to the generic handler, which parses the segment as an `ImageRole`, fails, and 400s. A stale comment in `register` asserted the opposite ("actix matches the fixed segment count, so ordering is not load-bearing here"), which is true only between routes of DIFFERENT arity and was the reasoning that allowed the ordering. Found by query, from the `WARN http request completed with error status` line that exists for exactly this purpose. NOT a defect, checked and dismissed during the same investigation: `otel.status_code: "OK"` beside `http.status_code: 400` is deliberate in `tracing-actix-web` (`root_span_builder.rs:77`) and follows the OTel HTTP convention that a SERVER span is not an error for a 4xx — forcing ERROR there would inflate Tempo's error rate with every client probe.|B115/V66. Chapter route moved above the generic one; the stale comment corrected in place so the next route added here does not inherit it. Guard drives the registered app, since a shadowed route is invisible to a handler-level test.
B119|2026-07-26|AUDIO AHEAD OF PICTURE FOR A WHOLE TITLE (Rogue One; not reproducible on titles that DirectPlay, which is why it first looked media-type specific). The source's eac3 stream starts at `start_time 1.700000` against a video stream at 0. `ensure_continuous_audio_covering` recorded the start its session was ASKED for — `0.0` for a from-0 session — but the produced file's first sample sits at the audio stream's own start, because ffmpeg preserves it (verified by running the production argv against the real source over HTTP: first audio pts 1.678667; a seek session was near-exact at 587.978667 against a requested 588). The segment slices that file with `rel = input_seek - start_seconds`, and `-ss` on an input is measured from that input's own start time, so `rel` landed 1.7 s too deep and every frame received audio from 1.7 s later. It affected the ENTIRE film rather than just the opening, because once a from-0 session exists `ensure_continuous_audio_covering` tries candidate `0.0` first and every later segment reuses it — which also explains why resuming mid-film looked better (seek sessions were near-exact). Proven by cross-correlating our produced audio against the source at the 600 s mark: peak at +1680 ms, matching the stream's 1.700 s start. DIAGNOSTIC HISTORY WORTH KEEPING: this hypothesis was raised and WRONGLY DISMISSED twice before it was proven — once because a synthetic AAC fixture preserved the offset through both session types, and once because the real segments 0-1 were silent (the film has no sound until ~17.6 s, so silence proved nothing about alignment). Neither check tested content alignment on a loud passage, which is the only thing that could have settled it. The user's correction that the audio was AHEAD rather than behind is what re-opened it.|B119/V71. Probe the produced file's first audio packet and record THAT as `start_seconds`, cached per session file; log the skew at INFO when a session does not start where it was asked to. Muxed mpegts cache tag bumped 25 -> 28 (the shift is baked into cached segments); the fMP4 rungs are audio-free and unaffected. NOT covered: the demuxed `/vp9/audio` rendition is a different path and has not been measured for the same defect.
B120|2026-07-26|AUDIO 1.7 s AHEAD OF PICTURE IN BROWSERS, for the whole of any title whose audio stream starts after its video — the demuxed `/vp9/audio` rendition carrying the same defect class as B119 on a different code path, found by going back and MEASURING the path B119 explicitly recorded as unmeasured. Rogue One's eac3 starts at `start_time 1.700000` against video at 0. The whole-file session runs ffmpeg with no `-ss`, and ffmpeg re-bases the only mapped stream so its first sample sits at zero — silently dropping the offset, so fragment N holds source `[audio_start + N*6, ...)` while the playlist and the (source-anchored) video place it at `N*6`. Proven live: fragment a17 served at playlist position 101.997 s, content beginning at 103.700 s, cross-correlation peak 1.036. A title whose audio starts at 0 was measured as a control and was clean. `-output_ts_offset`, `-copyts` and `-avoid_negative_ts disabled` were each measured on ffmpeg 8.1 and change nothing (-1690 ms in all six combinations); `-af adelay` lands it at +9 ms, the opus preskip.|B120/V72. Probe the source audio track's start once per (source, track) and pad the from-0 session by it; log at INFO when a track needs it. HLS_GEN_VERSION 9 -> 10 to orphan every cached rendition. NOTE: the first version of the guard test passed against the bug because requesting segment 2 spawns a session SEEKED to it, which lands honestly — a vacuous test caught only by disarming the fix and watching it stay green.
B121|2026-07-26|A SEEK SESSION'S AUDIO FRAGMENT BUFFERED MINUTES EARLY. ffmpeg's HLS muxer numbers each session's `tfdt` from that session's own first sample, so `a300.m4s` came back anchored at 450.0 s — its 75-fragment offset within the session that started at segment 225 — against a playlist position of 1800.0 s. The `audio_hls_args` comment had asserted since B42 that `-output_ts_offset` makes the fragments source-anchored; it does not, and neither do `-copyts` or `-avoid_negative_ts disabled` (all measured on ffmpeg 8.1). The whole-file session was never affected, because its fragment index already IS its timeline position — which is why this survived undetected behind every from-0 playback.|B121/V72. Shift every `tfdt` by the producing session's start on the serve path, where that start is known; the cache reports it alongside the bytes. Saturate rather than wrap a shift below zero. Timescale read from the rendition's init, failing loudly rather than assuming 48 kHz.
B118|2026-07-26|STUTTER AND ACCUMULATING AUDIO DESYNC (Rogue One worst; also Ghost in the Shell) — reported as two separate faults, one defect. Every hardware-encoded segment carried 143 video frames where its window implies 144. `-enc_time_base 1:90000`, which stops an encoder quantizing the zero-based first-frame pts to its default 1/framerate timebase, is emitted for SOFTWARE ONLY because the hardware encoders reject it; a comment asserted the frame-snapped grid made it unnecessary for them, and that was wrong. Measured on the real argv against the real source one flag apart: `with -enc_time_base` 144 frames first pts 12.012000 (the boundary), `without` 143 frames first pts 12.032856 (+half a frame). Confirmed in production on the SAME segment across two devices — `seg 40 dev Nvenc:0 expF 144 gotF 143 def 1` beside `seg 40 dev cpu expF 144 gotF 144 def 0`. One frame per 6.006 s is 0.7% of the picture; each segment's timestamps stay individually correct (measured A/V offset never exceeded 40 ms across ten minutes of the title), so it does not present as a fixed skew — it ACCUMULATES, reaching ~500 ms after roughly 70 s, which is why the user described the desync as starting almost immediately and the stutter as constant. Two hypotheses were refuted first, by measurement rather than argument: (1) Rogue One's eac3 track really does report `start_time 1.700000` against a video stream at 0, but both continuous-audio session types preserve that offset — proven with a fixture that probes `audio start_time 1.700000`, and against the real file, where segments 0-1 carry 12.01 s of audio at peak amplitude ZERO because the film is silent until ~17.6 s; (2) container A/V misalignment, bounded at 40 ms, an order of magnitude too small. The `frame_deficit` signal added for this hunt (V67) is what settled it, by disagreeing across devices for one segment index.|B118/V70. `-r <source rate>` for the hardware branch, verified to restore the count AND the tiling (`-fps_mode passthrough` restores only the count). Cache tags bumped on all three video rungs in a separate commit, because the defect was in the encoder not the container and a cached short segment outlives the fix — the B116 lesson, applied without having to re-learn it.
B116|2026-07-26|GOOGLE TV CANNOT PLAY ANY TRANSCODED TITLE (Rogue One, Ghost in the Shell, Fringe S01E03; Jellyfin Android TV 0.19.9). ExoPlayer fetched exactly ONE segment and stopped, then retried the whole PlaybackInfo → master → variant → segment cycle — six PlaybackInfo calls in 20 s. Not latency: the retry was served the same segment from cache in milliseconds and stopped identically. The bytes, pulled back from prod and probed, say why — `hls1/360p/163.ts` reports `nb_streams: 1`, video only, on the MUXED mpegts ladder, under a playlist advertising audio. Cause: the guard resolving a segment's continuous-audio slice read `attempt_opts.audio.is_some()`, but `SegmentOpts::to_transcode_options` sets `audio: None` UNCONDITIONALLY ("a segment never runs an audio encoder"), so the condition was always false, `muxed_audio_source` was never resolved, and `build_args` turned `audio: None` into `-an`. Two commits the same day did it: 91ceb24 added the guard while lowering still carried an audio codec, then 5047bb4 made a per-segment audio encode unrepresentable and, without touching the guard, turned it off. Browsers never noticed — their fMP4 rungs deliver audio as a separate rendition and genuinely carry none in the segment, so only the muxed ladder (Android TV) was affected. The existing integration guard could not catch it: `mpegts_segment_timeline` hand-builds `TranscodeOptions { audio: Some(Aac), muxed_audio_source: None }`, a combination the real lowering can no longer produce, so it asserted stereo audio on a path production never takes. All three PlaybackInfo decisions shared `downgrade: subtitle_burn` with `direct_play_block: not_blocked` — the burn downgrade is what put these titles on the transcode path at all, which is why only some titles failed.|B116/V68. Fixed twice, deliberately: first minimally, by reading the SEGMENT's delivery intent; then structurally, by splitting `ResolvedAudio`/`ResolvedSegment` so the broken state has no representation and the compiler enforces it. The structural pass immediately found a second instance the first had missed. INCOMPLETE ON FIRST DEPLOY: the fix governs cache MISSES only, so every video-only segment already on disk kept being served — proven by refetching the exact failing segment from the pod running the fix and probing it (`nb_streams: 1`, byte-identical). Closed by bumping the muxed-mpegts codec tag 8 → 24, which orphans them; the audio-free fMP4 tags (13, 12) were never affected and keep their warm cache.
B117|2026-07-26|CHAPTER THUMBNAILS 400 ON ANDROID / GOOGLE TV — the same defect class as B115, one arity down and a different client dialect. The kotlin SDK addresses a chapter image as `/Images/chapter?imageIndex=N` (index in the QUERY) where jellyfin-web uses `/Images/chapter/{N}`; pharos routed only the path form, so the 2-segment request fell to `/items/{id}/images/{image_type}`, which parses the segment as an `ImageRole` and 400s on `chapter`. Found in the same Loki window as B116 while reading the Google TV playback failure. Notably, the FIRST attempt at the fix registered the new route AFTER the generic one and reproduced the bug exactly — evidence that "remember to order this correctly" is not a durable guard.|B117/V69. Both spellings share one `serve_chapter_image`, and every chapter route is grouped ahead of the `{image_type}` patterns rather than relying on each future edit to re-derive the ordering rule.
B122|2026-07-26|THUMB IMAGES ARE AN ARBITRARY VIDEO FRAME, AND THE PICKER OFFERS NOTHING TO REPLACE THEM WITH (user report: Project Hail Mary's landscape card is "ugly and not useful", with no search button; Rogue One has the button but it "returns no images"). One cause, three symptoms. Neither provider publishes a thumb image TYPE: `parse_tmdb_images` maps `posters`->Primary, `backdrops`->Backdrop, `logos`->Logo, and TVDB the same. So (a) an enriched movie ends up with a Backdrop row and NO Thumb row, and `/Items/{id}/Images/Thumb` — which every video advertises, `image_tags_for` inserts the tag unconditionally — falls through to extracting a frame at `image_seek_seconds`, the title card on a film with a dark opening; and (b) filtering the online candidate list to `Type=Thumb` matched nothing, so the picker was empty even for a matched item. Live counts on the deployed library: of 1619 movies, 740 have a Thumb from a local sidecar and ZERO from any provider, so ~880 films were showing a frame. The third symptom was a different, self-healing cause: Project Hail Mary had no `provider_ids` at all until the backfill reached it, and `/RemoteImages/Providers` returns [] for an unmatched item, which is what hides jellyfin-web's search button entirely.|B122/V74. A backdrop is the landscape still a Thumb wants, so serve it before extracting a frame, and offer backdrops as Thumb candidates in the picker (with `artwork_role_from_type` admitting Thumb so the pick can be downloaded). Containers excluded: `series_metadata` has no Thumb locator, so that pick is refused rather than written where nothing reads it.
B123|2026-07-26|INTRO SKIP WORKS ON SOME EPISODES AND NOT OTHERS (user report: "the intro skip code doesn't appear to be functional"). Not the API and not the client: `/MediaSegments` is served, Android TV 0.19.9 calls it with the five `includeSegmentTypes` repeats `CiQuery` already merges, and 74 of 117 calls in a week returned segments. The detector's RECALL is the defect. `confidence = agreeing / (n - 1)` divides by every other episode in the season, but a comparison only contributes a span when the aligner finds one, so a peer that located NOTHING was counted as dissent and the bar rose with season length. Measured by pulling Fringe S01's cached fingerprints out of the deployed database and re-running the detector: episode 12 had NINE independent episodes agree on 136.7-158.7 (22.0 s, the same length as every emitted intro) and was discarded because 9 < half of 19; episodes 14 (8/8) and 9 (7/8) likewise. Against the comparisons that produced evidence they score 1.00, 1.00 and 0.88. Library-wide the symptom was 3686 Intro rows for 10855 episodes, 217/221 on one show against 39/1124 on another. TWO further defects found alongside, both of which kept it invisible: `set_media_segments` with an empty slice writes no row, so `season_is_current` (which asked for rows) treated every intro-less season as never-analysed and re-fingerprinted it every 30 minutes forever — 460 distinct seasons re-analysed in one week — while the comment beside the write claimed it was stamped; and `SEGMENT_SCHEMA_VERSION` was written into `media_segments` and never read, so it could not force re-detection either.|B123/V75. Divide by the comparisons that located a span (`min_agreeing` untouched, so a lone coincidental pair still cannot set a segment): 11 -> 17 of 20 on the measured season, with the 1-of-2 episode still correctly dropped. `media_segment_scans` records the analysis apart from its result. `SEGMENT_DETECT_VERSION` split from `SEGMENT_SCHEMA_VERSION` and bumped, so the consensus change re-runs arithmetic over cached points instead of re-decoding 10k+ episodes over NFS. MediaSegmentStore gained its first backend-conformance coverage — its Postgres placeholders had only ever been type-checked.
B124|2026-07-26|THE EDIT-IMAGES DOWNLOAD BUTTON DID NOTHING for Primary, Backdrop and Thumb alike (user report, Project Hail Mary). The download was never ineffectual: the POST returned 204, the bytes were fetched and written, the artwork rows were recorded (the deployed database holds tmdb-sourced Primary, Backdrop AND Thumb rows for that film) and the item froze to `manual`. The CLIENT was never told to look again. `image_tag_for` hashed (item id, role) only — a comment beside it recorded the now-false premise that images are deterministic re-extractions, so the hash IS the version — and images are served `Cache-Control: private, max-age=604800`. A picked image overwrites the item's deterministic cache slot, so `/Items/{id}/Images/Primary?tag=6d50f48447ab4ba3` is byte-identical before and after, and a browser holding a fresh week-long entry for that URL never revalidates. The ETag was CORRECT (size+mtime derived, and it did change) and never consulted, because nothing asked. Found by reading the response headers and the advertised `ImageTag` side by side rather than by re-reading the write path, which was working the whole time.|B124/V76. `media_items.art_version` (migration 0050, both backends) rides on the row every DTO already loads; `image_tag_for` takes it; the RemoteImages download, the manual upload and the delete bump it. `set_artwork` deliberately does NOT, since the scanner re-records identical sidecar paths on every pass. Golden bodies regenerated — the tag strings were the only field that changed.
B125|2026-07-26|ANIME SHOWS AND EPISODES DISPLAY RANDOM VIDEO FRAMES (user report: "not of anything useful"). Three independent causes, each ending in the same frame-extract fallback. (1) BOTH providers filed the per-episode still under `ArtworkRole::Thumb` (`tmdb::parse_episode_detail`, `tvdb::parse_episode_detail`), but Jellyfin treats an episode's still AS its Primary and every episode tile requests Primary — so a fully enriched episode still advertised no poster. Of 10855 episodes just 522 carried ANY provider image and 783 had no Primary row from any source, concentrated exactly where the report came from (Rick and Morty 90, Code Geass 76, X-Men TAS 76, The Ancient Magus' Bride 48, Mushoku Tensei 45, Jujutsu Kaisen 24). (2) `parse_series_detail` read `data.image` — the poster — and nothing else, so 0 of 178 shows had a backdrop, though the SAME `/series/{id}/extended` body already carries `data.artworks[]` (the array `parse_tvdb_artworks` reads for the picker). (3) The series pass asked TVDB only, freezing 44 shows as `none` — Death Note, Dragon Ball GT, Code Geass, Campfire Cooking in Another World — nearly all anime, which TMDB carries; a posterless show falls back to a representative episode, which falls back to a frame.|B125/V77. Still -> Primary in both parsers; series backdrop + logo taken from `artworks[]` by best score; TMDB tried when TVDB declines, with `Transient` kept distinct from `NoMatch` so a blip cannot freeze a show for the TTL. Episodes whose Primary is a local sidecar are untouched — the backfill still skips any role a local file covers.
B126|2026-07-26|PLAYBACK DEAD ON A LAPTOP — jellyfin-web stuck perpetually buffering on a black screen (user report), while the server answered `200` to every request and logged a complete encode for every segment. The client refetched `h264cmaf/init.mp4` EIGHT times in 0.5 s, then the same `.m4s` repeatedly, and reported `HLS Error: otherError / internalException`. Bytes pulled back from prod, same segment one query parameter apart, say why: segment 7 without subtitles starts at 42.105 with `def_dur 1001`, and WITH subtitles starts at 157.738 with `def_dur 3753` — the init declares `mdhd timescale = 24000` and that segment was muxed on 90000, so the player divided it by the wrong clock and placed a 42 s fragment at 157 s (157.738/42.105 = 3.75 = 90000/24000). hls.js will not join a fragment that far outside the range its playlist promised. Cause: the mp4 muxer inherits the track timescale from the encoder, and the two device routes set it differently ON PURPOSE — software carries `-enc_time_base 1:90000` (B118's exact-timestamp route) while hardware rejects that flag and is pinned with `-r 24000/1001` instead. Each is correct read on its own clock, but the scheduler load-balances ONE session's segments across every device and they all decode under the single init segment 0 produced (`seg 69/70/71 device cpu` beside `seg 64-68 device Nvenc:0`, same media, same session). Burning subtitles is what made it reliably visible: the burn pushed encode time past realtime (6.2 s per 6.0 s segment), NVENC saturated, and segments spilled to CPU mid-session — which is why Men In Black International played while Fringe S01E05 did not. Invisible by construction: `produced_frames == expected_frames` on every one of these segments, so the completeness check (V67) passed while the clock the frames were stamped on was wrong by a constant factor.|B126/V78. `-video_track_timescale` pins the timescale at the muxer; `HLS_GEN_VERSION` 10 -> 11 orphans the mixed cache.
B127|2026-07-26|TWO REAL SHOWS NEVER MATCH A PROVIDER, so they carry no poster, no backdrop and no episode stills (found auditing the metadata backfill after B125's TMDB fallback: of 178 shows, 166 gained an overview and the fallback rescued 31 that TVDB alone had frozen, but Code Geass and Forever (US) stayed `none` — Code Geass 154 episodes, 2 with a Primary, both self-generated video frames). Cause is the scorer, not the providers: `title_similarity` is pure length-normalized Levenshtein, so `Code Geass` against the canonical `Code Geass: Lelouch of the Rebellion` scores ~0.29 and `Forever (US)` against `Forever` scores ~0.70, both under the 0.7 `match_min_confidence` floor, even though the shorter title is an exact whole-token PREFIX of the longer — the appended subtitle / qualifier is charged as edit distance in proportion to the length gap. B125's fallback could not save these because the fallback re-runs the same scorer. Ten of the twelve `none` rows are NOT this bug — eight are `Season One`..`Season Eight` (season folders promoted to top-level series) and two are raw release filenames — a separate scan/parse defect left for its own fix.|B127/V79. `prefix_containment` scores an exact whole-token prefix `0.9 + 0.1·coverage`; `title_similarity` returns the max of it and the Levenshtein. `MIN_PREFIX_MATCH_CHARS` guards a trivial leading word from blanket-matching; the prefix must end on a token boundary; symmetric so it fires whichever side carries the tail. Pure pharos-core change, no cache/schema/migration. Cleared `metadata_refreshed_at` for the affected shows is unnecessary — the TTL re-admits them, and a manual clear can force the re-match sooner.
B128|2026-07-26|H264 CMAF PLAYBACK STALLS MID-STREAM IN THE BROWSER (Attack on Titan S01E01, Firefox, ~41 s): `HLS Error: otherError / internalException` and `FFmpegVideoDecoder … avcodec_send_packet: Invalid data found when processing input`. Source is HEVC → transcoded to h264 and served as a shared-init fMP4 (CMAF) rendition. Proven by pulling a live session's bytes and decoding each media segment under its init: seg0/seg1 decode CLEAN, but seg6/seg7 are corrupt (`number of reference frames (1+4) exceeds max (4)`, `deblocking_filter_idc 5 out of range`, `illegal reordering_of_pic_nums_idc`). Per-segment device logs: seg0-5,8 on `Nvenc:0`, seg6/seg7 on `cpu`. The init's `avcC` is NVENC's SPS; the CPU segments are libx264 slices encoded against libx264's SPS, and CMAF strips inband parameter sets, so the decoder parses libx264 slice headers with NVENC's SPS → garbage. The scheduler load-balances segments across devices BY DESIGN, and burning subtitles (this title has PGS) pushed NVENC past realtime so segments spilled to CPU — which is why it reproduced under load and not always. The slice-header misparse errors are SPS fields (POC type, frame_num bits, ref structure) that no `-profile`/`-level`/`-refs` pin can align, so unlike B126's timescale this cannot be forced identical across encoders — the rendition must be single-encoder. V67 frame-completeness and V78 timescale both passed on the corrupt segments (neither reads the parameter set).|B128/V80. `device_supports` makes hardware ineligible for `H264 + Container::Fmp4`, pinning the whole CMAF rendition (init + every segment) to libx264 — realtime at `-preset veryfast`, self-consistent, GPU freed. Scoped to Fmp4: the self-describing mpegts h264 variants keep hardware. HLS_GEN_VERSION 11 -> 12 orphans the mixed-encoder cache. H265 fMP4 carries the identical latent hazard but no hevc CMAF rung exists today.
B129|2026-07-26|THE SELECTED AUDIO TRACK PLAYS THE WRONG LANGUAGE (user: Aliens shows English auto-selected yet plays Ukrainian; Campfire Cooking S1E1 shows Japanese yet plays English). Not the V73 selector — it correctly picks English for Aliens (abs index 4, ahead of three Ukrainian tracks) and Japanese for Campfire, and reports it as `DefaultAudioStreamIndex` so the UI shows it. The pick never reached the render path: the demuxed audio rendition (`/vp9/audio.m3u8`) is a separate HLS session whose track comes from the `AudioStreamIndex` on its request, forwarded from the `TranscodingUrl`'s query string. `stream_selection` put an `AudioStreamIndex` on that URL ONLY when the CLIENT named one on the request, and on first play jellyfin-web names none (it trusts the server's default and appends the index only after a manual switch), so `resolve_audio_rel` saw `None` and ffmpeg default-selected the container's first/default-disposition track — Ukrainian for Aliens (track 1 is default), English for Campfire (English default). Structural: `default_audio_stream_index` was computed AFTER the `TranscodingUrl` was built, so the URL could never carry it. Confirmed from the deployed pod: the AoT/Aliens `TranscodingUrl` was `master.m3u8?PlaySessionId=…&renditions=…` with NO `AudioStreamIndex` while the MediaSource set `DefaultAudioStreamIndex`.|B129/V81. `ensure_audio_index_param` appends the server-selected absolute index to the transcode URL when it carries none (mirrors the `StartTimeTicks` append), forwarded through master → audio group → `resolve_audio_rel`. Idempotent with a manual switch (skipped once the URL names a track). No cache/schema change. Live verification: user plays Aliens and hears English; Campfire hears Japanese.
B130|2026-07-27|SKIP INTRO INVISIBLE IN EVERY BROWSER — a viewer reported that Skip Intro never appears while Skip Outro does. `/MediaSegments/{id}` was correct the whole time (verified body carries both an Intro and an Outro entry), and detection had 6113 intro rows across 481 seasons. The client never asked: jellyfin-web's `MediaSegmentManager.onPlayerPlaybackStart` reads `MediaSource.HasSegments` and returns BEFORE calling `getItemSegments` when it is false — and pharos hardcoded `has_segments: false`, having added the field only to satisfy the strict kotlin decoder (B13). So the entire media-segment surface was unreachable from every browser, for every item, always, with no error anywhere: the request that would have failed was never made. Found by the ABSENCE of a request in Loki — a browser played an episode whose intro is stored at 322.3-342.8s and issued no `/MediaSegments` request in the whole session — then confirmed by grepping the deployed `main.jellyfin.bundle.js` for the call site. Fix: answer `has_segments` from the same two sources `build_media_segments` serves (a detected row, or a chapter whose title classifies), so the advertisement cannot disagree with the response, and log the verdict. Guarded by V82.
B131|2026-07-27|INTRO DETECTION SEEDED NO SHIFTS — `align::compare` discovered candidate alignment shifts from an inverted index probed at `point ± InvertedIndexShift` (±2 as an INTEGER addend), while point acceptance is fuzzy to `max_bit_diff` (6 differing bits of 32). On a bit-packed chromaprint word those are different notions of similarity, so two episodes could share an entire opening — every point of it inside the 6-bit tolerance — and seed ZERO candidate shifts: the opening was never compared, not compared and rejected. Measured by replaying a real season's stored fingerprints: 9 of 10 intro pairs produced no shift, against 6 of 10 credits pairs matching at 91s on the SAME five files. Fix: bounded exhaustive search over every shift whose overlap could still hold a `min_duration` span (~2800 shifts x <=1400 points, milliseconds per pair); `SEGMENT_DETECT_VERSION` bumped so seasons re-run the cheap half over cached fingerprints, `SEGMENT_SCHEMA_VERSION` deliberately untouched. The synthetic suite had passed throughout because its filler was an arithmetic ramp, which DOES align under some shift — fixed by avalanching the filler, not by relaxing the assertion. Guarded by V83.
```
B132|2026-07-27|SERVER UNUSABLE FOR PLAYBACK DURING THE SEGMENT RE-DETECTION SWEEP — reported as "slowing the server down to the point of it being unusable for watching things" while the `SEGMENT_DETECT_VERSION` 2→3 sweep ran. Not an I/O problem: fingerprints were already cached, so the sweep was doing almost no disk work. `analyze_season` called `detect_season_verbose` twice DIRECTLY on the async runtime, after the `bg_io` permit covering fingerprinting had been dropped. That was tolerable while shift discovery was an indexed lookup; B131 replaced it with a bounded exhaustive search (~2800 shifts x <=1400 points per pair, every episode against every other), so each season pinned a tokio worker for a long stretch — starving the same executor that serves playback and HTTP — and, having no permit, ignored the regulator that parks background work while a client streams. The adjacent stage ducked correctly and the expensive one did not.|B132/V84. `detect_gated` holds a `bg_io` permit across both windows and runs them on `spawn_blocking`. Test `detection_stands_down_while_playback_holds_the_gate` drains the semaphore, asserts the task stays pending, then releases and asserts completion; verified to FAIL with the gate disarmed ("consensus ran while playback held every bg_io permit"). Mitigated live before the fix shipped by stamping the 1845 un-analysed episodes current so the sweep idled — reversible, no segments deleted.
B133|2026-07-28|A SHARED-INIT RENDITION'S URLs CARRIED NO IDENTITY FOR WHAT PRODUCED THEM, SO THE BROWSER CACHE OUTLIVED THE ENCODER CHANGE. `init.mp4` and every `.m4s` are served `Cache-Control: public, max-age=31536000, immutable` under a URL naming only the item and segment index. When spec 003 moved CMAF H264 from libx264 to NVENC, clients kept serving THEMSELVES the previously fetched libx264 init while fetching NVENC segments, whose SPS that init does not describe — issue #114, but resident in the browser rather than on the server. Diagnosis was delayed by exactly that: the fault survived a full server-side cache wipe (`HLS_GEN_VERSION` 12->13) AND a playback restart, which was read as evidence that NVENC's output was malformed, and the feature was reverted on that reading. An offline box-by-box diff of NVENC vs libx264 output built with pharos's own argv then showed the muxes are STRUCTURALLY IDENTICAL — same box tree, `mdhd timescale=90000`, `trun sample_count=143`, `tfhd`/`trun` flags, one `moof`; the only differences are the expected `avcC` (52 vs 61 bytes, the longer NVENC SPS) and a 514-tick (5.7 ms) `tfdt` offset, and both decode cleanly. Nothing was wrong with the hardware output. Fix: embed `HLS_GEN_VERSION` in the init + segment URIs of every shared-init (fMP4) rendition (`rendition_qs`), so a generation change is a URL change and the client fetches a matching init instead of reusing an incompatible one. Guarded by V90.
B134|2026-07-28|CACHE HITS WERE UNCOUNTABLE, AND ONE OF THE TWO HIT PATHS WAS ENTIRELY SILENT. Found while investigating a viewer's repeated playback failures on "Disclosure Day" (HEVC/E-AC3 → h264 CMAF, remote Firefox 153). The server record was rich on the miss path — 490 `hls segment transcoded (cache miss)` lines carrying device, queue wait, frame deficit — and empty on the hit path, so the 647 segment requests the client actually made could not be split into hits and misses at all. Two defects: the fast hit branch incremented `pharos_segment_cache_total{result="hit"}` and logged nothing, and the post-lock re-check branch (a request that queued on the single-flight lock and then found the file populated by the winner) returned the bytes having touched NO counter and NO log — invisible in both the hit rate and the miss rate, while having paid the full lock wait. This mattered beyond tidiness: with V89/V90 fresh (cached artefacts outliving the rule that produced them), the load-bearing question was whether that session had been served pre-deploy bytes, and nothing in the record could answer it — the investigation had to fall back on fetching live segments and parsing their boxes by hand. Not a regression; the hit path had never been instrumented.|B134/V91. `record_cache_hit` on BOTH branches: counter `pharos_segment_cache_total{result,class,hit_path}` (`hit_path` bounded to `fast`/`post_lock`, `none` on a miss so the ratio keeps its series), histogram `pharos_segment_cache_read_seconds` (a slow hit off a contended PVC stalls a client exactly like a slow encode, while every transcode metric stays clean), and an `hls segment served (cache hit)` line carrying the miss line's identity fields plus `age_secs` and `read_ms`. The existence check moved from `try_exists` to `metadata`, so the mtime behind `age_secs` costs no extra stat. The miss counter gained the same `class`/`hit_path` labels for symmetry. Test `a_cache_hit_is_counted_and_labelled` installs a thread-local `DebuggingRecorder`, serves a pre-populated segment whose SOURCE PATH DOES NOT EXIST (so it cannot pass by accidentally encoding), and asserts the counter and all three labels; verified to FAIL with the increment disarmed ("a cache hit must emit pharos_segment_cache_total — it is the signal").
B135|2026-07-28|RE-LANDING THE CMAF HARDWARE AFFINITY WITHOUT A GENERATION BUMP WOULD HAVE REPRODUCED #114 FROM DISK. Found while investigating whether #139 was safe to re-land now that B133/#142 embeds `HLS_GEN_VERSION` in shared-init rendition URIs. #139 touches device.rs, options.rs and scheduler.rs and NOT pharos-cache — it carries no generation bump, and did not need one when first written because the bump lived in its companion #140 (12->13). The revert then bumped 13->14 (correctly: reverting an assignment rule is itself an assignment-rule change, V89), and #142 kept 14 while making it `pub`. So a straight revert-of-the-revert resolves the constant back to 13 and re-lands the GPU rule under a generation that already names libx264 output: measured 5042 CPU-produced `codec=26` (H264+fMP4) segments cached in the preceding 36 h, every one of which would then be served beneath an NVENC-produced init. That is exactly B128/#114 — a shared-init CMAF rendition whose segments and init come from different encoders — reached through the cache rather than through the scheduler. The conflict git raised on the revert was the whole finding: it is the one line in the change that MUST NOT be taken from either side.|B135/V89 (third application). Resolved to `HLS_GEN_VERSION = 15` rather than to either side of the conflict. Safe where the first attempt was not, because #142 makes the bump visible to the CLIENT cache too — the first #139 changed the rule while browsers kept serving themselves a year-`immutable` init from the previous generation, which is what made its failure look like malformed NVENC output and cost a revert of working code. Evidence for the re-land itself: NVENC CMAF ran at 4.42x realtime (p50 1330 ms) against libx264 CMAF at 1.71x (p50 3463 ms) on the same rung, and the 215 NVENC CMAF segments produced while #139 was briefly live decode correctly under their own init (research R4: three independent NVENC encodes byte-identical `avcC`).
B136|2026-07-28|THE COLD-START PREWARM WARMED THE OPENING OF EVERY TITLE, INCLUDING ON RESUME — so on a resume it warmed three segments the client would never fetch and left the one gating playback cold. Not a partial miss but an INVERSION: the prewarm exists to keep a cold ffmpeg spawn (and the large NFS-backed source open) off the critical path of the first segment request, and on a resume it moved that spawn onto exactly the request it was meant to cover, while spending three background encodes on bytes nobody would ask for. `prewarm_cold_start` looped `0..COLD_START_PREWARM_SEGS` with no reference to the start position, though `resume_ticks` was already in scope at the call site and already rode the response as `start_position_ticks` (and, for native players, `StartTimeTicks` on the transcoding URL — B74). Measured live while investigating a viewer's repeated failures on "Disclosure Day": the session resumed at 91.9 min, playback began at segment 918, and the prewarm produced segments 0, 1 and 2. Episodic viewing is mostly resumes, so this was the common case rather than the edge one. Invisible because the prewarm logged only a debug line on FAILURE — a prewarm warming the wrong end of a film was indistinguishable from one warming the right end.|B136/V92. `prewarm_base_segment` resolves the resume position through `pharos_core::SegmentGrid` — the same grid the playlist enumerates and `serve_segment` keys on — so the warmed index cannot drift from the one the client requests; the prewarm then runs `base..base+COLD_START_PREWARM_SEGS`. A position at or past the end resolves to `None` and warms nothing rather than clamping to the last segment (clamping would warm bytes no client will request), and a missing duration also declines rather than falling back to 0, which would be actively wrong for a resume. An INFO line now records the decision (`resume_ticks`, `prewarm.base`, `prewarm.count`, `resumed`) so the choice is queryable. Test `prewarm_starts_at_the_resume_segment_not_at_zero` pins the measured case (5514 s -> segment 919) plus both sides of a segment boundary and the two decline paths; verified to FAIL with the old hardcoded 0 restored ("left: Some(0), right: Some(919)"). NOT fixed here: the prewarm still builds mpegts opts while browsers are served only h264cmaf, so browser prewarm remains unreachable regardless of position — tracked separately.

B137|2026-07-28|A GROUP WATCH FROZE ON A MEMBER THAT HAD ALREADY RECOVERED, AND PRESSING PLAY MADE THE HANG LONGER. Reported live: "the video stopped seemingly needing to buffer, however it was stuck until I seeked back". At 19:50:31 one member POSTed `/SyncPlay/Buffering`, so V19 froze the group and broadcast a bare `Pause`. It was not starved: every segment request in the window was a cache HIT served instantly (segments 123-130, including 124-130 at 19:50:36, five seconds AFTER the buffering report), i.e. the member already held ~42 s of buffer. It never sent `Ready`/`BufferingEnd` because it had nothing to send — the freeze paused its player, and jellyfin-web posts `Ready` only on a player TRANSITION, so a paused player awaiting a withheld `Play` can never signal recovery. This is the Buffy join-wedge (PR#64) one member over: that fix clears the SENDER's stale `buffering` flag, and here the stuck flag was on a non-sender, so `GroupMsg::Unpause` opened a readiness gate on precisely the member that could not ack (`pending=1` proves it — had the sender been the buffering one, the set would have emptied and playback would have started). The second press hit `if state.waiting.is_some() { return; }` and vanished. Worse, the press EXTENDED the hang: the B55 buffering anti-wedge armed at 19:50:31 would have force-resumed at 19:51:01, but opening a gate disarms that path (`if state.waiting.is_none()`) and starts a fresh 30 s `READY_TIMEOUT_MS` from 19:50:39 — healing moved to 19:51:09, ~37 s of dead air. A manual seek at 19:50:43 cleared the freeze wholesale and forced real transitions in all three clients (nine `Ready` posts), which is why seeking back was the only thing that worked. Second, latent: when the buffering anti-wedge does fire it force-clears every `buffering` flag but does NOT ack the gate those flags created, clearing the cause and leaving the effect.|B137/V93. FIXED in three parts, after the instrumentation shipped first. Diagnosis needed four Loki queries and hand-correlation of three streams because nothing was countable: `readiness gate opened pending=1 reason=Unpause` named neither the member held nor which of five situations opened the gate, a gate rescued by the anti-wedge logged the same line as one every member acked, the freeze broadcast a `Pause` indistinguishable from a user pause, and the dropped press left no trace at all. Shipped: `GateReason` (wire string kept as jellyfin protocol, `label()` split out so the five situations that all broadcast "Unpause" separate — `seek`, `buffering_unpause`, `queue_change`, `playlist_item`, `next_item`, `previous_item`, `restored`), persisted with the gate so a pod takeover keeps it; `pharos_syncplay_gate_total{reason,outcome}` + `pharos_syncplay_gate_wait_seconds` with every gate terminating in exactly one of `resolved`/`anti_wedge`/`superseded`/`replaced` (they sum to gates opened, so a dropped gate shows as drift not silence); `pharos_syncplay_buffering_freeze_total{outcome}` + `_seconds` with `recovered`/`superseded`/`anti_wedge`, where `superseded` counts freezes a human had to break out of; `pharos_syncplay_unpause_ignored_total{gate}` for the swallowed press. Gate-open, cancel and timeout lines now name the holdout member ids and whether a V19 freeze is in force. Tests assert the `anti_wedge` counter from a gate driven to its deadline, `superseded` from a freeze ended by a manual seek, and the dropped-unpause counter; each verified RED when disarmed, and both label sets asserted distinct (a collision between `anti_wedge` and `resolved` would sum a hanging group into the healthy bucket and silently disarm the alert). The named query for the next occurrence: `sum by (outcome) (pharos_syncplay_gate_total)` and `sum(pharos_syncplay_unpause_ignored_total{gate="buffering_unpause"})`. FIX: (1) an explicit `Unpause` supersedes the freeze outright, as `PauseShared` already does — the sender-clear's own reasoning ("the sender undergoes no transition, so a gate opened on it self-deadlocks") applies to every captured member, since the freeze pauses them all; V19 is preserved by RE-REPORT rather than by holding, and the test asserts the stalled member re-freezes the group immediately afterwards, so the fix cannot have traded a deadlock for a loss of buffer isolation. (2) `enter_waiting` caps a new gate's deadline at the earlier of `READY_TIMEOUT_MS` and any armed `buffering_since + BUFFERING_MAX_MS` — a gate may bring the group's release forward, never push it back. (3) the buffering anti-wedge resolves the `buffering_unpause` gate its own flags created instead of clearing the cause and leaving the effect (defence-in-depth: after (1) that pairing is reachable only via a snapshot restored across a deploy, and it carries no test because the state can no longer be built through the actor's message API — a fabricated fixture would assert itself rather than the behaviour). Two existing tests asserted the old gating and were moved onto the path where an Unpause still gates (a `BufferingStart` on an already-PAUSED group flags the member without engaging a freeze), so B57's BufferingEnd-resolves-promptly and the never-gate-the-author guarantee both keep coverage. (1) and (2) each verified RED when disarmed.
B138|2026-07-28|B137's FIX COVERED ONE OF THREE HANGS — the wedge class is any gate deadlocking on a member with no player transition to make, not just the freeze-plus-Unpause instance. Found by checking the previous night's film after the viewer said "we did get several": 7 freezes on 2026-07-27, most correctly self-recovering, but THREE 30 s anti-wedge hangs. (1) 22:10:34 — the B137 shape, and worse than the reported one: freeze at 22:10:34, press at 22:10:44 opens a gate, second press at 22:10:59 swallowed, the freeze anti-wedge clears the flags at 22:11:04 but cannot resume because a gate is open, and that gate then runs to its OWN deadline at 22:11:14. 40 s, two presses ignored. Confirms B137's fix (3) was a live 10 s contributor, not the theoretical guard it became after fix (1). (2) 22:12:14 — a SEEK gate on 3 members, two acked, `e08fb57b` never did; a press at 22:12:29 was discarded because `waiting.is_some()`; 30 s. Not a freeze at all, so B137's supersede does not apply. (3) 22:12:56 — an Unpause gate on one flagged member with NO freeze active (the 22:12:48 BufferingStart did not freeze, the group not being Playing), so `group_paused_due_to_buffering` was false and B137's supersede was skipped; 30 s. The common factor in all three is not the freeze — it is that a gate's only recovery is a 30 s timeout while the member it waits on may be structurally unable to answer, and a viewer pressing play during that window changes nothing.|B138/V93. Two changes, both evidence-led. Sizing first: 4 days of production gate open->resolve deltas show acks mostly under 3 s but tailing to 15.5 s and 18.1 s, so halving `READY_TIMEOUT_MS` globally — the obvious move — would truncate legitimate waits and start groups without members about to arrive; it was rejected on that measurement. Instead `GateReason::timeout_ms()` splits the bound by KIND of wait: `BufferingUnpause` gets `UNPAUSE_GATE_TIMEOUT_MS` (5 s, above the observed bulk) because a human is watching from t=0 and its holdouts are the members B137 showed cannot ack, while loading-client gates keep 30 s. Second, an Unpause landing on an open gate is no longer discarded: it acks the sender (which has just asserted readiness and will never signal any other way) and caps the REMAINING wait, one-directionally so the old spam-play-extends-the-hang bug cannot return and without replacing the gate. Covers (2) and (3); (1) was already covered. Three tests, each verified RED when disarmed — note the one-way test initially passed WHILE disarmed because it advanced a full grace window at the end, letting a re-armed deadline expire too; it now steps just past the first press's cap, the only interval that separates the two behaviours. A test that cannot fail is not evidence.
B139|2026-07-28|THE COLD-START PREWARM BUILT MPEGTS OPTIONS WHILE BROWSERS ARE SERVED ONLY CMAF, so browser prewarm was unreachable for every session regardless of position. A web client on h264 gets the demuxed all-fMP4 master (the `h264cmaf` rung, PR#70) and never requests an `/hls1/*.ts` segment; `prewarm_cold_start` called `build_segment_opts`, which hard-forces `SegmentContainer::Mpegts` + muxed audio for any video item because that is correct for the .ts route it serves. So each browser session spent three BACKGROUND encodes on segments no client would fetch AND left the cold ffmpeg spawn (plus the large NFS-backed source open) on the very first request the prewarm exists to absorb — both halves of the waste, on the commonest playback path. Identical in shape to B136 and it SURVIVED B136: that fix corrected WHERE the prewarm aimed (segment index on a resume) without touching WHAT it built (container), and the two failures compose — a resumed browser session warmed the wrong segments of the wrong rendition. Invisible for the same reason as B136: the prewarm's only signal is that segments get produced, which is equally true when they are unreachable.|B139/V92 (third application). `fmp4_segment_opts` split into `fmp4_segment_opts_resolved`, taking the bitrate cap and session burn set as parameters rather than reading them off an `HttpRequest`, so the prewarm — which already holds both from PlaybackInfo — derives the key through the SAME function the CMAF route uses instead of reconstructing it. The cap handling is the trap and had to differ: the mpegts arm post-clamps a computed bitrate while the CMAF path folds the cap through `effective_video_bitrate`, so reusing the post-clamp would have produced a valid-LOOKING fMP4 key that missed on every fetch. The `demuxed_cmaf` predicate at the call site mirrors `master_playlist`'s `demuxed_h264` exactly (web client, non-audio, first ranked rendition h264) and reads `renditions` — the same value `select_master_video` parses back off the URL that very response returns, so the prewarm and the master cannot disagree without the response contradicting itself. The chosen rendition is logged. Test `a_browser_prewarm_builds_the_key_the_cmaf_route_requests` pins container, audio-free delivery, the cap fold and the grid window, asserts the mpegts rung unchanged AND asserts the two containers DISTINCT so it cannot pass by the renditions collapsing; verified RED with the prewarm pointed back at `build_segment_opts`. Deliberately NOT asserted: equality of the prewarm and route keys — they now call one function with one set of inputs, so that comparison would be tautology rather than evidence.

B140|2026-07-29|ANDROID TV PLAYS THE FIRST 8 MiB OF A DIRECTPLAY TITLE AND THEN GIVES UP — reported as "the Android TV app cannot play Central Intelligence" while Batman played fine. Every server signal said the negotiation was healthy: `decision: direct_play`, `direct_play_block: not_blocked`, `downgrade: none`, and three 2xx `/videos/{id}/stream` responses (one 200, two 206). The client then made no further request for that item and the viewer moved on to another title. Cause: the 8 MiB `DIRECTPLAY_RANGE_CAP_BYTES` window added for the Deadpool report (a browser `<video>` stalling a reverse proxy on a multi-GB `bytes=X-` body) was applied to EVERY client. A browser MSE source buffer treats a short response as "this window is done" and issues the next `bytes=X-`, which is what makes the cap work; a native progressive player does not — ExoPlayer's `DefaultHttpDataSource` sizes its read from the response's `Content-Length` and returns `RESULT_END_OF_INPUT` to the extractor once that many bytes are consumed, so the film simply *ends* at 8 MiB. The delivery path was silent, which is why this looked like a codec problem: PlaybackInfo logged fourteen fields and the code choosing the actual bytes logged one `debug!` on one branch.|B140/V94. Instrumentation first, as its own commit: `pharos_directplay_delivery_total{delivery,client}` + a `directplay: delivery decision` line carrying the requested Range, the file size, the served window and whether that window stops short of EOF, recorded on every branch. Then the fix: `DirectPlayClient::caps_ranges()` restricts the cap to `Browser`, so a native player's open-ended range is answered with the whole tail. Guard test `native_open_ended_range_is_served_whole` was verified red by disarming the gate (it returned `bytes 0-8388607/8388708` instead of the full `bytes 0-8388707/8388708`).

B141|2026-07-29|MUSIC GENRES VIEW SPINS FOREVER — selecting any genre under Music showed a spinner that never resolved. jellyfin-web's music library calls `ApiClient.getMusicGenres` → `GET /MusicGenres`, an endpoint pharos never registered; the 404 rejected a promise with no `.catch` on it, so nothing ever cleared the loading state. Same class as the Edit Images spinner and the missing `DELETE /Videos/ActiveEncodings`: a jellyfin-web flow dies silently on a missing endpoint rather than reporting an error, so the symptom names the view and not the route. Serving `/Genres` under the alias would have been wrong — it lists every film and television genre, which is not what the tab means.|B141/V96. New `list_music_genres` handler: ids and names come from `genres_with_counts` (so a music genre's `Id` is byte-identical to the `/Genres` one and the `/Items?ParentId=` pivot resolves unchanged), while membership and counts are recomputed over `MediaKind::Audio` items, splitting `probe.genre` on the `split_genre_field` convention. `Type` is `MusicGenre`. Guarded by a test seeding a mixed library and asserting a video-only genre never appears and the count is audio items rather than every item carrying the genre.

B142|2026-07-29|MUSICBRAINZ MATCHED FEWER THAN HALF THE ALBUMS IT SHOULD HAVE — found by running the verification query on the first live pass of T108 rather than by a report: 25 `hit` against 22 `no_candidates`, 3 `no_cover_art`, 6 `unavailable`, 1 `no_album_tag`. The misses were not obscure music, they were ordinary albums whose tags carry pressing detail: `A Rush Of Blood To The Head (Japan)`, `Invaders Must Die [Bonus Tracks]`, `Prospekts March (CD EP UK & Europe)`, `Talk (Remixes) (CDr Promo) US`. A MusicBrainz release GROUP is the abstract album — those qualifiers belong to an individual release, so a title wearing one matches no release-group at all. Second cause: every `Ministry of Sound` compilation missed because the query narrowed on `artist:"Various Artists"`, and MusicBrainz credits a compilation to its actual contributors, so naming the placeholder EXCLUDES the release being searched for. Third, the 6 `unavailable` lines said only `error sending request for url (...)` — reqwest's `Display` names the request and stops, so none of them said whether it was DNS, TLS, a timeout or a refusal.|B142/V95. `search_attempts` builds an ordered ladder — exact tag, then the title with trailing bracketed qualifiers stripped, then title-only — walked ONLY while an attempt returns zero candidates, so a clean tag that matches still costs exactly one rate-limited request and the later rungs are paid for solely by albums that would otherwise have stayed blank. Compilation placeholders (`Various Artists`/`VA`/`Soundtrack`/...) drop the artist clause. Scoring uses the stripped title so removed qualifiers don't count against similarity. `error_chain` walks `source()` so the underlying cause reaches the log. Stripping refuses to eat a wholly-bracketed title or an interior bracket. Every stripping test uses a title that genuinely missed in production.

B143|2026-07-29|B142'S BETTER QUERIES WOULD HAVE DONE NOTHING FOR 30 DAYS. Caught by reasoning about the deploy rather than by a symptom: the album-art pass stamps a miss so the album's other eleven tracks don't each re-run a rate-limited search, and the TTL then holds that verdict for `refresh_ttl_days` (30). Correct for a stable query, wrong the moment the query improves — the 22 albums B142 fixes had ALREADY been stamped by the first live pass, so shipping the fix would have changed nothing visible until late August, and the natural conclusion would have been "the fix didn't work". A stamped verdict is only as good as the question that produced it.|B143/V97. `ALBUM_ART_QUERY_VERSION` (1 -> 2 for B142) is stamped into `match_external_id` as `miss-v{N}` beside every miss, and `audio_items_needing_art` re-admits any `none` row whose marker differs from the current one REGARDLESS of the TTL. An unversioned stamp (what the first release wrote) counts as stale, which is what reaches the existing rows. A miss reached by the CURRENT version is still held for the full TTL, so an album that genuinely has no artwork is not re-searched every pass — asserted by a test in each direction, plus a backend-conformance arm covering both engines.

B144|2026-07-29|A TAG CLIPPED MID-WORD MATCHES NOTHING, HOWEVER CLOSE IT IS. Seen in the second live pass after B142/B143 (hits 25 -> 65): `The Prodigy - Always Outnumbered Never Outgu` missed every rung. The album is `Always Outnumbered, Never Outgunned`; the tag was truncated by whatever wrote it. Every rung of the B142 ladder is a QUOTED Lucene phrase, which matches exactly, so a clipped title finds zero candidates and the confidence floor never gets a chance — `title_similarity` scores the pair at 0.88, comfortably above the 0.85 floor, if only the search returned it. Also confirmed NOT fixed by this and left honestly open: `Ministry of Sound: Anthems II: 1991-2009` still misses, because MusicBrainz indexes the album without its label prefix and the resulting similarity (~0.53) is correctly below the floor — accepting it would need a rule that guesses which leading words are a label, which is how a wrong cover gets shipped.|B144/V95. A final UNQUOTED rung, `releasegroup:(words...)`, appended to the ladder: Lucene scores partial matches where a phrase match cannot. Lucene operators (`-` = NOT, `:` = field, unbalanced brackets = parse error) are DROPPED rather than escaped, since there is no phrase to preserve; a title with nothing searchable left yields no rung at all rather than a query matching everything. Safe only because acceptance stays gated on `MIN_ALBUM_CONFIDENCE` — a loose query returns loose candidates and the similarity floor is what stops one becoming a wrong cover. `ALBUM_ART_QUERY_VERSION` 2 -> 3 so B143 re-admits every existing miss.

B145|2026-07-29|B144'S UNQUOTED RUNG WAS INERT FOR THE CASE IT WAS WRITTEN FOR, AND MUSICBRAINZ WAS THROTTLING US. Caught by verifying B144 in production instead of trusting its reasoning. `The Prodigy - Always Outnumbered Never Outgu` still recorded `no_candidates` with the ladder visibly ending in `releasegroup:(Always Outnumbered Never Outgu)`. Cause: MusicBrainz's search ANDs bare terms, so the unquoted rung STILL required the clipped token `Outgu` to match a real word — dropping the quotes fixes phrase-vs-token matching, which was never the problem. B144's premise (`title_similarity` scores 0.88, so the match is acceptable) was correct and its remedy did not deliver a candidate to score. Two more defects in the same evidence: `Coldplay - Prospekt's March` produced `releasegroup:(Prospekt s March)` because the tokeniser split on the apostrophe, ANDing against a stray `s`; and six `unavailable` lines read `release-group search returned 503`, i.e. MusicBrainz asking us to slow down — their limiter is a burst budget, and B142's ladder had quietly turned one request per album into up to four.|B145/V95. The unquoted rung wildcards its FINAL token (`Outgu*` matches `Outgunned`), gated on `MIN_WILDCARD_STEM` = 4 so a short token can't wildcard half the database into the similarity floor. Apostrophes stay inside a word. A 503 backs off — honouring `Retry-After` (delta-seconds only, clamped 1..60) or 5s — retries ONCE, and pushes the shared `RateGate` out via `delay()` so every other album waits too rather than walking into the same throttle; counted as `pharos_album_art_throttled_total`. `ALBUM_ART_QUERY_VERSION` 3 -> 4. LESSON, and the reason V97 exists: the first verification (25 -> 65 hits) proved the pass worked and was then used to assume the NEXT change worked too. It did not, and only a second query showed it.

B146|2026-07-29|SELECTING ANY ALBUM CRASHES THE GOOGLE TV APP, AND THE ALBUM GRID LABELS ITEMS `NameGuidPair`. Diagnosed from a live logcat trace over adb (the first crash's stack had already rotated out of the buffer; the user reproduced it while a filtered tail was running): `java.lang.NullPointerException: Attempt to invoke virtual method 'boolean org.jellyfin.sdk.model.api.UserItemDataDto.isFavorite()' on a null object reference at ItemListFragment.addButtons(ItemListFragment.java:451)`. The album detail screen dereferences `item.userData` unconditionally, and `SynthItemDto::folder` left `UserData` as `None`, so it was omitted from the wire for every synthesised item — album, artist, album-artist, genre, studio, tag, playlist. EXACTLY the crash class as B68, where a library folder missing its `UserData` killed the same client; the fix there was applied to `CollectionFolderDto` alone and the synth path was never revisited. Note what was NOT the cause: the album JSON is correct (`Name` is the album name, `AlbumArtists` a proper `[{Id,Name}]` array) — verified by dumping both `/Albums` and `/Items?IncludeItemTypes=MusicAlbum` before touching anything.|B146/V98. `UserData` is populated in `SynthItemDto::folder` — the ONE constructor every synth item passes through — rather than at the dozen call sites where the next one would be forgotten. Guard test asserts it across `/Albums`, `/Artists`, `/Artists/AlbumArtists`, `/MusicGenres` and `/Items?IncludeItemTypes=MusicAlbum`, checks `IsFavorite` is a boolean and `ItemId` names its item (a present-but-empty object would be the same crash one field further in), and was verified red by disarming the fix. Two golden files regenerated; the only delta is the added block. STILL OPEN: whether the `NameGuidPair` label is the same root cause (a failed bind falling back to a type name) or separate — re-check after deploy. Also observed in the same trace: a burst of `coil3.network.HttpException: HTTP 404` for album images, because `synth_album_dto` advertises a Primary tag unconditionally from a hash of the album id rather than from a cover that exists (B130 in reverse) — tracked separately.

B147|2026-07-29|ALBUM ART COULD ONLY EVER MATCH A PERFECT TAG — the reason B144 and B145 both appeared to do nothing. Two hours after B145 deployed the counters had not moved at all (192 `no_candidates`, 26 `below_confidence`, 0 `hit`), so this was not a short verification window: the pass had drained and matched nothing. Reproducing the EXACT request pharos makes proved the query was fine — `releasegroup:(Always Outnumbered Never Outgu*)` returns `Always Outnumbered Never Outgunned` at MusicBrainz score 100. The failure was in scoring. `pharos_core::match_best` multiplies title similarity by `year_factor`, which returns 0.85 when the year is unknown on EITHER side; this lookup never supplies a query year, so every candidate was multiplied by 0.85 and the effective bar became `title_similarity >= 1.0` — an exact normalised-title match. The 65 hits recorded earlier were exactly the albums whose tags were already perfect. The clipped-tag case B144 and B145 were each written to rescue scores 0.88 and was rejected by a multiplier neither of them touched. SECOND defect, which sent the diagnosis the wrong way for hours: the memo stored `Option<String>`, collapsing every failure into "no candidates", so an album's first track logged the true cause and its other eleven logged one that was never diagnosed — hence 192 `no_candidates` against 26 `below_confidence` when nearly all of them were the latter.|B147/V99. Scoring moves to `best_by_title`, title similarity alone, so `MIN_ALBUM_CONFIDENCE` means what its name says; the floor still rejects a genuinely different album (asserted with the real `Trance Classics` vs `Piano Classics (disc 1)` pair). The memo becomes `Result<String, AlbumArtMiss>` and replays the real reason. Guard test uses the verbatim live MusicBrainz candidates and ALSO asserts `match_best` still rejects them, so the reason for the indirection cannot be quietly lost. `ALBUM_ART_QUERY_VERSION` 4 -> 5.

B148|2026-07-29|AN ALBUM WHOSE ART EXISTS UNDER A SIBLING RELEASE-GROUP STILL RENDERED BLANK. Found in the SAME verification that proved B147 worked (hits 0 -> 103): `The Prodigy - Always Outnumbered Never Outgu` moved from `no_candidates` to `no_cover_art for dd5eafdd`, i.e. the search B144/B145 built finally located it — and then gave up. MusicBrainz routinely holds several release-groups for one album: the live response returned `Always Outnumbered Never Outgunned`, `Always Outnumbered, Never Outgunned` and `Always Outnumbered: Never Outgunned`, ALL at score 100, and the Cover Art Archive has art for only some of them. `title_similarity` normalises punctuation away so the variants TIE — there is no principled winner — yet the code committed to whichever sorted first and reported `no_cover_art` if that one happened to be bare. A coin-flip between a cover and a blank tile, on 31 albums.|B148/V95. `release_groups` returns every candidate clearing the floor, best first, and `album_art` walks them until one actually has a cover (capped at `COVER_ATTEMPTS` = 3, since each is a rate-limited request and past the first few the remaining candidates are ones the floor barely admitted). The memo is written from `album_art` AFTER a cover lands, so a re-run never re-picks a coverless sibling; a transport failure still leaves the memo untouched so an outage is not cached as a verdict. The guard test uses the three real tied candidates and asserts they SCORE IDENTICALLY — the absence of a principled winner is the reason the fallback has to exist, so asserting a winner would have hidden the point (the first version of the test did exactly that and was wrong). `ALBUM_ART_QUERY_VERSION` 5 -> 6.
B149|2026-07-29|ALBUM TILES TOLD EVERY CLIENT TO FETCH A COVER THAT DID NOT EXIST. Seen in the Google TV logcat trace captured for B146: a burst of `coil3.network.HttpException: HTTP 404` per screen of album tiles. `synth_album_dto` stamped `ImageTags.Primary` unconditionally from a hash of the album id, and `synth_primary_tag` (added for the album/artist image work) resolved a representative track without ever checking that track HAS art. `dto::image_tags_for` already states the correct rule for real items and documents exactly this hazard — a video representative can always serve a Primary because a frame is extracted on demand, but an AUDIO track's only Primary source is cached artwork — and the synth path simply never applied it. This is the mirror of B130 (art that could be served but was never advertised) and worse: the client pays for a request per tile and still shows a blank. TWO TESTS WERE ASSERTING THE BUG. `artist_children_are_albums_plus_loose_tracks` demanded a Primary tag from a seed with no artwork at all, and `a_music_album_advertises_the_primary_image_it_can_serve` set `has_primary_art: true` on a `MediaItem` and called `put` — but `has_primary_art` is denormalised and `set_artwork` is its ONLY writer, so `put` silently drops it and that test's track never had art either. Both passed only because the tag was unconditional.|B149/V95. `synth_primary_tag` applies the same audio rule as `image_tags_for`; `synth_album_dto` becomes async and resolves its tag through it instead of hashing an id. Both tests were rewritten to record art via `set_artwork` (the real writer) and now assert BOTH directions — an album with covers advertises, one without does not — stated side by side so neither can be relaxed alone. Two golden files regenerated; the only delta is the removal of the phantom tag.

B150|2026-07-29|OWL CITY'S SINGLE `Fireflies` WAS GIVEN THE ATTIC'S SLEEVE — the wrong-cover failure the confidence floor was supposed to make impossible, reported from production. Cause: the B142 ladder's last rung DROPS the artist (`releasegroup:"Fireflies"` with no artist clause), and B147 made scoring title-only. `releasegroup:"Fireflies"` returns 217 release-groups whose top five are ALL exact-title matches by different acts (Simon Falconer, Still Corners, Katie Garibaldi, Peter and the Wolf, Mike Levy — every one scoring 100), so every candidate scored a perfect 1.0 and the pick was arbitrary. The artist was never verified against anything. Two correct decisions combined into an incorrect one: relaxing the query was right, and scoring on title alone was right, but together they removed every constraint that said WHICH act's album this is.|B150/V100. `Candidate` now carries the `artist-credit` MusicBrainz returns, and `ranked_by_title` rejects any candidate whose credited artist contradicts the file's tag. Permissive where a mismatch is not evidence: an unknown artist on either side cannot contradict, containment accepts `Owl City` against `Owl City feat. Shawn Chrystopher` in both directions, and a compilation placeholder never narrows (MusicBrainz credits the contributors). SECOND HALF, without which the fix could not reach the reported bug: a wrong cover is INVISIBLE to `audio_items_needing_art`, which skips anything with `has_primary_art` — so Fireflies would have kept the wrong sleeve forever no matter how good the matcher got. New `clear_provider_artwork` drops one provider's artwork rows and re-opens those items; `invalidate_stale_album_art` runs it when the stored matcher version differs from `ALBUM_ART_QUERY_VERSION` (6 -> 7), the same wipe-and-regenerate idiom as `TRICKPLAY_GEN_VERSION`. Both directions tested, and the conformance suite covers the new statements against a real postgres.

B151|2026-07-29|THE ALBUM GRID SHOWED `NameGuidPair(name=<artist>, id=<id>)` WHERE THE ALBUM NAME BELONGS. Reported twice before it was placed, because the string looks like a pharos serialisation fault and is not one — no album is named that, no name in the library is blank, and every `Name` pharos emits comes from real tag data (all three checked directly against the production database before touching code). It is jellyfin-androidtv rendering, and the exact line is `BaseItemExtensions.getFullName` at the installed v0.19.9: a `MUSIC_ALBUM` card's title is `artists?.joinToString(", ") ?: albumArtists?.joinToString(", ") ?: albumArtist` followed by the name. `albumArtists` is a `List<NameGuidPair>`, so `joinToString` invokes Kotlin's data-class `toString()` on each element. `SynthItemDto` has no `artists` field AT ALL, so the first branch is null and the second wins.|B151/V98. `SynthItemDto` gains `Artists` as plain strings, set at every site that sets `AlbumArtists` — the two album builders and the `/Items/{id}` synth-album resolution. Guard asserts across `/Albums`, `/Items?IncludeItemTypes=MusicAlbum` and the artist-children rail that any album offering `AlbumArtists` also offers `Artists`, that they are strings, and that both name the same act. Two golden files regenerated; the only delta is the added `Artists` array. METHOD NOTE worth keeping: three rounds were lost guessing from the server side. What placed it was reading the CLIENT's source at the version actually installed — `gh api repos/jellyfin/jellyfin-androidtv/contents/<path>?ref=v0.19.9` — after the user quoted the literal rendered text. A rendered string that looks like a struct dump is a client formatting bug; go to the client's source, not the server's.

B152|2026-07-29|A BLANK ARTIST TAG SHADOWED A GOOD ONE, SWITCHING B150'S ARTIST VERIFICATION OFF FOR THE VERY TRACK THAT MOTIVATED IT. Found by checking the reported item's row after B150 deployed, rather than assuming the fix reached it: Owl City's `Fireflies` carries `album_artist = ''` (present but empty) alongside `artist = 'Owl City'`, and the selection was `album_artist.as_deref().or(artist.as_deref())` — `Option::or` sees `Some("")` and keeps it. Downstream an empty artist reads as "artist unknown", which `artist_is_consistent` deliberately treats as "cannot contradict"; so the track would have gone straight back to matching any of the 217 release-groups called `Fireflies`. The 740-item invalidation had correctly cleared its wrong cover, so it WOULD have been re-matched — wrongly, and looking like B150 had failed.|B152/V100. Tag selection filters blank/whitespace values before the fallback, so a present-but-empty `album_artist` falls through to `artist`. Guard uses a spy resolver to assert the artist the lookup was actually GIVEN, rather than inferring it from a match outcome — the rule under test is tag selection, not matching. `ALBUM_ART_QUERY_VERSION` 7 -> 8 so the affected rows are re-admitted again. NOTE on why the album still resolves: `Fireflies - Single of the Week` vs MusicBrainz's `Fireflies` scores 0.93 via `prefix_containment`, comfortably over the floor, once the query is narrowed to Owl City.

B153|2026-07-29|ASKING FOR A TYPE PHAROS STORES NONE OF RETURNED THE ENTIRE LIBRARY — 51.6 MB of JSON, on every album page open. Reported with the exact request: jellyfin-web's album view asks for the album's music videos, `/Users/{u}/Items?IncludeItemTypes=MusicVideo&Recursive=true&AlbumIds=…`. `MediaKind::from_wire` has no MusicVideo mapping, and every one of the three type-filter sites read "zero recognised kinds" as "no kind filter" (`if !wanted.is_empty() { mq.kinds = wanted }`), so the honest answer — an empty list — became the whole library serialised. "I asked for X" must never widen to "I asked for anything".|B153/V101. `parse_kind_filter` returns a three-way `KindFilter`: `Unset` (no param), `Kinds` (at least one stored kind named), `MatchesNothing` (types named, none of them storable OR synthesised). The nothing case sets the existing empty-but-present `Ids=` shape so the SQL emits `1 = 0`. TWO TRAPS, both caught by tests rather than reasoning: (1) `build_media_query` assigns `mq.filters = f` wholesale further down, so setting the flag at the parse site was silently discarded and the first fix changed nothing — it must be applied AFTER that assignment; (2) a blanket "unknown type means nothing" broke `IncludeItemTypes=Series`, which is answerable by folding Series tiles out of stored episode rows on this very path, so `SYNTHESISED_ITEM_TYPES` exempts the types pharos answers without storing. Guard asserts all three directions: unstored type is empty, synthesised type is still answered, stored type still filters.

B154|2026-07-29|24 ALBUMS HELD A COVER AND STILL RENDERED AS BLANK TILES. `build_synth_image_map` picked an album's (and an artist's) representative track by lowest TITLE, and `synth_primary_tag` — correctly, per B149 — refuses to advertise a Primary for an audio row that has no cached artwork. But the MusicBrainz backfill records the cover against exactly ONE track of the album, and which track that is has nothing to do with title order: alphabetically-first is rarely the matched one. So the tile asked a coverless representative whether it had art, was told no, and emitted no `ImageTags.Primary` while the album's cover sat in the cache. Proved by query against the live DB rather than inferred: 126 albums, 105 with a Primary on some track, only 83 whose lowest-title track held it — a 24-album gap naming `Toxicity (UK CDS2)`, `Hypnotize`, `Morning View`, `Prospekts March (CD EP UK & Europe)` and 20 more. Invisible because both the "no art anywhere" and the "art on another track" cases produce the identical absent-tag wire shape.|B154/V102. The album/artist representative key is now prefixed by whether the track has art (`'0'` with, `'1'` without) before the title — the same `has_primary_art`-first ranking the library tiles in the same function already used — so a group containing any covered track always resolves to a covered one, with title still breaking ties for stability across restarts. Test `album_representative_prefers_the_track_that_has_a_cover` puts the cover on the alphabetically-LAST track and asserts the tag names it; disarmed by dropping the rank prefix, it fails on the absent tag.

B155|2026-07-29|COVERS APPEARED ONLY AFTER A DEPLOY, NEVER WHILE THE SERVER RAN. The synth-image memo (`AppState::synth_image_ids`) caches a synth id → representative item id, INCLUDING negatives, for the whole life of the pod — it exists so a grid of tiles costs one full-library scan instead of one per tile. The metadata backfill, though, keeps downloading album art for hours after boot, and B154 makes the correct representative a function of which tracks have art. So the first render of an album tile froze that album's answer against the artwork that existed at that instant: an album whose cover landed a minute later kept resolving to the coverless track it was warmed with, and one warmed before any of its tracks were even matched kept its negative. The only thing that ever cleared it was a restart, which is why new covers seemed to arrive in a batch on each deploy rather than as the backfill found them.|B155/V103. `AppState` gained an `artwork_epoch` counter, bumped by the backfill whenever a pass enriched anything, and `synth_image_cached` drops the entire memo when the epoch has moved since the map was built — so the next request re-warms against current artwork. Bumping on `enriched > 0` rather than per-artwork-write keeps it to at most one invalidation per pass. Test `artwork_landing_after_the_memo_warmed_re_picks_the_representative` warms the memo on a coverless album, then records a cover and asserts the tag appears; disarmed by removing the `clear()`, it fails.

B156|2026-07-29|OPENING A GENRE ANSWERED 400 `invalid id`. `/Genres` and `/MusicGenres` hand out `genres.wire_id` as the tile id, and every client fetches an item before listing its children — but `fetch_item_dto`'s synth chain resolved library folders, Series/Season, BoxSets, Playlists and MusicAlbum/MusicArtist and then fell through to `parse_item_id`, which rejects a 32-hex id. So the one id shape the genre endpoints themselves emit was the one the detail fetch refused. Found by sweeping three hours of access logs for non-2xx responses rather than from a report: `GET /Users/{u}/Items/689afe218badc80f689afe218badc80f 400`, twice, from Firefox 152 — the id hashes to the genre `Acid Punk`. jellyfin-web does not `.catch` this fetch, so the page simply stopped (the same silent-dead-end shape as B130's missing route).|B156/V104. `synth_genre` resolves a genre wire id against `genres_with_counts()` (no full-library scan on the miss path) and returns the same folder DTO the list builders emit, with `ChildCount` from the row. The `Type` follows MEMBERSHIP rather than the endpoint the client came from — a genre carried only by audio types as `MusicGenre`, anything else as `Genre` — because jellyfin-web picks its detail renderer from `Type` and a music genre rendered as a video Genre lists nothing. Test `opening_a_music_genre_resolves_instead_of_400` takes the id from the `/MusicGenres` listing rather than re-deriving the hash (so a hash change cannot make the test pass while the client breaks); disarmed, it reproduces the live 400 exactly.

B157|2026-07-29|THE ALBUM TILE CARRIED A SLEEVE AND THE PAGE BEHIND IT DID NOT. The grid builder (`synth_album_dto`) resolves `ImageTags.Primary` through `synth_primary_tag`, but the DETAIL fetch (`synth_album_or_artist`, and the artist arm beside it) built its DTO with `image_tags: Some(Default::default())` — an EMPTY map, which serialises as `"ImageTags": {}` and reads to every client as no image at all, exactly the trap `stamp_synth_image_tags` documents for the list path. The detail page has no `stamp_synth_image_tags` call to rescue it. So clicking a covered album tile landed on a coverless header, and the inconsistency between the two surfaces made the art look intermittent rather than absent from one code path.|B157/V102. Both arms now call `synth_primary_tag` — the same lookup the grid uses and the same one that serves the bytes — so the header advertises a Primary exactly when the tile does, and neither advertises one that would 404 (B149). Test `album_and_artist_detail_advertise_the_cover_the_grid_shows` puts the cover on a track and asserts both the album and the artist detail DTO name it; it depends on B154 (without it the representative is the coverless track and there is nothing to advertise from either surface).

B158|2026-07-29|COVER DOWNLOADS TIMED OUT, SO ALBUMS THAT HAVE ART STAYED BLANK. `front_cover_url` asked the Cover Art Archive for `/release-group/{mbid}/front`, which redirects to the archive.org ORIGINAL — a full-resolution sleeve scan, routinely several MB and sometimes tens — and the reqwest client applied a single 20-second WHOLE-request timeout to it. So every cover was pulled at full resolution across a home connection to render a 280px tile, and the slow ones died mid-body. Found by reading the decision log during the B152 re-match rather than from a symptom: `unavailable` was the single largest miss reason (27 consecutive failures on `The Definitive Monkees` alone), and because the reason carried its cause the diagnosis was one line — `provider unavailable: reading cover art body: error decoding response body: request or response body error: operation timed out`. A bare `unavailable` would have sent this back to guessing, which is the whole argument for §"Expose the cause".|B158/V105. The URL now asks for `front-500`, the Cover Art Archive's derived 500px thumbnail — one to two orders of magnitude smaller and still larger than the biggest surface that renders a cover (the detail header). The client's single timeout is also split: `connect_timeout` 10s so a dead host still fails fast, whole-request 45s so a live transfer has room, because one combined budget made an unreachable server and a slow download the same event. No `ALBUM_ART_QUERY_VERSION` bump is needed — `Unavailable` is already excluded from the miss marker (a transport failure is not the item's fault), so the affected rows stayed eligible and retry on the next pass. Test `front_cover_url_asks_for_a_derived_thumbnail_not_the_original` asserts the size suffix explicitly, because dropping it reintroduces the timeouts silently — the bare URL still resolves.

B159|2026-07-29|THE MISS REASON NAMED THE WRONG CAUSE, AND CONTRADICTED ITSELF DOING IT. `ranked_by_title` rejects a release-group on TWO independent grounds — the credited artist is inconsistent, or the title is below `MIN_ALBUM_CONFIDENCE` — but the miss arm reported both as a confidence shortfall, quoting `top_by_title`'s score, which is computed over ALL candidates and ignores the artist filter entirely. So an album rejected because the release-group belongs to a different artist was logged as `best release-group \`Experience\` scored 0.95, below 0.85`: a sentence that refutes itself, since 0.95 is not below 0.85. Found while auditing the 23 albums still blank after B152/B154-B158, where it accounted for 14 of the decision lines. This is a worse failure than a bare reason: a bare `below_confidence` says "I don't know why"; this one confidently sends the reader to lower a threshold that was never consulted, and lowering it would have admitted wrong covers (the exact B150 regression) without fixing a single one of these albums.|B159/V106. The classification moved out of the async fetch into a pure `classify_miss(scored_title, artist, candidates, query)`, which reports `ArtistMismatch { best, want, have }` when the top title CLEARED the floor (so only the artist can have rejected it) and `BelowConfidence` only when the title genuinely fell short. The new variant carries both the wanted and the credited artist, so the next question — is `artist_is_consistent` too strict? — is answerable from the log instead of by experiment. Test `a_wrong_artist_is_reported_as_such_not_as_a_low_score` drives `classify_miss` itself rather than formatting a hand-built variant (the first version of this test asserted only `Display` and passed with the routing disarmed, which is precisely the mistake this bug is about); disarmed, it now reports `below_confidence` and fails.

B160|2026-07-29|EVERY LADDER IMPROVEMENT WIPED THE WHOLE LIBRARY'S COVERS. `invalidate_stale_album_art` keyed its `clear_provider_artwork("musicbrainz")` on `ALBUM_ART_QUERY_VERSION` — the version bumped whenever the SEARCH changes — so a fix that could only ever turn a miss into a hit still deleted every cover already handed out and re-downloaded them at MusicBrainz's one request per second. Measured on the live library this morning: B152 (a blank artist tag no longer shadows a good one) bumped v7→v8 and took coverage from 1243 covered tracks to 727, with roughly an hour of blank tiles before it climbed back past where it started. Nothing about B152 could make an existing match WRONG, so the entire cost was waste — and it was invisible as a bug because the recovery looked like the fix working. Re-admitting misses never needed the clear at all: the miss marker already carries the query version and `audio_items_needing_art` re-admits any row stamped by an older one, touching nothing that has a cover.|B160/V107. Split into two versions with different costs. `ALBUM_ART_QUERY_VERSION` re-admits MISSES and clears nothing (the marker already does the work). The new `ALBUM_MATCH_VERSION` is the one whose change drops cached covers, and is bumped only when a past match may be WRONG — B150's artist check is the example, written because Owl City's `Fireflies` had been given The Attic's sleeve and those covers had to go. An ABSENT record is also no longer treated as a stale one: a fresh install (and the first boot after this split) adopts the version silently rather than reading "never recorded" as "changed" and wiping the library. Tests `a_query_bump_re_admits_misses_without_dropping_covers` and `a_first_boot_adopts_the_match_version_without_clearing`; both were disarmed and confirmed red, the second catching exactly the first-boot wipe this change risks.

B161|2026-07-29|THE BANDWIDTH TEST SENT ZEROS, WHICH WOULD HAVE MADE EVERY CLIENT OVERESTIMATE ITS LINK THE MOMENT ANYTHING GZIPPED. `/Playback/BitrateTest` answered `vec![0u8; n]`. jellyfin-web's automatic quality detection divides the byte count it ASKED for by the wall time to get a bandwidth figure and picks a bitrate from it, so the measurement is only meaningful if that many bytes actually crossed the wire. Zeros compress to essentially nothing: with gzip anywhere on the path, a 3 MB probe goes out as a few KB, the client concludes the link is roughly a thousand times faster than it is, and selects a bitrate the connection cannot sustain — B81's rebuffering reached from the opposite direction. Latent rather than live: nothing on this deployment compressed, which is exactly why it had to be fixed BEFORE B162 turned compression on. Noticed from a user's question about why an album page pulls 1 MB + 3 MB from this endpoint (it is jellyfin-web's normal escalating probe, and not itself a fault).|B161/V108. Two independent guards, so neither one quietly becoming untrue re-opens it: the body is now xorshift64* noise, which no compressor can shrink, and the response carries `Content-Encoding: identity` so actix, angie and any future CDN leave it alone. No new dependency — the generator is eight lines and deterministic. Test `the_bitrate_payload_does_not_compress` asserts the property structurally (distinct byte values and longest run) rather than by invoking a compressor, so it needs no dev-dependency; disarmed back to `vec![0u8; n]` it fails on both counts. `a_short_bitrate_payload_is_still_exact` pins the length for sizes either side of the 8-byte generator stride, because a body off by one byte skews the very figure this endpoint exists to produce.

B162|2026-07-29|NOTHING ON THE HOST WAS COMPRESSED, SO EVERY PAGE LOAD MOVED MEGABYTES IN FULL. The angie pod that fronts jellyfin-web (and reverse-proxies every REST call to pharos) had no `gzip` directive, and pharos has no compression middleware either — so the bundle's 1 MB stylesheet went over the wire as 1 MB, and pharos's list endpoints answered with uncompressed JSON. Reported as "this CSS is 1MB" while asking about an album page. Measured locally against the real bundle and the live server through the same config: the 1 MB stylesheet drops to 349 KB (it embeds base64 assets, so 2.9x is the honest ceiling), and a 500-track `/Items` list drops from 1,059,697 bytes to 83,114 — 12.7x, on exactly the request the library grid makes.|B162/V109. `gzip on` in the shared `mkAngieUi` config, with `gzip_types` as an ALLOW-list of text-shaped types. Deliberately at the proxy rather than as actix `Compress` middleware: angie already fronts every pharos response, whereas an actix compressor would also sit on the video/segment/image paths, where switching a response to chunked transfer breaks the `Content-Length` and `Range` handling those paths depend on — B101 was a size-0 DirectPlay HEAD and B140 a range served whole, both in that machinery. The allow-list also keeps `application/octet-stream` uncompressed, which is what makes the bandwidth probe's measurement survive (B161 hardens the same boundary from the other side). Verified end to end before shipping, not asserted: through the new config the 3 MB probe still reports `Content-Length: 3000000` with no `Content-Encoding`, while the JSON list compresses. NOTE: the deployment pins `pharos-jellyfin-web` to a full commit SHA with `pullPolicy: IfNotPresent`, so this image does NOT roll out until that pin is bumped in the GitOps repo — unlike the server image, which Flux advances automatically from its sortable tag.

B163|2026-07-29|A 213-PIXEL THUMBNAIL REQUEST RETURNED THE FULL 1000x1500 POSTER. `ImageCache::scaled_artwork` skipped scaling for any source under a 256 KiB `SCALE_THRESHOLD_BYTES`, reasoning that a small file already reads fast. But the question a thumbnail request asks is not "is this FILE big?", it is "is this IMAGE bigger than the box it goes in?" — and a 1000x1500 poster compresses to about 253 KiB, slipping under the threshold. `GET /Items/{id}/Images/Primary?fillWidth=213&fillHeight=213` returned 259,639 bytes, byte-identical to the same request with no size parameters at all, and the client decoded a 1.5-megapixel bitmap per tile. Found by a Playwright walk that records every response size across the main views (`payload-audit.spec.ts`), which is also how B153 and B162 would have been caught without anyone noticing a slow page first. On the album grid the caps meant roughly 26 MB where about 2 MB was needed.|B163/V110. The decision now reads the source's INTRINSIC WIDTH from its header — PNG IHDR, WebP VP8/VP8L/VP8X, or the JPEG SOF marker chain — and scales whenever the image is meaningfully wider than the target, whatever it weighs. A 20% band above the target still declines, so a 480-px cap does not re-encode a 500-px sidecar, and scaling UP is never attempted. The parse is a short read of the header, no decode, so asking the correct question per request is cheaper than the byte check it replaces; `SCALE_THRESHOLD_BYTES` survives only as the fallback for a format whose header cannot be read, where guessing wrong risks upscaling. `width_from_header` is split out and tested on byte literals, including the trap that makes a naive scan wrong: DHT (0xC4) sits inside the 0xC0..=0xCF marker range but carries no dimensions, so reading it as a frame header yields a garbage width.

B164|2026-07-29|THE RESUME ROW IGNORED `Limit` AND RETURNED THE WHOLE BACKLOG. `resume_items` read `MediaTypes` off the query string but never `Limit` or `StartIndex`, so it answered with every resumable item however few the client asked for. jellyfin-web's "Continue Watching" home row requests `Limit=12`; on this library it was served 86 items — 401,001 bytes of JSON to render twelve tiles, the single largest API response on the home page. Found by the Playwright payload walk (`payload-audit.spec.ts`) rather than by anyone noticing, because the row LOOKS right: the client simply renders the first twelve and discards the rest, so the only symptom is bytes.|B164/V111. `Limit` and `StartIndex` are honoured, and `TotalRecordCount` remains the UNPAGED total — which is what Jellyfin means by it and what a client would page against; making it the page length would have made the row un-pageable. An absent `Limit` still returns everything, so callers that relied on the old behaviour (the Android TV `/UserItems/Resume` alias sends no limit) are unaffected. Test `resume_row_honours_the_limit_the_client_asked_for` seeds 30 resumable items and asserts both the page size and the untouched total; disarmed, it returns all 30.

B165|2026-07-29|AN HTTPS-ONLY DEPLOYMENT ADVERTISED ITSELF OVER PLAIN HTTP. `/System/Info/Public` and `/System/Info` returned `LocalAddress: http://prototype.aliflix.alison-jenkins.com` — the right host, the wrong scheme — and `derive_local_address` is the URL clients are told to use when publishing a reachable address to peers (casting, SyncPlay, native clients). Anything that followed it dropped to plaintext, or was blocked outright as mixed content on an HTTPS page. pharos was NOT at fault: it reads the scheme through actix's `ConnectionInfo`, which honours `X-Forwarded-Proto`. The angie config in front set `proxy_set_header X-Forwarded-Proto $scheme`, and since TLS terminates at the cluster gateway and angie's own listener is plaintext, `$scheme` is `http` — so the proxy overwrote the gateway's truthful `https` with its own. Isolated by sending the header to each hop in turn: straight to pharos with `X-Forwarded-Proto: https` returned `https://`, the same request through angie returned `http://`. The same clobber fed every other `connection_info().scheme()` caller — `dlna::base_url_from_request` and the remote-client check's sibling helpers.|B165/V112. angie now honours an inbound `X-Forwarded-Proto` via a `map` and falls back to `$scheme` only when the header is absent, so a deployment with nothing in front still describes itself correctly. Fixed in the shared `mkAngieUi`, so the pharos-ui pod gets it too. Verified against the live server through both configs before shipping: new config + `X-Forwarded-Proto: https` gives `https://`, header absent gives `http://`, old config with the header gives `http://`. Test `the_advertised_address_follows_the_clients_scheme` pins the SERVER half of the contract in-tree, because the proxy half lives in `flake.nix` and has no test harness — the half that can regress silently is the half a repo test can hold.

B166|2026-07-29|EVERY IMAGE HEAD ADVERTISED `Content-Length: 0`. `deliver_image` ended its HEAD path with `.finish()`, an empty body. actix's h1 encoder derives a HEAD response's `Content-Length` from the body's declared `BodySize` and DROPS any manually-inserted header, so `BodySize::Sized(0)` put a zero on the wire for every poster, thumb and backdrop. Spotted while inspecting an unrelated image request: `content-length: 0` sat next to `etag: "266a6-18c5d36c7e4e9ef1"`, whose own prefix is 157,350 in hex — the response was declaring nothing while its ETag described a 154 KiB file. This is B101 exactly, on the image path rather than the video one; there a zero-length HEAD collapsed Firefox's `<video>` seekability probe to `buffered`, so the class already has a demonstrated cost for any client that sizes a fetch from HEAD.|B166/V113. The HEAD path now returns a `SizedStream` declaring the file's real length. Its stream is never polled for a HEAD, so no bytes are read off disk — the response stays as cheap as `.finish()` was while telling the truth. The test asserts the DECLARED `BodySize` rather than the header, because the header is stamped by the h1 encoder at wire time and the in-process test harness never materialises it — asserting the header would have produced a test that passes on a bug (it reads `None` either way). Disarmed to `.finish()` it reports `Sized(0)`.

B167|2026-07-29|A DEPLOY EVICTED THE TWO PEOPLE WHO WERE QUIETLY WATCHING AND SPARED THE ONE PRESSING BUTTONS. A rolling update at 19:21:43 drained the pod, which closed all three `/socket`s in a live watch party ("draining — closing /socket so the client reconnects to the new pod"). The new pod hydrated the group at 19:22:13 with every member `connected=false`, `last_seen` stamped now. No client came back quickly: there was not one `/socket` request in the log — not even a failed one — until 19:33:48, and the other two returned at 19:43:34 and 19:43:46. Reconnects took 12, 22 and 22 MINUTES against a `DISCONNECTED_TTL_MS` of 600 s, so at 19:32:13 — exactly 600 s after hydration, to the second — the ghost prune removed alison and lace mid-film. The third member survived only because that person kept pressing Ready, and every attributed command refreshes `last_seen`: liveness was inferred from ACTIVITY, and watching quietly produces none. The eviction was also silent (B168), so the two clients kept playing against a group that had moved on, which is what the room experienced as "some of us got out of sync". The grace had already failed this way once — T83 chose 150 s, a member was evicted 12 s before its client reconnected, and the answer was to raise it to 600 s.|B167/V114. The verdict moved off the member and onto the GROUP. A disconnected member is no longer pruned individually at any age: it holds no readiness gate (`follower_ids` filters on `connected`), so retaining it costs a roster line while evicting it costs someone their evening — and no duration can separate "closed the tab" from "reconnecting", because the only evidence either produces is silence. What is still reaped is a group where NOBODY holds a socket and nothing has been heard from anyone for `GROUP_ABANDONED_MS`, which dissolves WHOLE; B29's guarantee that orphaned snapshots do not haunt the join picker is preserved through it. The dead-socket prune stays, narrowed to its actual justification — a member we still believe holds a socket but which has gone silent does block every gate to the anti-wedge timeout. Instrumentation shipped first and separately: `pharos_syncplay_socket_gone_seconds{cause}` measures the reconnect gap that both graces were guessed at without ever recording, `pharos_syncplay_members_awaiting_reconnect` makes retention a number rather than an absence of log lines, and the prune log now carries `silent_ms` and the `ttl_ms` it was judged against. The old test `a_disconnected_member_that_never_returns_is_still_pruned` asserted the behaviour that broke the party and is replaced by its inverse; disarmed to the old rule it fails with `left: ["stayed"]` — the button-presser surviving alone, which is the incident.

B168|2026-07-29|THE EVICTION WAS NEVER MENTIONED TO THE EVICTED. After B167's prune removed two members, their clients reconnected at 19:43:34 and 19:43:46 and the server logged "syncplay: /socket connected + registered in hub" and said nothing else. jellyfin-web keeps its own `groupInfo` and stays in SyncPlay mode until told otherwise, so both players carried on free-running against a group that had moved on. The first either user learned of it was eleven minutes after the eviction, when one pressed pause and the command path answered `NotInGroup` (B24, doing its job — but only for someone who happens to press something). Rich-on-success, silent-on-failure: the reconnect path that FINDS a membership sends a full `Joined` + catch-up, and the one that does not send nothing at all.|B168/V115. `ResyncMember` for a member the roster no longer holds now sends `ServerMsg::GroupLeft`, which stock jellyfin-web routes to `disableSyncPlay` — the client exits SyncPlay cleanly and can re-join, instead of pretending. Deliberately narrow: the obvious version, answering EVERY group-less socket connect with `NotInGroup`, is wrong, because `Manager.processGroupUpdate` maps both `NotInGroup` and `GroupLeft` to `disableSyncPlay(true)` and that toasts "SyncPlay disabled" — an unconditional send would fire on every page load for every user. Verified by reading the deployed bundle rather than assumed. This fires only where the server holds the contradiction: an attachment to a group that does not list the member. Counted by `pharos_syncplay_stale_membership_healed_total` so a recurrence is a rate, not a report from the sofa.

B169|2026-07-29|EVERY FILM WAS DATED BY WHEN IT WAS COPIED, NOT WHEN IT CAME OUT. "Lara Croft - Tomb Raider - The Cradle of Life" (2003) displayed as 2026, `PremiereDate` 2026-07-29 — the day it was looked at. 139 films in the live library carry a 2025 or 2026 year, among them "2001: A Space Odyssey" as 2025-07-07 and "Apocalypse Now" as 2025-05-06. The signature that settles it: "300" and "300 - Rise of an Empire" are both dated 2026-07-19, "Avatar" 2026-07-20, "47 Ronin" 2026-07-21 — consecutive days, which is an acquisition timeline rendered as release dates. Both probe backends read the container's `creation_time` tag as a release date; it is the time the FILE was muxed. The libav backend (the deployed default) aliased it into `date`, so one tag poisoned `production_year` and `premiere_date` together, and since the `embedded` provider outranks `filename` (30 vs 10) it also beat the `(2003)` sitting in the filename. The online match was never at fault — the item carries the right TMDB id, overview and rating (5.762, exactly what the UI showed) — but `apply_enrichment` fills only empty fields on the "local always wins" rule, so a mux date already in the row locked the real date out permanently. Reported as "films keep getting the wrong year", with a screenshot showing 2026 beside a correct 5.8 rating: the pairing is the diagnosis, since a wrong match would have got both wrong.|B169/V116. `creation_time` is removed from the date sources in BOTH backends, reversing the fallback B90 added; a genuine `DATE`/`originaldate` tag is still read. `PROBE_SCHEMA_VERSION` 5→6 so the incremental scan re-probes and the affected rows drop the mux date, after which the metadata backfill fills the real year from the provider it had already matched correctly (admission is TTL-based, so no manual re-admit). The libav tests use REAL ffmpeg-generated fixtures rather than a hand-built tag map, because the defect was in WHICH container keys get consulted — precisely what a mocked map assumes instead of testing; ffmpeg stamps `creation_time` into an MP4 unprompted, so the trap is present without asking for it. Disarmed, both backends report `Some("2026-07-29T11:04:12.000000Z")` where `None` is required. Instrumentation shipped first and separately: `pharos_metadata_field_source_total{field,provider}` records which of the four providers actually supplied each dated field — the merge is first-Some-wins and nothing recorded the winner, which is why a wrong year and a right one looked identical.

B170|2026-07-30|EVERY BOOK WAS UNOPENABLE, AND CLICKING PLAY DID NOTHING WHATSOEVER. 004-books emitted `Path` only when the client's `Fields` asked for it, matching how the field behaves for every other kind. jellyfin-web ships three book readers — `bookPlayer` (epub.js), `comicsPlayer` (libarchive.js), `pdfPlayer` (pdf.js) — and each one's `canPlayItem` is a test on `item.Path`'s extension. The item they test is the one the DETAILS page fetched, and that request sends `/Users/{uid}/Items/{id}` with **no `Fields` at all**; the `Fields=CanDownload,Path` seen in the bundle belongs to the episode download button, which is what the original research read it as. So `Path` was absent exactly where it decides, `canPlayItem` returned false for all three readers, and `playbackManager` declined: no iframe, no `/Items/{id}/Download` request, no console error, nothing in a server log. The failure had no observable symptom of any kind — which is the class the whole feature was written to defend against, built into the defence. Found only because the acceptance test was automated in real Chromium instead of being called a manual step; before/after on the same fixture: iframes 0→1, reader containers 0→3, chapter-one marker false→true, `/Download` never requested→requested.|B170/V117. `Path` is emitted unconditionally for `MediaKind::Book` and stays `Fields`-gated for every other kind. Also fixed alongside: `BaseItemDto` had no `CanDownload` field at all despite the readers asking for it by name. Tests split accordingly — `a_book_always_carries_its_path` asserts `Path` on BOTH an unfielded list and `/Items/{id}` (the exact route that was broken), while `path_is_absent_for_a_non_book_unless_the_client_asks_for_it` keeps the gate honest for a movie; the V69 case-spelling test moved onto the movie, because run against a book it would pass no matter what `fields_requests` did. Disarm-verified (`false &&` on the book clause → 2 red). Golden diff inspected before regeneration: exactly 368 `"CanDownload": true` additions and no `Path` leak into any non-book payload.

B171|2026-07-30|THE BOOK DOWNLOAD ROUTE WOULD HAVE ADVERTISED A ZERO-LENGTH HEAD. `/Items/{id}/Download` is how all three readers fetch a book's bytes, and a HEAD that answers `Content-Length: 0` tells a client there is nothing to fetch. This is B166/B101 on a third route: actix's h1 encoder derives the header from the body's `BodySize` and discards a hand-set one, so any `.finish()`-shaped HEAD path reproduces it. Caught in review rather than in production because V113 already named the class — the value of writing the invariant down.|B171/V113. The route is built on `NamedFile`, whose `SizedStream` makes the declared length structural rather than something a later edit can drop. Asserted at the `BodySize` level, not the header, for the reason V113 records: the in-process test harness never materialises the header, so a header assertion reads `None` either way and passes on the bug.

B172|2026-07-30|EVERY PART-WATCHED FILM REPORTED 0% PROGRESS ON THE ROUTE THE GRID RENDERS FROM. `BaseItemDto::from_domain_with_user_data` built its `UserData` through `UserItemDataDto::from_domain`, whose runtime argument is a hardcoded zero, while holding an item whose `run_time_ticks` had been computed two lines above. B36 had fixed exactly this for the session and user-data routes and left the ITEM routes — `/Items` and `/Users/{uid}/Items/{id}` — untouched, so a card's resume bar drew empty for a film watched half through. The value was always right there; it simply was not passed. Found by a 004-books test asserting that a BOOK reports 0% honestly (no runtime, so no percentage): the counterweight film assertion, present so an "always 0" implementation could not pass, went red instead.|B172. `from_domain_with_user_data` now passes the item's own `run_time_ticks`. A book's runtime is 0, which yields 0.0 rather than a NaN — serde writes NaN as `null` and a client draws a null percentage as a full or broken bar, which reads as corrupted state rather than as an absent duration. Goldens moved by exactly one field: 58 `PlayedPercentage` values from `0.0` to a real non-zero, with no other field disturbed.

B173|2026-07-30|ONE CORRUPT PDF WOULD HAVE TAKEN THE SERVER DOWN. `lopdf` 0.34 overflows the stack on deeply nested PDF objects (RUSTSEC-2026-0187, CVSS 7.5), and 004-books put it on the scan path — which parses whatever a user drops in a media folder, so the input is untrusted by construction. A stack overflow ABORTS the process: it is not a `Result` any caller can handle, so V6 ("a bad file is logged and skipped; the scan continues") cannot hold against it, and on the deployment the scanner runs in-process with the server. Never reached production — CI's cargo-audit failed the PR, which is the entire reason that job exists.|B173/V6. Upgraded to lopdf 0.44 — not the 0.42 the advisory asks for, because 0.42 and 0.43 depend on `ttf-parser` unconditionally and that crate is itself flagged unmaintained (RUSTSEC-2026-0192), which `cargo deny check` treats as an error where `cargo audit` only warns. 0.44 makes it optional, so with default features off it leaves the graph entirely rather than being tolerated by an ignore entry — pharos reads the page tree, the info dictionary and image XObjects, and never asks lopdf to touch a font. 0.42+ also drops the parser feature flags 0.34 forced. `a_deeply_nested_pdf_does_not_take_the_process_down` builds a PDF holding 50k nested arrays and requires the reader to return at all; nextest's process-per-test means a regression surfaces as a CRASHED test rather than a failed assertion, which is the only honest signal for a class that by definition cannot be caught. Disarm-verified by pinning back to 0.34: SIGABRT, "has overflowed its stack". `embed_image` stays off and the `image` crate is confirmed absent from the lock, so R11's no-raster-decoder premise is unchanged.

B174|2026-07-30|EVERY TYPED LIBRARY WAS EMPTY AND ALL 14,229 ITEMS SAT IN ONE MIXED PILE. `backfill_library_ids` stamped `library_id` by iterating `libraries()` and letting each root overwrite the last, on a stated assumption: "Longest-root-wins is not needed (roots don't nest in practice); a later root simply overwrites if it does." They do nest — a whole-share `mixed` root beside typed libraries for its subfolders is the obvious way to configure this, and it is exactly what the deployment does. `libraries()` orders by NAME, `media` sorts after `Books`/`Movies`/`Music`/`TV Shows`, so the containing root was applied LAST and swallowed everything inside it. Live: `media|mixed|14229` against `Movies|0`, `Music|0`, `TV Shows|0`. Nothing errored; the library simply rendered as one undifferentiated grid, which looks like a configuration choice rather than a defect. Found while adding a `books` library for 004-books — the config change would have silently done nothing, which is how the underlying bug surfaced at all.|B174/V120. Roots are now applied SHORTEST-FIRST in both backends, so the most specific library has the last word; ordering by name was never meaningful for assignment, only for display, and `libraries()` keeps its name order for that. Test `a_nested_library_beats_the_root_that_contains_it` reproduces the exact ordering trap — the containing root is deliberately NAMED so it sorts last, because a test whose names happened to sort the other way would pass against the bug — and asserts the third case too: an item directly under the mixed root and in no typed library must still be claimed, or the fix trades one silent gap for another. Disarmed, the Books library comes back `[]`, which is the production symptom exactly.

B175|2026-07-30|OPENING A BOOK TOOK BETWEEN 1.3 AND 35 SECONDS, AND NONE OF IT WAS THE SERVER. Reported as "the performance of opening the book is pretty awful". pharos was exonerated first, by measurement: a 24 MB Kindle book converted COLD and delivered in 160 ms TTFB / 384 ms total in-cluster, 223 ms through the public ingress, 8 ms warm; the converted EPUBs are structurally sane (26 chapter parts, compressed, no giant blob). The cost is entirely in the reader jellyfin-web ships. `bookPlayer` renders the book, hides it behind `opacity: 0`, and keeps the spinner up until `book.locations.generate(1024)` resolves; epub.js 0.3.93 — the version jellyfin-web 10.11.8 pins, and still the newest release — ends `Locations.process` with `this.processingTimeout = setTimeout(() => completed.resolve(…), this.pause)` and defaults `this.pause = pause || 100`, which jellyfin-web never overrides. So every book pays a flat 100 ms of IDLE WAIT per linear spine section before anything becomes visible. Measured against six real library books at **101.1 ms per section**, invariant to file size (a 26-section 24 MB book opened faster than a 30-section 159 KB one) and unchanged by `--disable-background-timer-throttling` — a sleep, not work. Deployed: The Docker Book 1.3 s (12 sections), The Prince 3.1 s (30), The Way of the Web Tester 12.7 s (121), Learn D3.js 35.1 s (342). Worst affected are NATIVE `.epub`s, not the converted Kindle ones. No server-side signal could ever have shown this: the request the reader makes completes in milliseconds and the wait begins after it.|B175/V124. `jellyfinWebBundle` in `flake.nix` rewrites the constant to 0 in the epub.js chunk before the bundle enters the image, keeping the `setTimeout` so each section still yields to the event loop and the tab stays responsive. Measured 4.6-5.6x faster to open across the same six books: 1.3 s→0.29 s, 3.1 s→0.59 s, 12.7 s→2.5 s, 35.1 s→6.2 s. The rewrite asserts exactly one matching file and exactly one matching site, then asserts BOTH post-conditions (old constant gone, zero pause present) and fails the build otherwise — the identifier is minifier-chosen, so a nixpkgs bump can rename it, and a patch that silently matched nothing would restore the full slowness with every test still green. `the_served_epub_js_has_no_per_chapter_sleep` covers the other half, that the bundle actually served is the patched one, which a `bundle = pkgs.jellyfin-web` regression would break without troubling the derivation; pointed at stock upstream it fails with `"100"`. The devShell's `JELLYFIN_WEB_DIR` was moved onto the same derivation so the compat suite exercises what production serves.

B176|2026-07-30|MARKING SOMETHING UNWATCHED LEFT IT RESUMING FORTY MINUTES IN. `op_unmark_played` set `played = false` and left `last_played_position_ticks` untouched, so an item could report `Played=false` beside a live resume point. jellyfin-web draws the resume bar and the Continue Watching row off the POSITION, not the flag, so a card marked unwatched still showed a progress bar, still sat in Continue Watching, and still started part-way in when played. Jellyfin's own `MarkUnplayed` clears both. Surfaced sideways: the 004-books `/Download` spec began timing out after the epub.js patch (B175) sped the reader up, because the page-turn spec before it left a read position on the same seeded book and `bookPlayer` resumed to chapter two — the flag and the position are one answer to "have I seen this", and a mutation that moves one and not the other leaves the pair contradicting itself.|B176. `op_unmark_played` clears the position too. The test drives the position through `/Sessions/Playing/Progress` — the route a client actually uses, so the value survives the same path it does in production — and asserts on both the item's `UserData` and on Continue Watching, the surface the stale position was visible on. Disarmed, it fails with `left: Number(50000000)`. The books spec now clears the position itself rather than inheriting whatever the previous spec left, so its precondition is stated instead of assumed.

B177|2026-07-31|EVERY COLD SEGMENT WAS 3.4x SLOWER THAN IT NEEDED TO BE, BECAUSE PHAROS COMPETED WITH ITSELF. Found by profiling live playback rather than from a report. One client segment request launches itself plus six speculative prefetches, and all seven were admitted to the single GPU at once; the client's own segment then got a seventh of the encoder. Measured on the deployment, same file, same rung, idle GPU: with the prefetch window already warm one job runs alone and the segment costs **1 860 ms**; with the window cold, seven jobs are admitted and the same segment costs **6 358 ms** (mean encode across the seven: 6 229 ms). `queue_wait_ms` and `lock_wait_ms` were 0 in every observed case and `transcode_ms ≈ encode_ms`, so none of it was queueing — the cost was inside ffmpeg, competing with work nobody was waiting for. B108's `background_headroom` was aimed at exactly this and cannot reach it: it reserves a PERMIT, which is what a client's segment needs to start, and says nothing about the encoder throughput it needs to finish. The same profiling showed cost is decode/IO-bound, not encode-bound — 360p costs the same as 1080p for a 16x smaller output — so the crowding is not recovered by serving a cheaper rung.|B177/V125. A speculative job now skips a device already carrying a client's segment plus `background_alongside_client` (default 1) of its own kind, and tries the next device or is shed. Deliberately not zero: prefetch is shed rather than queued, so what is refused is never retried, and starving it entirely would make every segment a cold miss — the opposite bug, which `speculative_work_may_fill_an_idle_device` pins. Instrumentation shipped first and separately: `JobDone.peer_jobs` and `pharos_transcode_peer_jobs{class,device}` record what each job encoded BESIDE, because `encode_seconds` rising cannot distinguish a heavier source from a crowded device and the two want opposite fixes. The fix test asserts what joined the CLIENT'S device rather than what was admitted at all — a prefetch on an otherwise idle second device costs the client nothing — and disarmed reports 3 joining instead of 1.
